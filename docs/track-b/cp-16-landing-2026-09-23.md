# CP-16 landing and repository consolidation — 2026-09-23

**LAND complete; CP-16 administratively closed. Local only; no publication or model promotion.**

The Owner explicitly asked the assistant to squash the CP-16 branch, commit the pending
main-checkout work in separate ordered commits, and leave a clean repository with only main
and its primary checkout. This task-specific instruction authorized the Git operations
normally reserved to the Owner; it does not amend the standing governance rules.

## Landing and preservation

| Identity | Value |
|---|---|
| Starting main | `6621402b2be9aed85be433432bcffffb56adf3e4` |
| Reviewed candidate | `bf3ca602e32e99e45c7835e3f95148f62b608099` |
| Evidence tip / `evidence/cp-16` | `5ec8a92a4032569b31a1a4f0bb3c512793d15a78` |
| Squash landing / `land/cp-16` | `d62769c0cf23c5c252213da92d7f39684e4aa284` |

The staged squash tree and landing tree were exactly equal to the evidence-tip tree:
97 changed files relative to starting main. The candidate-to-evidence delta contains only
CP-16 evidence paths. Both lightweight tags were created and the final and failed candidates'
reachability from the evidence tag verified before branch/worktree removal.

There were **23 pending files** initially: four byte-identical CP-16 overlaps and **19 other
files**, rather than the externally estimated 20. Every pending file was backed up and hashed.
Only the four verified overlaps were temporarily restored/removed to permit the squash;
the squash restored their exact bytes. All 23 original contents were then committed unchanged.

The ordered commits following the research landing preserve distinct work:

1. `5947055` — project-local recovery rules, ignore rules and historical archive references.
2. `763eb54` — Q&A append-tool fix, its regression tests and existing Owner/interview edits.
3. `764a7d3` — programme plan, independent reviews, decision packet and issued handoff.
4. `9c734ba` — blocked/PASS receipts and the pre-landing programme-state record.
5. The commit containing this document — landing/closure and operational citation updates.

Eight existing Q&A-tool regression tests passed; the real Q&A file was not changed by this
task. No CP-16 experiment or scientific verification was rerun. Landing verification checked
Git identities, file preservation, exact tree equality and evidence reachability.

## Reclamation and citation map

After preservation checks, the clean `.local/worktrees/cp-16/lead` checkout and
`gauntlet/cp-16` branch were removed. The only ignored files in that checkout were ten
disposable Python bytecode files. Only local `main` and `/Users/djourno/Downloads/PJM` remain.

| Historical reference | Current retrieval |
|---|---|
| `gauntlet/cp-16` reviewed chain | `evidence/cp-16` |
| `.local/worktrees/cp-16/lead/<path>` | The same relative path in the primary checkout; exact historical bytes via `git show evidence/cp-16:<path>` |
| Final candidate or failed-attempt SHA | Unchanged SHA, reachable through `evidence/cp-16` |
| Squashed experiment | `land/cp-16` |

**Reproduction, noted 2026-09-24:** run CP-16 commands in a checkout of `evidence/cp-16`, as
its `reproduce.md` requires ("the reviewed checkout"). On main, `capstone_v21.md` is now the
later ratified v21-r4. CP-16's `check_protocol` and its supplied-input identity test therefore
refuse there by design. `tests/cp16/conftest.py` skips that test with this reason.

The issued brief, handoff, locked anchor/amendments and scientific evidence retain their
original bytes and historical execution references. They are completed-run records, not
instructions to resume a deleted checkout. Current operational status is in `progress.md`
and this document; receipt/packet notices explicitly supersede old retention instructions.
Do not rewrite the reviewed evidence to describe later landing events.

The pre-existing GitHub Desktop stash is preserved unchanged. Ignored recovery archives are
also retained. The task backup at `.local/artifacts/cp16-landing-2026-09-23/` contains the
original pending files, SHA256 manifest, binary diffs, pre-operation Git bundle, staged-tree
checks and final validation/accounting. It is local recovery material, not an off-device backup.

## Scientific status and next routing

Engineering PASS remains accepted. H−B2 meets the **exploratory, development/post-selection**
joint-improvement rule; H−P does not. Both H and P fail original §8 criteria 1–2 and meet
3–6. Neither a new product threshold nor product qualification follows from landing.
The earlier FAIL, monitoring limitations and all resource debits remain preserved.

Recommended next work is bounded weather-archive admission under programme §4.1 and a local
research presentation from accepted CP-15/16 evidence. Admission requires actual field/lead,
availability and decoding evidence, not archive start dates alone. Neither task is launched
or authorized by this landing. No new model experiment, CP-17 freeze or prospective clock.

No push, remote-ref mutation or public-surface update occurred. Local README/site sources
already discuss CP-15 and remain stale for CP-16; their research update is separate work.
