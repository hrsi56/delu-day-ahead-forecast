# CP-16 evidence chronology and limitations

The current execution resumes the existing checkpoint under v21-r3. `attempt-1/` preserves the prior report, protocol, manifest, lineage, resource snapshot, independent FAIL and terminal return byte-for-byte. The original raw interrupted lineage, accounting audit, supervisor-failure record and logs elsewhere in this directory also remain historical evidence. Their v21-r2 limits and blocked instructions describe that completed attempt; the exact v21-r3 anchor, amendment and issued brief supply current authority.

No historical gap is retrospectively cured. In the interrupted admission, RSS during the monitor failure is unknown and the original worker exit was not captured by its supervisor. Earlier Builder BLAS/concurrency assertions were unsupported. The cumulative 3,730 policy-day debit and all fit/compute charges stand. Regenerated admission, components and residual state supply current acceptance evidence; the interrupted attempt supplies no outer score or research conclusion.

Resumption repairs: cumulative effort retains a conservative one-hour historical upper bound and adds the resumed session's elapsed time; intervening Owner idle is excluded. The only changed numerical cap is the authorized 9,000 policy-days. Purely synthetic future fixtures have no policy-day charge. Real-data reproductions, failures and controls remain charged before execution. Atomic rename races are tolerated, monitor exceptions terminate the process group, and successful state/vectors are saved per origin. Cache content, origin, input, scale and frozen-protocol identities are verified. Canonical timestamp comparisons normalize storage resolution without changing UTC instants, including restart.

The independent audit that identified cache and partial-save gaps was a bounded read-only engineering audit, not Integration. The saved-result oracle Builder is likewise not the final Critic. The final Integration review must be a fresh session on one exact committed candidate, with cooperative read-only isolation. It must inspect all ten §14.8 items; an unfavorable research result is not an Engineering failure, and Engineering PASS never authorizes promotion or publication.

All results retain development_post_selection status. A65 vintage availability and historical data revisions remain inherited assumptions. Hour/block support threshold 56 dates is a reporting convention, not a power calculation; the 17-day peak is descriptive. Twenty-eight per-hour errors provide no nominal-tail guarantee. Native v1 nine-quantile pinball remains historical and distinct from seven-quantile WIS. Historical CP-15 product status remains NOT_DEMONSTRATED.
