# Verdict — M3.5/CP-3B — Integration — FAIL

- Candidate SHA: f791c4c4db785cf0943d8dfd4cd2d1057afed8eb
- Plan / version / bar: capstone_V6_8.md, v6.8, §9.2 as amended 2026-09-15; CP-3B six-item bar
- Verbatim bar excerpt:
  > **1. WASM export builds and runs.** `marimo export html-wasm` produces the app plus its `assets/` directory. It must be served over HTTP — `file://` does not work. Prove it by serving locally and driving it in a real browser: the quantile selector and the one load-forecast control both respond, and the fan chart renders.
  >
  > **2. THE HARD GATE — model identity.** All four public surfaces assert "the shipped model is exactly the model the holdout evaluated." `mlflow.pyfunc` will not load under Pyodide, so whatever runs in the browser is a re-implementation, and that claim breaks the moment it diverges. Prove equivalence; do not assert it. Commit a fixture of at least 30 delivery days sampled across all three regimes. For every row, the browser path's nine quantile outputs must match the frozen champion bitwise — or, if and only if a difference is demonstrably a platform artifact, to a tolerance you measure, state as a number, and defend. A tolerance chosen to make the test pass is a failure of this item. Give it a positive control. Perturb one CQR threshold and require the comparison to fail.
  >
  > **3. §5.2 delivery-day availability holds in the browser too.** No browser-side helper, demo path or scenario control may reintroduce a row-wise boundary or consume a value unavailable at the 12:00 CET D−1 origin. Reproduce the control: masking delivery-day prices changes output by exactly 0.0; a D−1 mutation moves it. Report the figure.
  >
  > **4. Network dependencies measured and disclosed.** Unlike the Pages export, this page will fetch — Pyodide and its wheels come from a CDN. That is permitted here; item 3's zero-runtime-calls bar governs `docs/index.html` only, and that property must not regress (test 19 guards it). What is not permitted is leaving it unmeasured: report the exact hosts, request count, and total transferred bytes on a cold cache, and put the cold-load size on the Space card in plain language. If first load is heavy, publish the number, not an adjective.
  >
  > **5. Honest labelling, carried from CP-3 unchanged.** Whatever renders over the holdout period is a historical out-of-sample replay and must say so on the page — never a live forecast. Scenario perturbations stay labelled ceteris-paribus sensitivity probes that may be out of distribution. Every number renders from `claims.py` — retype nothing; add claims there if you need new ones, and keep `make verify` passing at zero disagreements.
  >
  > **6. One fresh Integration Critic PASS** on a final candidate SHA, from a clean detached worktree, with a verdict-only delta to the evidence tip.
- Suspended clause confirmed verbatim in §9.2 at this SHA: yes — capstone_V6_8.md line 342, identical words; the file additionally bolds "run in server mode".
- Worktree clean before and after: yes — `status --porcelain` empty and HEAD f791c4c4db785cf0943d8dfd4cd2d1057afed8eb before, after `make wasm`, after pytest/verify/container, and at the end. Only gitignored byproducts exist (.venv/, app/public/, app/__marimo__/, dist/, two `__pycache__/`).

## Commands actually run

| # | Command | Exit | Observed |
|---|---|---|---|
| 1 | `git -C /tmp/critic-cp-3b status --porcelain` / `rev-parse HEAD` | 0 / 0 | empty / f791c4c4db785cf0943d8dfd4cd2d1057afed8eb |
| 2 | `uv sync --locked --dev` | 0 | Python 3.13.15, lightgbm 4.7.0, marimo 0.24.2, mlflow 3.16.0 |
| 3 | `MLFLOW_DISABLE_AGENT_HINT=1 uv run make wasm` | 0 | payload 31,192,669 B/15 files; fixture 43 days/1031 rows/2 fail-closed; gate max dev 0.0, 0 null mismatches; 3 controls broke gate; §5.2 masked 0.0, D−1 220.9433; dist/space-wasm 738 files; removed stray CLAUDE.md from export root. `git status --porcelain` after: empty |
| 4 | critic script `item2_independent.py` (frozen champion via `load_champion`; shipped `app/public/browser_champion.py` exec'd from its bytes, as the notebook does) | 0 | fingerprint 57e3ad40…e0fb; thresholds and all nine booster texts equal the frozen object; 43 fixture days: 9,261 cells, float64 bits equal, 0 null mismatches; committed fixture values == fresh `forecast_delivery_day` bit-for-bit (all 43 days); out-of-fixture days the shipped slice supports (2020-06-30, 2022-07-31, 2026-03-24, 2026-08-25): bits equal; other 120 slice days fail closed to all-NaN |
| 5 | critic `item2_generality.py` (payload-shaped slices built with the builder's encoding) | 0 | 99 days incl. 25-h fall-back days 2021-10-31/2022-10-30/2025-10-26, days after fall-back, spring-forward days, holidays, bridge days (2023-10-02, 2024-10-04, 2025-12-26, 2026-01-02, 2026-05-15), null-load days, 60 random days: 20,241 cells, 0 divergent days |
| 6 | critic `item2_holdout.py` vs committed `reports/cp2/holdout_predictions.parquet` `final_*` | 0 | all 90 holdout days, 19,440 cells, 0 bitwise-divergent days |
| 7 | rsync to scratch copy (no .venv/.git/dist/app/public) + own `uv sync --locked --dev`; `delu_forecast.__file__` resolves to the copy | 0 | baseline gate identical to worktree |
| 8 | `mutate.sh` (payload → `verify_wasm_equivalence.py` → `pytest tests/test_22`) | — | M1 `price_lag_24h`→same-day read: verify 1 (151.67, 9 null mismatches), masked control fails; M2 rolling window +1 h: verify 1 (17.76), masked control fails; M3 bridge-day weekday 4→3: verify 0 (max dev 0.0); M4 720 h std ddof 0: verify 1 (17.13) |
| 9 | M3 module through critic slices | 0 | diverges by 5.75 / 8.33 / 3.19 / 3.35 EUR/MWh on 2024-10-04 / 2025-12-26 / 2026-01-02 / 2026-05-15 — fixture gate is blind to calendar logic |
| 10 | M5: one CQR threshold (p25\|p75) +1e-12 in shipped champion.json | 1 / 1 | verify exit 1 (max dev 1.02e-12); `test_…_bitwise` fails → tolerance genuinely 0.0; grep finds no approx/isclose/atol on the gate |
| 11 | critic `item3.py` on shipped payload | 0 | mask D=2026-09-06 prices: bitwise identical, 0.0; D−1 all hours +250: 220.9433340396028; D−1 hour 23 only: 273.71; all 43 fixture days' own prices NaN or 1e6: identical; per-hour sweep of D: identical; later days masked: identical; load ×1.06: 10.46 |
| 12 | `uv run pytest -q -p no:cacheprovider` | 0 | 174 passed |
| 13 | `uv run python scripts/verify_release.py` | 0 | disagreements: none (README, Space card, Static Space card, Pages export, MLflow record); docs/index.html 0 external fetching refs |
| 14 | `uv run python scripts/verify_container.py --out /tmp/critic-cp3b-container.json` | 0 | passed true: build, run_offline, offline_probe (all hosts unreachable, /health 200), cli_offline, marimo_server_mode |
| 15 | `git diff --stat b1bce877…..HEAD -- models/ data/snapshot.parquet reports/cp2/` | 0 | empty |
| 16 | `measure_wasm_load.py --port 8847` + Browser pane at http://localhost:8847 | — | page ran; wire log 314 same-origin requests, 14,205,139 B gzip = record exactly; raw 38,295,272 B. In-page CDN bytes were cache-contaminated (2nd origin critic.localhost:8849 reported jsdelivr 13,872,932 B = the scipy wheel alone), so CDN bytes re-measured with curl |
| 17 | curl of the 18 Pyodide v314.0.0 files (Accept-Encoding br) and marimo lock | 0 | ≈31.76 MB brotli bodies (record 31,898,711 B, within 0.5 %); lock 113,533 B = record |
| 18 | curl probes of six public HF Static Spaces (dalle-mini, CompVis license, nanotron playbook, three recent), Accept-Encoding gzip/br/zstd, browser UA | 0 | no `Content-Encoding` on any HTML/JS body (e.g. distill.bundle.js 329,312 B plain; html2canvas.js 437,922 B plain). `huggingface.co/spaces/<id>` is a Hub wrapper that iframes `<id>.static.hf.space` and loads scripts from huggingface.co, cdnjs.cloudflare.com and an AWS WAF edge SDK host |
| 19 | `uv run make wasm-serve` (8820) + Browser pane | — | identity panel: 43 days, 1031 rows, 9261 values, undefined reproduced True, max dev 0.0, "bitwise identical"; 95 % click → band redrawn, "coverage … 0.9398"; slider 1.06 → median+band redrawn, "Scenario active … sensitivity probes …", dashed outcome line hidden; back to 1.00 → original polylines restored exactly, dashed line back; console errors: none |
| 20 | scratch copies of dist/space-wasm with served series.json altered, Browser pane | — | D prices (2026-09-06) NaN → in-page check over all 43 days "0.0, bitwise identical"; D−1 (2026-09-05) +250 → "DIVERGED by 220.9433340396028" (= host to full precision) |
| 21 | docs/ via `python3 -m http.server 8851` + Browser pane | — | 7/7 images data: URIs and decoded; 0 Resource Timing entries; one request (the document) |
| 22 | `scripts/build_space.py` in pristine scratch copy | 0 | space/README.md identical to candidate; `reports/cp3/space_bundle.json` total_bytes 37,180,424 vs committed 37,177,119 |
| 23 | GET `huggingface.co/api/spaces/Yarden-Viktor/delu-day-ahead-forecast`; `git branch -r --contains HEAD` | 0 | 200: public Static Space created 2026-09-15T00:37:13Z holding only HF template files (.gitattributes, README.md, index.html, style.css); nothing from the bundle. No remote ref contains the candidate; origin/main = b1bce87 |

## Evidence actually inspected

capstone_V6_8.md §9 (lines 328–400) and line 25; AGENTS.md; Makefile; .gitignore; .dockerignore; .github/workflows/tests.yml; app/browser_champion.py; app/wasm_showcase.py; src/delu_forecast/{features,postprocess,conformal,model,showcase}.py; the WASM parts of claims.py (lines 85–112, 290–440); scripts/{build_wasm_payload,verify_wasm_equivalence,build_wasm_space,measure_wasm_load}.py; the head of verify_container.py; tests/test_22, test_23 and test_19; reports/cp3b/{equivalence,payload,browser_run,network}.json, the head of cold_load_paths.jsonl; space-wasm/README.md; README.md lines 155–195; space/README.md lines 18–40; docs/deploy.md lines 1–40; the header of app/public/boosters/p50.txt, champion.json, calendar.json, fixture.json; a raw scan of docs/index.html; the diffs b1bce87..HEAD of docs/index.html, app/showcase.py, surfaces.py, verify_release.py, build_pages.py, check_links.py and three cp3 records. I also read the rendered text of the live page in the browser and the Hub wrapper HTML of a public Static Space. **Disclosure:** while tracing a record's history I ran `git show --stat HEAD`, which printed the builder's commit message (about a `.pyc` fix). Nothing in this verdict relies on it.

## Checklist verdict

| # | Checklist item | Verdict | Evidence |
|---|---|---|---|
| 1 | WASM export builds and runs | PASS | `make wasm` exit 0 → index.html + assets/ (712 entries) + public/. Served over HTTP (8820 and 8847) and driven in the Browser pane. The quantile selector responds (95 %: band redrawn, coverage annotation 0.9398) and the load control responds (×1.06: median and band recomputed, probe label shown, outcome line hidden; restored exactly at 1.00). Fan chart: one inline SVG, 1 band polygon, median and outcome polylines. 0 console errors. |
| 2 | Hard gate — model identity | PASS | Recomputed independently, not taken from test_22. Browser path equals the frozen `mlflow.pyfunc` output bit for bit on all 43 fixture days (9,261 cells) and on 4 out-of-fixture days in the shipped slice. It also matches beyond the slice: 99 days incl. 25-h/23-h days, holidays and bridge days (20,241 cells), and all 90 committed CP-2 holdout predictions (19,440 cells). The committed fixture values equal a fresh recompute. The fixture spans pre-crisis (2020-07), crisis (2022-08) and post-crisis (2026-03 DST window with the 23-h 2026-03-29; 2026-08/09 holdout). Tolerance is really 0.0: a +1e-12 threshold change fails the gate. Real breaks (same-day lag, +1 h rolling boundary, ddof) all fail it. Observed in Pyodide: the in-page check reports 0.0 on 43 days, and reports divergence when fed a mutated D−1. Observations: (a) the fixture's expected values are regenerated into gitignored app/public/, not committed; the day list is committed in code and the summary in equivalence.json. (b) The fixture holds no holiday, bridge or winter day, so a bridge-day regression passes the gate (M3) although the current code is correct there. (c) `cross_build` in equivalence.json is a literal written by the script, not a measurement. |
| 3 | §5.2 availability in the browser | PASS | No row-wise boundary or same-day read in browser_champion.py: lags are calendar-day D−1/D−2/D−7 and fail closed; rolling window is [Berlin midnight(D)−720 h, midnight(D)), one value per day; the probe scales only D's A65 vector. Host, on shipped bytes: masking D's prices changes output by **0.0** (bitwise identical); D−1 +250 moves it by **220.9433340396028**. Observed in Pyodide with altered served payloads: 0.0 "bitwise identical" and "DIVERGED by 220.9433340396028". |
| 4 | Network dependencies measured and disclosed | FAIL | Correct: hosts (cdn.jsdelivr.net, files.pythonhosted.org, wasm.marimo.app, own origin), 337 requests, per-host sums = totals, opaque sizes labelled out-of-band, card in plain language and matching the record, docs/index.html still zero runtime calls. **Wrong: the published cold-load size.** network.json gzips the Space's own files "because Hugging Face serves Static Spaces compressed". Six live HF Static Spaces serve HTML/JS with no Content-Encoding. The same 314 same-origin requests are 38,295,272 B raw, not 14,205,139 B, so a cold first visit is **73,059,660 B (≈73 MB)**, not 48,969,527 B. "About 49 MB" is on the Space card ("Measured on a cold cache: 48.97 million bytes"), the README and the Space link label on docs/index.html. Secondary: every surface links `huggingface.co/spaces/…`, a Hub wrapper that also loads huggingface.co, cdnjs.cloudflare.com and an AWS WAF host. Those are outside the 4-host / 337-request figure. |
| 5 | Honest labelling, carried from CP-3 | FAIL | Correct: the replay label renders on the page and the card; the probe label renders whenever a scenario is active and is on the card; app/public/claims.json == `build_claims()`; `verify_release.py` 0 disagreements; sdk: static. **Untrue rendered statements:** (a) page header "Every number below is computed here, by the champion itself … Nothing is precomputed and replayed at you", and card "every number on the page is computed in your browser, by Pyodide, from the champion's own nine LightGBM boosters". Both are false: the coverage annotation, cutoffs, fingerprint and limitation figures come precomputed from claims.json, and the identity check compares against precomputed outputs. (b) Identity panel "The two undefined rows are the spring-forward day, where the D−1 calendar-day lag has no source hour". This is a retyped count and it is wrong: the null rows are hour 02 of 2026-03-30 (D−1 lag) and 2026-03-31 (D−2 lag), not rows of 2026-03-29. Minor: the probe label, which CP-3's app and the Pages export state unconditionally, is absent at page load; "9 quantiles" and the Pages URL are literals. |
| 6 | One fresh Integration Critic PASS | FAIL | This fresh review of f791c4c from a clean detached worktree returns FAIL, so no PASS exists for this SHA. |

**Boundaries.** docs/index.html: no regression (0 runtime-call constructs, 7/7 embedded images decode, 1 request). Champion, snapshot and reports/cp2: unchanged since b1bce87; fingerprint recomputed = card; no new holdout metric. Dockerfile and `make container-verify`: pass. `build_space.py`: works, but its committed `reports/cp3/space_bundle.json` total_bytes is stale by 3,305 B; the figure is not published. Nothing pushed. **For the owner:** a public Static Space `Yarden-Viktor/delu-day-ahead-forecast` already exists, created 2026-09-15T00:37:13Z, holding only HF template files. Its creator cannot be identified from the repo, and no candidate script calls HF. README and deploy.md still describe creating it as a remaining step. Also: the "sleep" wording on the surfaces is accurate (that Space's runtime reads RUNNING). The docstring of `measure_wasm_load.py` cites `scripts/record_wasm_load.py`, which does not exist.

## On FAIL only
- Single largest meaningful gap: the cold-load size on the Space card, README and docs/index.html link label (about 49 MB / 48.97 million bytes) is not what the Space costs. It assumes Hugging Face gzips the Space's files, and live Static Spaces do not. The same recorded 314 same-origin requests cost 38,295,272 B raw, so a cold first visit is ≈73.06 MB.
- Exact next acceptance test: `tests/test_23` fails unless one of two things holds. (i) The same-origin component of `reports/cp3b/network.json` equals the uncompressed byte sum of the recorded cold-load paths (a replay with gzip off; today 38,295,272 B, total 73,059,660 B). (ii) A committed live probe record shows a Hugging Face Static Space returning `Content-Encoding` for `text/plain`, `application/json` and `application/javascript`. The card, README and docs/index.html label must then render the resulting figure from claims.py, and `verify_release.py` must stay at 0 disagreements. In the same pass, item 5 closes when the page header and card no longer claim every number is computed in the browser, and the fail-closed sentence is rendered from fixture.json (dates, hours and lags), not retyped.
