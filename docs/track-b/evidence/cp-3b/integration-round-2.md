# Verdict — M3.5/CP-3B — Integration — FAIL

- Candidate SHA: 8c39262aad8a34559c78d57fc4d27a6abbcbafcb
- Plan / version / bar: capstone_V6_8.md, v6.8, §9.2 as amended 2026-09-15; CP-3B six-item bar
- Verbatim bar excerpt:
  > **1. WASM export builds and runs.** `marimo export html-wasm` produces the app plus its `assets/` directory. It must be served over HTTP — `file://` does not work. Prove it by serving locally and driving it in a real browser: the quantile selector and the one load-forecast control both respond, and the fan chart renders.
  >
  > **2. THE HARD GATE — model identity.** All four public surfaces assert "the shipped model is exactly the model the holdout evaluated." `mlflow.pyfunc` will not load under Pyodide, so whatever runs in the browser is a re-implementation, and that claim breaks the moment it diverges. Prove equivalence; do not assert it. Commit a fixture of at least 30 delivery days sampled across all three regimes. For every row, the browser path's nine quantile outputs must match the frozen champion bitwise — or, if and only if a difference is demonstrably a platform artifact, to a tolerance you measure, state as a number, and defend. A tolerance chosen to make the test pass is a failure of this item. Give it a positive control. Perturb one CQR threshold and require the comparison to fail.
  >
  > **3. §5.2 delivery-day availability holds in the browser too.** No browser-side helper, demo path or scenario control may reintroduce a row-wise boundary or consume a value unavailable at the 12:00 CET D−1 origin. Reproduce the control: masking delivery-day prices changes output by exactly 0.0; a D−1 mutation moves it. Report the figure.
  >
  > **4. Network dependencies measured and disclosed.** Unlike the Pages export, this page will fetch — Pyodide and its wheels come from a CDN. That is permitted here; item 3's zero-runtime-calls bar governs `docs/index.html` only, and that property must not regress (test 19 guards it). What is not permitted is leaving it unmeasured: report the exact hosts, request count, and total transferred bytes on a cold cache, and put the cold-load size on the Space card in plain language. If first load is heavy, publish the number, not an adjective.
  >
  > **5. Honest labelling, carried from CP-3 unchanged.** Whatever renders over the holdout period is a historical out-of-sample replay and must say so on the page — never a live forecast. Scenario perturbations stay labelled ceteris-paribus sensitivity probes that may be out of distribution. Every number renders from `claims.py` — retype nothing; add claims there if you need new ones, and keep `make verify` passing at zero disagreements.
  >
  > **6. One fresh Integration Critic PASS** on a final candidate SHA, from a clean detached worktree, with a verdict-only delta to the evidence tip.
- Suspended clause confirmed in §9.2 at this SHA: yes — `capstone_V6_8.md` line 342: "The presentation layer is a reactive marimo notebook **run in server mode** (`marimo run` inside the container — not WASM)". The rest of §9.2 (selector + at most one load-forecast control, replay label, probe label, no keep-alive) is present and was judged as standing.
- Worktree clean before and after: yes — `status --porcelain` empty before and after every build/test run and at the end; `rev-parse HEAD` = 8c39262aad8a34559c78d57fc4d27a6abbcbafcb at start and end. Only gitignored byproducts were created (`.venv/`, `app/public/`, `dist/`, `app/__marimo__/`, `__pycache__/`, `.pytest_cache/`).

## Commands actually run

All in `/tmp/critic-cp-3b` unless stated. Scratch work lived in the session scratchpad (outside the worktree).

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `git -C /tmp/critic-cp-3b status --porcelain`; `rev-parse HEAD` | 0 | empty; 8c39262a… |
| 2 | `uv sync --locked --dev` | 0 | |
| 3 | `MLFLOW_DISABLE_AGENT_HINT=1 uv run make wasm` | 0 | 76 s. Payload 15,363,807 B / 14 files. Fixture 54 days / 1,296 rows / 4 fail-closed. Gate max dev 0.0, 0 null mismatches. 3 controls broke the gate. §5.2 masked 0.0, D−1 220.9433. `dist/space-wasm` 738 files; stray `CLAUDE.md` removed from the export root. |
| 4 | `uv run python scripts/build_space.py` | 0 | `dist/space` 100 entries; `git status --porcelain` empty |
| 5 | `git diff --stat b1bce877bfcf61add43a60df638326fd87a2a100..HEAD -- models/ data/snapshot.parquet reports/cp2/` | 0 | empty (b1bce877 is an ancestor of HEAD) |
| 6 | scratch `critic_item2.py` (`uv run`, 5.6 min) and `critic_fp.py` | 0 | independent identity and availability checks (item 2, item 3) |
| 7 | scratch `critic_branches.py` | 0 | fixture calendar-branch coverage from the frozen `build_base_features` |
| 8 | `uv run python scripts/verify_release.py` | 0 | "disagreements: none"; docs/index.html external references 0; PASS |
| 9 | `MLFLOW_DISABLE_AGENT_HINT=1 uv run pytest -q` | 0 | 178 passed |
| 10 | `uv run pytest -q tests/test_19_static_page_is_offline.py` | 0 | 20 passed |
| 11 | `uv run make wasm-serve` (8820), driven in a real browser (Claude Browser pane, Chromium) | — | see item 1 |
| 12 | scratch `critic_svg.py` | 0 | host rebuild of the fan-chart SVG, compared with the browser's |
| 13 | `uv run python scripts/measure_wasm_load.py --emulate-hf --port 8871 --log /tmp/critic-cp3b-final-wire.jsonl` + Browser pane: cold visit, then a repeat navigation, then a fetch/XHR cache probe | — | see item 4 |
| 14 | scratch copies of `dist/space-wasm` with only `public/series.json` altered, served by `python3 -m http.server` on 8881/8882/8883 + Browser pane | — | see item 3 |
| 15 | isolated scratch copy (rsync without .git/.venv/dist/app/public), own `.venv` via `uv sync --locked --dev`, `make wasm-payload`, then scratch `break_driver.py`: 6 variants × (`scripts/verify_wasm_equivalence.py` + `pytest tests/test_22_wasm_equivalence.py`) | 0 | see item 2 |
| 16 | read-only `curl`: `nanotron-ultrascale-playbook.static.hf.space` (index.html, `/`, .js, .png ×3, CDN hop), `xenova-whisper-web.static.hf.space` (728 KB .js, index.html, If-None-Match), HF API tree/space JSON, `cdn.jsdelivr.net/pyodide/v314.0.0/…`, `wasm.marimo.app/pyodide-lock.json` (GET gzip, HEAD), four `files.pythonhosted.org` wheels | 0 | see item 4 |
| 17 | `curl https://huggingface.co/api/spaces/Yarden-Viktor/delu-day-ahead-forecast`; `git -C /tmp/critic-cp-3b ls-remote origin` (read-only) | 0 | Space holds only `.gitattributes, README.md, index.html, style.css`; no remote ref at the candidate SHA, no `gauntlet/*` branch on origin |
| 18 | `MLFLOW_DISABLE_AGENT_HINT=1 uv run python scripts/verify_container.py --out /tmp/critic-cp3b-final-container.json` | 0 | build, run_offline, offline_probe, cli_offline, marimo_server_mode: 5/5 ok |
| 19 | final `git status --porcelain`, `rev-parse HEAD` | 0 | empty; 8c39262a… |

## Evidence actually inspected

- **Plan and policy:** `capstone_V6_8.md` §9.2; `AGENTS.md`, read from the worktree.
- **Code:** `app/browser_champion.py`, `app/wasm_showcase.py` (all cells and rendered prose), `src/delu_forecast/{features,model,showcase,claims}.py`, `scripts/{build_wasm_payload,verify_wasm_equivalence,build_wasm_space,measure_wasm_load}.py`, `tests/test_22_wasm_equivalence.py`, `tests/test_23_static_space.py` (network tests), `Makefile`, `.gitignore`.
- **Records:** `reports/cp3b/{equivalence,payload,network,browser_run,space_wasm_bundle}.json`, `reports/cp3b/cold_load_paths.jsonl`, `models/champion/{champion_card.json,MLmodel}`, `reports/cp2/{holdout_report.json,holdout_predictions.parquet,timings.json}`, `reports/cp3/mlflow_registration.json`.
- **Surfaces:** `space-wasm/README.md` (all of it), `README.md` (WASM paragraphs), `docs/index.html` (raw HTML: tags, attributes, inline scripts), `tests/fixtures/wasm_equivalence_fixture.json` (confirmed tracked).
- **Export:** `dist/space-wasm/` (file sizes, binary sniff against `.gitattributes`, external hosts referenced by the assets, Pyodide v314.0.0 and `wasm.marimo.app` URLs).
- **Pyodide lockfiles:** jsDelivr `pyodide-lock.json` (lightgbm 4.6.0 wasm32, no OpenMP library package) and marimo's `pyodide-lock.json`.
- **Not used:** commit messages were not read. A repository-wide grep for "43 delivery" incidentally printed lines of `docs/track-b/evidence/cp-3b/integration-round-1.md`. Nothing in this verdict relies on them.

## Checklist verdict

| # | Checklist item | Verdict | Evidence |
|---|---|---|---|
| 1 | WASM export builds and runs | PASS | **Build.** `make wasm` exit 0. `dist/space-wasm` has `index.html`, `assets/` (710 files) and `public/`. It was served over HTTP by `make wasm-serve` on 127.0.0.1:8820 and driven in a real Chromium; all cells rendered.<br>**Quantile selector.** Clicked 80 → 95 %. The radio state changed, the band polygon changed, and the note read "Nominal 95 %; … was 0.9398".<br>**Load-forecast slider.** Focused and moved 1.00 → 1.10 with ArrowRight ×10 (aria-valuenow 1.1). The page showed "Scenario active: load forecast × 1.10" plus the probe label, the dashed cleared-price line was removed, and the band and median changed.<br>**Fan chart.** One inline SVG. Its (95 %, ×1.10) polygon and median polyline equal, string for string, a host rebuild made with the notebook's own `fan_svg` on `app/public/browser_champion.py`.<br>An error-only console read on the last page load (8883) returned none; earlier messages had already been dropped from the buffer, so this is not a whole-session console audit. |
| 2 | Model identity hard gate | PASS | **Method.** Recomputed, not taken from test_22 or equivalence.json. The frozen champion was loaded with `load_champion`. `app/public/browser_champion.py` was exec'd from its bytes, and the boosters were decoded as the notebook decodes them (text → b64decode → gunzip → decode).<br>**Payload identity.** Shipped booster text equals the frozen `model_to_string()` for 9/9 heads, and the CQR thresholds are equal. The fingerprint recomputed from the shipped payload is 57e3ad40…e0fb, the same as the card, the frozen object and `holdout_report.json`.<br>**Fixture.** Committed and tracked: 54 days, 1,296 rows, three regimes (2020-07 pre-crisis, 2022-08 crisis, post-crisis). The frozen pipeline confirms calendar-branch coverage: Friday bridges 2024-12-27 and 2025-05-30, Monday bridge 2023-10-02, weekday holidays, days after holidays, day_type 0/1/2, all 7 weekdays, 23/24/25-hour days, summer and winter.<br>**Bitwise equality, host.** The browser path matched the frozen champion bit for bit, with 0 null mismatches, on:<br>• all 54 fixture days against a fresh `forecast_delivery_day` run;<br>• the fixture days against `ChampionModel.predict` over the whole snapshot (no truncation, no masking);<br>• `holdout_predictions.parquet` `final_*` on the 13 holdout days the slice supports (312 rows);<br>• 8 off-fixture days the slice supports (192 rows);<br>• every browser-defined cell in the 302-day slice (13,356 cells). None is defined where the frozen model is NaN; the other 238 slice days fail closed and are never rendered;<br>• the scenario path at 42 scale values.<br>**Tolerance is 0.0.** CQR p25\|p75 +1e-9 fails the comparison. In an isolated scratch copy with its own `.venv`, p10\|p90 +1e-12 in `champion.json` gives gate dev 1.02e-12, verify exit 1, and the bitwise test FAILED.<br>**Real breaks.** Each failed the gate (verify exit 1, and `test_the_browser_path_equals_the_frozen_champion_bitwise` FAILED); the unmodified control passed 15/15:<br>• rolling window shifted +1 h into D: dev 17.755, and the §5.2 control broke too;<br>• bridge weekday 4 → 3: dev 6.311;<br>• DST ambiguous hour fails open: 18 null mismatches;<br>• `<0` → `<=0` in the negative-price count: dev 5.281.<br>**In the browser.** The identity panel read 54 days, 1296 rows, 11628 values, max dev 0.0, "bitwise identical". It printed "DIVERGED by 220.9433340396028" when served a D−1 mutation.<br>**Note.** The `cross_build` block in `equivalence.json` is a literal the script writes, not a measurement. |
| 3 | §5.2 availability in browser | PASS | **Code read.** Prices are read only through:<br>• `_lag`: D−1/D−2/D−7 at the same local hour, failing closed on 0 or 2 rows;<br>• `_rolling`: epoch hours boundary−720 … boundary−1, where boundary is D's Berlin midnight.<br>The scenario scales only D's A65 vector. D's prices feed only the dashed outcome line, shown at ×1.00.<br>**Host, shipped bytes.** D prices set to NaN, +1000 or −5000 on all 54 fixture days: bitwise identical, max diff **0.0**. D−1 +250 on 24 rows: **220.9433340396028**. Also D−2 126.37, D−7 121.83, D−30 14.79; one D−1 hour (23:00) +250: 273.71; D−1 load ×1.5: 0.0.<br>**In Pyodide.** Scratch copies of the export, with only `series.json` altered, were loaded in the page. D (2026-09-06) prices +10000: panel 0.0, "bitwise identical" (the altered file was confirmed loaded via the chart's y-axis). D prices NaN: 0.0, "bitwise identical". D−1 +250: "DIVERGED by 220.9433340396028". |
| 4 | Network measured and disclosed | FAIL | **Cold-load figure: reproduced.** Hosts sum to 57,246,862 B and 345 requests. My own cold visit through the emulator (fresh port 8871) logged app 314 requests / 21,560,133 B and CDN stand-in 8 / 922,341 B, identical to the record. In-page Resource Timing on my first cold load showed jsDelivr 18 / 31,898,711 B, identical. I sized the PyPI wheels at 2,752,144 B, uncompressed, identical.<br>**Serving model: matches the live platform.** On live Static Spaces:<br>• text: 200, uncompressed, ETag, no Cache-Control, ACAO `*`; If-None-Match returns 304;<br>• binary: 302 `no-store`, 1,048 B, to a signed `us.aws.cdn.hf.co` URL whose signature changed on 3 consecutive requests;<br>• minor differences: `.html` has no ETag, and `/` is a 33 B 302.<br>**Card.** Carries "about 57 MB … 345 requests from 5 hosts" and "57.25 million bytes", matching the record. `docs/index.html` still makes 0 runtime calls (raw HTML; test_19 20 passed).<br>**Why it fails: an unmeasured repeat-visit claim.** The page renders "A repeat visit is served from your browser cache." (`app/wasm_showcase.py:434`), directly after "Measured in this browser session". But `network.json` records `repeat_visit.measured: false`. Its own derivation says fonts and images are "fetched again on every visit", which the live no-store, rotating-URL headers confirm. The sentence is hard-coded outside `claims.py`, so it bypasses test_23's guard ("a repeat visit was not measured").<br>**Minor.** `wasm.marimo.app` is recorded at 113,533 B, sourced "HTTP HEAD". HEAD returns 405, and the GET is served gzip at 28,126 B on the wire. That overstates the total by 85,407 B (conservative). |
| 5 | Honest labelling | FAIL | **What holds.** Replay label and sensitivity-probe label are both on first render (before any interaction), and both on `space-wasm/README.md`. `sdk: static`. `verify_release.py` exit 0, zero disagreements. `claims.json` equals `build_claims()` (test_22).<br>**Statements not true of the artifact:**<br>(a) The card says (`space-wasm/README.md:50`, generated at `scripts/build_wasm_space.py:71`): "every number on the page is computed in your browser, by Pyodide, from the champion's own nine LightGBM boosters". That is false. The page renders committed claim-set figures (coverage 0.4407/0.7593/0.9398, the cutoffs, 152 days, limitation figures) and a Resource Timing table. The page itself says the evaluation figures are committed results that are not re-run.<br>(b) The page's repeat-visit sentence (item 4), which is also retyped prose.<br>**Minor:**<br>• "9 quantiles" is retyped in rendered text.<br>• The shipped notebook source embedded in `dist/space-wasm/index.html` says "over 43 delivery days … and a DST transition"; the fixture is 54 days with both directions.<br>• The card's "mutating a D−1 price moves it by 220.9433" is the figure for all 24 D−1 hours +250; one hour moves it 273.71.<br>• The card's "recorded … before the page existed" conflicts with the page source's own 43-day reference. |
| 6 | Fresh Integration Critic PASS | FAIL | This verdict, on 8c39262a… from a clean detached worktree, is FAIL.<br>**Boundaries (all hold):** frozen-artifact diff empty; container 5/5; `build_space.py` and `dist/space` OK; nothing deployed (the HF Space holds template files only); nothing pushed (no origin ref at the candidate SHA); `docs/index.html` has not regressed. |

## On FAIL only

- **Single largest meaningful gap:** the Static Space's public surfaces print statements not true of the artifact, and one is exactly the unmeasured network claim item 4 forbids.
  - `app/wasm_showcase.py:434` renders "A repeat visit is served from your browser cache." Yet `reports/cp3b/network.json` records `repeat_visit.measured: false`. The same record derives that fonts and images are re-fetched on every visit, which the live Static Space headers confirm: a 302 with `cache-control: no-store` to a signed URL that changes per request.
  - `space-wasm/README.md:50` tells visitors that "every number on the page is computed in your browser … from the champion's own nine LightGBM boosters". The page itself says the opposite of its evaluation figures.
  - Both are one-sentence fixes, and neither touches the model.
- **Exact next acceptance test:** on a new candidate SHA, run `uv run pytest -q tests/test_23_static_space.py::test_no_surface_states_an_unmeasured_repeat_visit tests/test_23_static_space.py::test_the_card_attributes_only_browser_outputs_to_the_browser`. It must pass, where:
  - **(a) Repeat-visit test.** Collect every string constant of `app/wasm_showcase.py` (ast walk) plus the full text of `space-wasm/README.md` and `README.md`. Fail if any sentence matches `re.compile(r"repeat visit[^.]*(served|come|comes|load|loads|download)[^.]*cache", re.I)` while `network.json["repeat_visit"]["measured"]` is false. Positive control: re-inserting "A repeat visit is served from your browser cache." into the collected text must make it fail.
  - **(b) Card test.** Fail if `space-wasm/README.md` contains "every number on the page is computed". Positive control: today's card fails it.
  - **Then:** `MLFLOW_DISABLE_AGENT_HINT=1 uv run make wasm` exit 0; `git status --porcelain` empty; full `uv run pytest -q` green; `uv run python scripts/verify_release.py` at zero disagreements; one fresh Integration Critic PASS on that SHA.
  - **Recommended alongside (not blocking):** record `wasm.marimo.app` at its gzip transfer size, with the method actually used.
