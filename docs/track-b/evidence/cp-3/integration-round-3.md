# Verdict — M3/CP-3 — Integration — FAIL

- Candidate SHA: 4571d9a6c93964fc183a9df753f03ba7d168078f
- Plan / version / bar: capstone_V6_8.md, v6.8, §12 "CP-3 (6 items)"
- Verbatim bar excerpt:
  > **CP-3 (6 items)**
  > - [ ] Bundled champion, CLI (`predict_next_day.py`), container, and marimo app run locally from a clean setup. The champion is registered as a DagsHub MLflow **model version with the `champion` alias** and release/lineage tags — **non-gating**: a registry or metadata-operation outage does not block release and is disclosed.
  > - [ ] The quantile-level selector and at most one simple load-forecast scenario control work, served by direct local inference or a one-dimensional lookup — with no multidimensional precomputed grid, dynamic SHAP, or per-cell OOD system. Scenario perturbations are labeled sensitivity probes that may be out of distribution.
  > - [ ] Static Pages export carries the §10 reading order, performs **zero runtime calls** to the Space or any external service, and renders every asset and link.
  > - [ ] HF Space deploys the **bundled** champion — the same artifact the holdout evaluated — and renders the same release snapshot. Free-tier sleep is disclosed, not performance-gated. Anything the Space shows over the holdout period is labeled **historical out-of-sample replay** on the page itself, never presented as a live forecast.
  > - [ ] README, Pages, Space metadata and MLflow links agree on the selected catalog, metrics, development evidence class, benchmark limitation, champion identity, and the holdout result with its power-qualification label and the statement that the shipped model is the evaluated model. **All four cutoffs — snapshot, raw-model fit, final calibration, holdout — appear separately and identically on every surface.** Limitations and reproduction instructions are complete.
  > - [ ] **One fresh Integration Critic returns `PASS`** on the final candidate from a clean detached checkout, with a verdict-only delta to the evidence tip.

  (Confirmed present in `capstone_V6_8.md` at this SHA, §12 → "### M3 — Showcase and release" → "**CP-3 (6 items)**", around line 483. The excerpt is the citation; the line number is a courtesy and is non-binding.)
- Worktree clean before and after: **yes** (`git status --porcelain` empty at start and at finish; `git rev-parse HEAD` = 4571d9a6c93964fc183a9df753f03ba7d168078f both times; only gitignored byproducts `.venv/`, `.pytest_cache/`, `dist/`, `app/__marimo__/`, `__pycache__/` were created)

## Commands actually run

| Command | Exit | Observed output |
|---|---|---|
| `git -C /tmp/critic-cp-3 status --porcelain` | 0 | empty (before and after review) |
| `git -C /tmp/critic-cp-3 rev-parse HEAD` | 0 | `4571d9a6c93964fc183a9df753f03ba7d168078f` (unchanged) |
| `uv sync --locked --dev` | 0 | full resolve from `uv.lock`, no drift |
| `uv run pytest -q` | 0 | `147 passed in 83.16s` |
| `MLFLOW_DISABLE_AGENT_HINT=1 uv run python predict_next_day.py --level 80 --self-check` | 0 | `passed: True`, `max_abs_difference_when_masked: 0.0`, `positive_control_d_minus_1_changes_output: True`, `max_abs_difference_positive_control: 220.9433340396028`; source `bundled data/snapshot.parquet (no network)`; replay label printed |
| `uv run python scripts/verify_release.py` | 0 | `PASS — every bound claim agrees on all four surfaces; the page fetches nothing`; `disagreements: none` (recorded, not relied on) |
| `uv run python scripts/verify_container.py --out /tmp/critic-container.json` | 0 | `build`, `run_offline`, `offline_probe`, `cli_offline`, `marimo_server_mode` all `[ok]`; image 1.66GB; container run `--network none`; probe: `outbound_network: unreachable (OSError)`, dagshub/huggingface/jsdelivr all unreachable; in-container `/health` 200 `{"status":"healthy"}`; in-container CLI self-check `passed: True` |
| `uv run marimo run app/showcase.py --headless --port 8891 --no-token` + `curl http://127.0.0.1:8891/health` | 0 | `200` `{"status":"healthy"}`; page renders in browser |
| `curl -s "https://dagshub.com/.../registered-models/alias?name=delu-day-ahead-champion&alias=champion"` (no credential) | 0 | `delu-day-ahead-champion` **v1**, `status: READY`, `aliases: ['champion']`, 35 tags |
| `curl https://huggingface.co/api/spaces/hrsi56/delu-day-ahead-forecast` | 0 | `{"error":"Invalid username or password."}` |
| `curl -o /dev/null -w %{http_code} https://huggingface.co/spaces/hrsi56/delu-day-ahead-forecast` | 0 | **401** (3/3 retries) |
| `curl -o /dev/null -w %{http_code} https://huggingface.co/hrsi56` | 0 | **404** (3/3 retries) |
| `curl https://huggingface.co/api/spaces?author=hrsi56` | 0 | `[]` |
| `curl -o /dev/null -w %{http_code} https://hrsi56-delu-day-ahead-forecast.hf.space/` | 0 | **404** (3/3 retries) |
| `curl -o /dev/null -w %{http_code} https://huggingface.co/api/spaces/gradio/hello_world` (positive control) | 0 | **200** — my anonymous client works |
| `curl -o /dev/null -w %{http_code} https://huggingface.co/gradio` (positive control) | 0 | **200** |
| `curl .../spaces/zz-nonexistent-user-<ts>/some-space` (negative control) | 0 | **401**; profile **404**; api `{"error":"Invalid username or password."}`; subdomain **404** — byte-identical to the target's response pattern |
| `curl -L -o /dev/null -w %{http_code} https://hrsi56.github.io/delu-day-ahead-forecast/` | 0 | **404** |
| `curl -L -o /dev/null -w %{http_code} https://github.com/hrsi56/delu-day-ahead-forecast` | 0 | **200** |
| Generator re-run: `build_pages.py && build_space.py && cp3_readme.py && cp2_readme.py && cp2_report.py`, then `git status --porcelain` | 0 | First run: `M README.md`, `M docs/cp2-model-report.md`, `M docs/index.html`, `M space/README.md`. **Diagnosed and cleared** — see below. After `git checkout -- .` + removing the gitignored `models/champion/**/__pycache__` and re-running all five: `git status --porcelain` **empty** |
| Isolated scratch copy at `…/scratchpad/break` with its **own** `uv sync --locked --dev` venv | 0 | `import delu_forecast` resolves to `…/scratchpad/break/src/delu_forecast/__init__.py` — isolation proven, not the worktree's `.venv` |
| Break 1 (`features.py`: rolling window `end=boundary - 1h` → `+ 23h`, a real §5.2 leak) → `pytest tests/test_18 tests/test_13` | 1 | **6 failed, 9 passed** — incl. `test_delivery_day_prices_change_nothing_and_a_d_minus_1_price_does`, and `test_13`'s per-hour sweep `hour 0 of D leaked into a prediction for D` on both catalogs |
| Break 1 → `predict_next_day.py --self-check` | **1** | `delivery_day_prices_change_nothing: False`, `passed: False` (clean worktree: exit 0) |
| Break 2 (README `25.9078`→`25.91`, **first occurrence only**) → `pytest tests/test_17` | 0 | 12 passed — the string still occurs elsewhere, so containment legitimately still holds |
| Break 2b (**all** occurrences) → `pytest tests/test_17`; `verify_release.py` | 1 | **2 failed** incl. `test_positive_control_a_reworded_number_is_caught`; `verify_release.py` → `FAIL — 1 problem(s)` |
| Break 3 (inject `fetch("https://huggingface.co/...")` into `docs/index.html`) → `pytest tests/test_19` | 1 | **2 failed** — `Finding(kind='script network API', detail='fetch(')` |
| Break 4 (delete the MTU-averaging limitation from `space/README.md`) → `pytest tests/test_21` | 1 | **3 failed** incl. `test_positive_control_a_limitation_dropped_from_one_surface_is_caught` |
| Browser: `docs/index.html` served on `127.0.0.1:8899`, network + console log read, both controls exercised | — | Exactly **one** request per load (`GET /index.html`), **zero** after exercising both controls; **no console logs**; 7/7 images `naturalWidth>0`; 12/12 internal anchors resolve; chart polygon drawn; 5 distinct fan geometries across control changes; restoring reproduces the initial geometry exactly |

Independent scripts I wrote (outside the worktree, in the scratchpad) rather than trusting the repo's own checkers: `indep_claims.py` (recompute + grep every bound claim, my own HTML→text), `indep_fingerprint.py` (re-implement the fingerprint hash over the loaded pyfunc), `replay.py` (replay the whole holdout, recompute MAE/pinball/coverage/crossings/DM), `dm2.py` (Newey-West DM from scratch), `pagecheck.py` (page payload vs live inference), `controls.py` (both controls via direct inference), `complete.py` (§10 (11)+(12) completeness), `rounding.py` (alternate-rounding sweep), `verbatim.py` (required paragraphs vs the plan text).

## Evidence actually inspected

**Plan.** `capstone_V6_8.md` §12 (CP-3 bar, confirmed verbatim), §9.1, §9.2, §9.3, §10 (reading order, items (11) and (12)), §10.1, §5.2, §6.2, §7.1, §7.2.

**Source.** `src/delu_forecast/showcase.py`, `claims.py`, `surfaces.py`, `model.py` (`fingerprint`, `CHAMPION_INPUT_COLUMNS`), `metrics.py` (`diebold_mariano`, `newey_west_lrv`), `features.py` (`_calendar_day_lag`, `_daily_price_statistics`), `predict_next_day.py`, `app/showcase.py`.

**Tests.** `tests/test_18_showcase_gate_boundary.py` and `test_13_champion_no_delivery_day_leak.py` read in full; `test_17`, `test_19`, `test_21` exercised via deliberate breakage.

**Artifacts.** `models/champion/{MLmodel, champion_card.json, python_model.pkl, code/}`; `reports/cp2/{holdout_report.json, holdout_predictions.parquet, catalog_selection.json, a69_benchmark.json, dm_development.json, development_pooled_metrics.csv}`; `reports/cp3/{mlflow_registration.json, link_check.json, container_check.json, pages_build.json, space_bundle.json}`; `docs/index.html` (raw, 1,006,064 bytes), `docs/deploy.md`; `README.md`; `space/README.md`; `/tmp/critic-container.json`.

**Live surfaces.** DagsHub MLflow registry (anonymous); huggingface.co (anonymous, with positive and negative controls); hrsi56.github.io; github.com.

### Recomputed, not accepted

- **Fingerprint from the loaded pyfunc.** Re-implemented the hash (catalog ‖ feature list ‖ quantiles ‖ four thresholds ‖ nine boosters' `model_to_string()`) over `mlflow.pyfunc.load_model(...).unwrap_python_model()`: `57e3ad40a7f48bb7896628ce2e5dec61314dea567da9867aede4bf1b0a10e0fb` — identical to `champion_card.json`, to `holdout_report.json`, and to the **live** registry tag. All four agree.
- **The shipped model is the evaluated model — proved, not asserted.** Replayed all 90 holdout days (2026-06-09 .. 2026-09-06, 2160 rows) through `showcase.forecast_delivery_day` with the bundled champion and compared to the committed `holdout_predictions.parquet`: **max absolute difference 0.0, bitwise identical.**
- **Headline metrics recomputed from that replay.** champion MAE 25.9077804410 / naïve 27.7577673611 (→ `25.9078` / `27.7578`, `-6.66%`); champion pinball 6.7082949158 / naïve 13.8788836806 (→ `6.7083` / `13.8789`, `-51.67%`); coverage 0.4407 / 0.7593 / 0.9398; crossing violations 0. Every published string reproduces exactly.
- **DM recomputed from scratch** (daily-vector pinball, Bartlett kernel, lag `floor(90^(1/3))=4`): statistic −8.679848490812, p 1.981482e-18, LRV 61.422551486544, effect −1.047756215853 → `-8.6798`, `1.98e-18`, `-1.0478`. Exact match. (My first pass used `sum/(n−k)` for the autocovariances and diverged; the repo uses the textbook `sum/n`. The repo is right and I was wrong.)
- **Static page payload vs live inference.** `window.__FAN__` decoded and compared against `forecast_delivery_day` at all 11 load scales × 24 hours × 9 quantiles: **max difference 0.0, bitwise identical**; the `actual` series matches the snapshot's cleared prices; `window.__COV__` matches `holdout_report.json`.
- **No surface rounds a number differently.** Swept 0–7 decimal places for 15 published quantities across README, Space card and Pages export looking for an alternate rounding alongside the canonical string: **none found**. (This matters because `surfaces.carries` is a containment test — Break 2 showed it passes while a stale variant coexists.)
- **`<code>`→backtick normalisation is faithful.** It is a pure textual rewrite that cannot alter a digit or a word, so it can convert a false miss into a hit but can never make a *different* number match. It is load-bearing for exactly three claims (`repro_tagged_commit`, `repro_registered_champion`, `repro_duckdb_sql`, which carry Markdown code spans). **Zero** item-5 bound claims match only because of it. Not a mismatch-hider.
- **§5.2 controls are real.** Breaking the rolling-window boundary in a scratch copy with its **own** virtualenv (import path verified to resolve to the scratch `src/`, not the worktree) failed 6 tests including both halves of the paired check and `test_13`'s full per-hour sweep, and drove the CLI self-check to `passed: False` / exit 1. `showcase.gate_feasible_frame` is the single assembly point: it drops every row after D, NaNs D's own prices, and restricts to `CHAMPION_INPUT_COLUMNS`; `load_scale` touches only the target day's `load_forecast_mw` (verified: the only differing column, off-target rows byte-identical, on-target ratio exactly 1.1).
- **Generator idempotency — first-run diff diagnosed and cleared.** The four modified files differed in exactly one value, `champion_dir_bytes`: committed `31,623,247` vs regenerated `31,696,801`. The delta is **exactly 73,554 bytes = the 8 gitignored `.pyc` files** that my own model loads wrote into `models/champion/code/delu_forecast/__pycache__/`. `claims.py` computes this figure with `rglob("*")`, which counts gitignored bytecode. The committed value equals the tracked-file total. After removing the `__pycache__` and re-running all five generators, `git status --porcelain` is **empty** — every committed surface is byte-identical to what its generator emits from a pristine checkout. Not a bar violation (`champion_dir_bytes` is not a bound claim and all surfaces carry the same value), but the generator is not idempotent once the champion has been loaded in the same checkout.

### The agreement table I built myself

Built from `README.md`, `docs/index.html` (my own tag-strip + entity-unescape), `space/README.md`, and the MLflow record — the committed `reports/cp3/mlflow_registration.json` **and** the live registry, which match on all 35 tags with **zero** value differences, same `run_id`, same `source`.

All 30 bound claims (`REQUIRED_ON_EVERY_SURFACE`) — the four cutoffs separately, selected catalog and both arm losses, all holdout metrics, coverage, DM statistic/p/effect, development evidence class, benchmark trio and its limitation, champion fingerprint, snapshot sha256, the power-qualification label, and the shipped-is-evaluated statement — are present on **all four** surfaces. **Disagreements: none.**

The four cutoffs appear separately and identically everywhere: snapshot `2026-09-06`, raw-model fit `2026-04-07`, final calibration `2026-04-09..2026-06-07`, holdout `2026-06-09..2026-09-06`.

§10 item (11): all **10** limitation elements on README, Space card **and** Pages export — none present on only some. §10 item (12): all **7** reproducibility elements on all three. §7.1 holdout-limitation paragraph and §6.2 exchangeability paragraph are **literal substrings of `capstone_V6_8.md`** — verbatim as the plan requires (as are the §7.2 benchmark limitation and the §7.1 DM label). Link discipline: every DagsHub reference on all three surfaces is the `.mlflow` URI (README 4/4, Space card 1/1, Pages 1/1); **zero** gated DagsHub UI paths.

## Checklist verdict

| # | Checklist item | Verdict | Evidence |
|---|---|---|---|
| 1 | Bundled champion, CLI, container, marimo app run locally from a clean setup; champion registered as a DagsHub MLflow model version with the `champion` alias and release/lineage tags (non-gating) | **PASS** | `uv sync --locked --dev` clean; `pytest -q` → 147 passed; CLI self-check `passed: True`, masked-difference `0.0`, positive control `220.94`; champion loads from `models/champion/` via `DEFAULT_MODEL_PATH` — no registry path exists in `showcase.load_champion`, and the container proves it by running the CLI and the app to completion under `--network none`; `verify_container.py` 5/5 ok (build, offline run, isolated probe, CLI, marimo); marimo `/health` → 200 and renders. Registry checked **anonymously with no credential**: `delu-day-ahead-champion` v1, `READY`, `aliases: ['champion']`, 35 tags incl. `release_status=portfolio_release`, `source_run_id`, `code_commit`, `snapshot_sha256`, all four cutoffs, and `artifact_fingerprint_sha256` matching `champion_card.json` and the fingerprint I recomputed from the loaded pyfunc. Registration succeeded, so §9.1's non-gating clause is not even needed. |
| 2 | Quantile-level selector and at most one load-forecast scenario control work, via direct local inference or a one-dimensional lookup; no multidimensional precomputed grid, dynamic SHAP, or per-cell OOD; sensitivity-probe label present | **PASS** | Served marimo DOM carries exactly **one** `marimo-radio` and **one** `marimo-slider` (plus static `marimo-table` displays with `selection=None`); both feed `forecast_delivery_day(...)` — direct local inference. Verified functionally: interval widths strictly increase 40.06 → 83.04 → 145.93 across 50/80/95, an unsupported level raises `ValueError`, scales 0.90/1.00/1.10 give distinct medians (max |Δ| 4.88 and 7.33), `load_scale=1.0` reproduces the unperturbed forecast bitwise, and the probe alters **only** `load_forecast_mw` on the target day. The Pages export uses a **one-dimensional** lookup: `__FAN__.series` is keyed by load scale alone (11 entries), each holding the model's native 24×9 output; the level selector picks two of the nine quantiles already present, so it is not a second precomputation dimension. Exercised in-browser: 5 distinct fan geometries, exact restore. SHAP is static only (committed PNGs + `shap_ranking.csv`); no OOD machinery anywhere. Sensitivity label present verbatim on the Pages export (`#scenario`, revealed exactly when the scenario is active — confirmed by driving the slider off ×1.00) and in the marimo app. |
| 3 | Static Pages export carries the §10 reading order, performs zero runtime calls, renders every asset and link | **PASS** (one defect noted, inherited from item 4) | All twelve reading-order items present as headings: 1 · The data, 2 · Three regimes, 3 · Feature catalog, **3b · spectral view**, 4 · Validation design, 5 · Results, 6–7 · Explainability (both the SHAP summary + top-10 + two dependence plots **and** permutation importance), 8 · Regime-stratified error, 9 · Reliability (three stages), 10 · Next-day forecast, 11 · Honest limitations, 12 · Reproducibility. **Zero runtime calls verified by reading the raw HTML myself**, not via `static_audit.py`: no `fetch`/`XMLHttpRequest`/`WebSocket`/`EventSource`/`sendBeacon`/`import()`/`importScripts`/`Worker`/`eval`/`Function`/storage APIs; no `<iframe>`/`<object>`/`<embed>`/`<link>`/`<meta http-equiv>`/`srcset`/`data-src`/`formaction`/`ping`/`integrity`/`crossorigin`; no inline `on*` handlers; the only URL inside script is the SVG namespace passed to `createElementNS`. Confirmed in a browser network log: exactly one request per load and **none** after exercising both controls; no console messages. All 7 figures are inline `data:image/png;base64` and rendered (`naturalWidth>0`); 12/12 internal anchors resolve; the fan chart draws. **Defect:** of its two outbound links, `https://github.com/...` resolves (200) but `https://huggingface.co/spaces/hrsi56/delu-day-ahead-forecast` returns **401 — the Space does not exist**. That is item 4's failure surfacing here, and it is fixed by fixing item 4. Separately: the export is not live at its own published canonical URL (`hrsi56.github.io/delu-day-ahead-forecast/` → **404**, Pages not enabled). |
| 4 | HF Space deploys the bundled champion — the same artifact the holdout evaluated — renders the same release snapshot; free-tier sleep disclosed, not performance-gated; holdout-period output labeled historical out-of-sample replay | **FAIL** | **No Hugging Face Space exists, and neither does the account.** Anonymous probes, 3/3 retries: `huggingface.co/hrsi56` → **404**; `api/spaces?author=hrsi56` → **[]**; `spaces/hrsi56/delu-day-ahead-forecast` → **401**; `hrsi56-delu-day-ahead-forecast.hf.space/` → **404**. A deliberately nonexistent namespace returns the identical pattern (404 / 401 / `{"error":"Invalid username or password."}` / 404), while `gradio` returns 200 on both profile and space — so my client works and the target genuinely is absent. The repo states the same: `reports/cp3/link_check.json` → `"Deployment is owner-only and was not performed by this checkpoint: GitHub Pages is not enabled and the Space is not created."`, and `docs/deploy.md` → "**Nothing was published:** no Space was created… A Hugging Face account first — it does not exist yet." The item's head — that the Space *deploys* the champion and *renders* the release snapshot — is therefore unmet, and §9.1's non-gating carve-out covers only the registry (which in fact succeeded), not the Space. **Sub-parts that are satisfied and which I verified anyway:** the bundled artifact **is** the artifact the holdout evaluated — fingerprint recomputed from the loaded pyfunc matches the card, the holdout report and the live registry, and the bundled champion replays the committed holdout predictions **bitwise** (max difference 0.0 over 2160 rows); free-tier sleep is disclosed and not performance-gated ("interactive demo — may take ~30 s to wake if asleep" on README and Pages; the Space card discloses "sleeps after inactivity and takes roughly 30 s to wake — that is disclosed, not optimised away"); and the replay label is verbatim on the Space card, the Pages export, the marimo app and the CLI, never presented as a live forecast. Everything except the deployment itself is in place. |
| 5 | README, Pages, Space metadata and MLflow links agree on catalog, metrics, evidence class, benchmark limitation, champion identity, holdout result + power-qualification label + shipped-is-evaluated; all four cutoffs separately and identically on every surface; limitations and reproduction instructions complete | **PASS** | Agreement table built by me from the four surfaces (live registry included): all **30** bound claims present on all four, **zero** disagreements; the live registry's 35 tags match the committed record exactly (same `run_id`, same `source`, no value differences). All four cutoffs appear separately and identically everywhere. **No surface rounds a number differently** (independent 0–7-decimal sweep over 15 quantities). Every metric recomputed from the committed CP-2 artifacts *and* from a full bitwise holdout replay — exact, including the DM triple. §10 item (11): **10/10** limitation elements on README, Space card and Pages export; item (12): **7/7** reproducibility elements on all three; none present on only some (and the positive control in `test_21` fires when one is removed). §7.1 holdout-limitation and §6.2 exchangeability paragraphs are literal substrings of `capstone_V6_8.md`. Every experiment-tracking link uses the `.mlflow` URI; zero gated DagsHub UI paths. The `<code>`→backtick normalisation is faithful and no bound claim depends on it. *Note, not a failure of this item:* `repro_pages_canonical` asserts the Pages report "is the canonical entry point" at a URL that currently 404s — the statement is consistent and complete across surfaces but not yet true of the world; it becomes true when item 4's remedy and step 1 of `docs/deploy.md` are done. |
| 6 | One fresh Integration Critic returns `PASS` on the final candidate from a clean detached checkout, with a verdict-only delta to the evidence tip | **FAIL** | This is that verdict, written from the clean detached worktree `/tmp/critic-cp-3` at `4571d9a6c93964fc183a9df753f03ba7d168078f` (clean before and after, HEAD unchanged). It is **FAIL**, on item 4. |

## On FAIL only

- **Single largest meaningful gap:** The Hugging Face Space required by item 4 does not exist — nor does the `hrsi56` HF account — so nothing deploys the bundled champion or renders the release snapshot, and the Space link published on the README and the Pages export is dead (401). Everything the repository can do without publishing is done and verifiable: the bundled artifact is provably the one the holdout evaluated (bitwise replay), the container runs it offline, the controls work, the surfaces agree exactly, and the registry entry is live. What is missing is the publication step itself, which `docs/deploy.md` correctly identifies as owner-only (account creation is not an action an agent may perform), and which the plan does **not** mark non-gating — §9.1's non-gating clause covers only the registry, and the registry succeeded.

- **Exact next acceptance test:** After the owner creates the `hrsi56` HF account, creates the Docker Space `hrsi56/delu-day-ahead-forecast` (public, CPU basic) and pushes `make space`, and enables GitHub Pages from `main:/docs`, a fresh critic must observe, anonymously and with no credential:
  1. `curl -o /dev/null -w %{http_code} https://huggingface.co/spaces/hrsi56/delu-day-ahead-forecast` → **200**, and `https://hrsi56-delu-day-ahead-forecast.hf.space/` reachable (a cold-start wait is acceptable and disclosed; it is not performance-gated);
  2. the running Space serving a champion whose fingerprint, recomputed from the loaded pyfunc, equals `57e3ad40a7f48bb7896628ce2e5dec61314dea567da9867aede4bf1b0a10e0fb`, with the four cutoffs and the `Historical out-of-sample replay — … This is not a live forecast.` label on the page itself;
  3. `curl -L -o /dev/null -w %{http_code} https://hrsi56.github.io/delu-day-ahead-forecast/` → **200**, serving a page byte-identical to the committed `docs/index.html`, with a browser network log showing zero requests to any external origin and its Space link resolving;
  4. `uv run python scripts/verify_release.py` still exiting 0 with `unresolved: []` in a regenerated `reports/cp3/link_check.json`;
  5. items 1, 2, 3 and 5 re-verified unchanged at the new candidate SHA.
