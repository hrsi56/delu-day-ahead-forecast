# Verdict — M1/CP-1 — Integration — PASS

- Candidate SHA: `368822de297e71f24fd08569815cae0289e65b96`
- Plan / version / bar: `capstone_V6_7.md`, v6.7, §12 M1 CP-1 (10 items)
- Verbatim bar excerpt:

  > **CP-1 (10 items)**
  > - [ ] Required snapshot contains **A44, A65/A01, benchmark-only A69, and A75 aggregate VRE** from 2019-01-01 through the pull date, with the ~25h A65/A01 head gap handled (trim or documented head NaNs) and the snapshot hash recorded. **The successful pull is archive-reachability and snapshot-completeness evidence, not pre-gate availability evidence** — there is no separate feed-existence or archive-depth probe.
  > - [ ] UTC indexing, Berlin 23/25-local-hour identity, PT15M→hour aggregation, chunk stitching with complete bins, missing-quarter fail-closed behavior, and the 2025-10-01 price transition are correct; no nulls in the DA price.
  > - [ ] The frozen `base` catalog is implemented with calendar/regime features, the A65 load forecast, lagged prices and closed-left rolling price statistics; the frozen augmented candidate adds **only** the 42-day, D-2-bounded `residual_load_proxy` (§4.1). **A69 and same-day actual columns are rejected by champion inference.**
  > - [ ] **The five tail partitions are pinned without overlap** (§5.1): Fold 5, Embargo A, the 60-day final-calibration slice, Embargo B, and the final 90-day holdout. `min(fold_5.delivery_date) >= 2025-10-01` is asserted directly rather than derived from a pull date. Calibration and holdout rows are excluded from every development fold and diagnostic; holdout outcomes are excluded from every fit and threshold. Each fold's §6.2 calibration slice carries its own one-day embargo. The partition and exclusion rules are asserted by a committed test.
  > - [ ] **ENTSO-E↔SMARD reconciliation** on a fixed stratified sample covering pre-crisis, crisis, post-crisis and the PT15M transition: DA prices agree within €0.01/MWh (both Reg-543 sourced — disagreement indicates an ingestion bug, not a market fact). The whole archive is not re-compared merely because it is available. *(96/96 sample hours already agreed at the 2026-06-12 spike.)*
  > - [ ] Targeted repository tests pass (§9.4): chunk stitching, a missing quarter, fall-back DST identity, closed-left rolling windows, transition aggregation, the champion/benchmark schema firewall, the spectral FFT-bin assertion, and the residual proxy's 42-day/D-2 boundary plus its DST behaviour on both sides — the target day's repeated/absent local hour, and the window's own 43/41-observation counts across a fall-back and a spring-forward boundary.
  > - [ ] Data leakage audit records every champion feature as KFT or LAG with justification; states the **A65 pre-gate assumption** and the **A75-revision caveat** honestly as assumptions; and confines A69 to the §7.2 benchmark and the §8.3 evaluation stratum.
  > - [ ] Committed snapshot/data folder and README carry the **CC BY 4.0 attribution statement** (ENTSO-E Transparency Platform; Bundesnetzagentur | SMARD.de) and the data cutoff.
  > - [ ] Three spectral figures and one 3–4 sentence interpretation paragraph are committed (§4.2).
  > - [ ] **One fresh Integration Critic returns `PASS`** on the final candidate from a clean detached checkout, with a verdict-only delta to the evidence tip.

- Exact-excerpt check: the unquoted text above occurs exactly once in `capstone_V6_7.md` at the candidate SHA.
- Worktree clean before and after: yes. Initial and final `git status --porcelain=v1` were empty; initial and final `git rev-parse HEAD` were `368822de297e71f24fd08569815cae0289e65b96`.

## Commands actually run

Every command below ran from `/tmp/cp1-critic.6ju4xU/checkout` unless another working directory is stated.

| Command | Exit | Observed output |
|---|---:|---|
| `git status --porcelain=v1 && git rev-parse HEAD && git branch --show-current && git status --short --branch` | 0 | Empty porcelain output; HEAD `368822de297e71f24fd08569815cae0289e65b96`; detached status `## HEAD (no branch)`. |
| `rg -n '^#{1,6} \|CP-1 \\(10 items\\)\|SMARD\|A65\|A75\|residual_load_proxy\|spectral\|partition\|reconciliation\|snapshot' capstone_V6_7.md docs/track-b/gauntlet-templates.md \| head -n 260` | 0 | Located template §2, plan §§3–5, §§9.3–9.6, and §12 CP-1; display was truncated after 175 matching lines. |
| `sed -n '68,125p' docs/track-b/gauntlet-templates.md; sed -n '69,166p' capstone_V6_7.md; sed -n '165,247p' capstone_V6_7.md; sed -n '332,393p' capstone_V6_7.md; sed -n '423,460p' capstone_V6_7.md` | 0 | Read the verdict contract and controlling plan extracts; display was truncated after 339 lines. |
| `rg -n '^### 4\\.1\|^### 4\\.2\|^### 5\\.1\|^### 5\\.2\|^### 5\\.3\|^## 5\\.' capstone_V6_7.md` | 0 | Located §4.1 at 117, §4.2 at 151, §5.1 at 165, and §5.2 at 201. |
| `sed -n '117,160p' capstone_V6_7.md; sed -n '165,235p' capstone_V6_7.md` | 0 | Read the complete feature/proxy, spectral, partition, and leakage contracts. |
| `git ls-tree -r --name-only HEAD -- data docs/data-leakage-audit.md src/delu_forecast tests .github/workflows sql scripts reports README.md pyproject.toml uv.lock; git log --oneline --decorate --all -- data/reconciliation_sample.json data/reconciliation.csv reports/ data/partitions.json` | 0 | All named artifacts are committed. Relevant order: `6a52118` data/features, `e18d1e1` spectral artifacts, `cd9b6dd` frozen sample, `368822d` reconciliation result. |
| `uv lock --check` | 0 | CPython 3.13.15; 34 packages resolved in 20 ms. |
| `uv sync --locked --dev` | 0 | Created `.venv`; installed 31 packages; `entsoe-py==0.8.0`, DuckDB, SciPy, Matplotlib, PyArrow, and pytest installed successfully. |
| `uv run pytest -q` (first invocation) | not surfaced | The execution wrapper yielded after 30.2 s with no output and without returning its session identifier through the wrapper. A later process check found no surviving pytest process; the command was rerun immediately to a reported terminal result below. |
| `uv run pytest -q` (reported rerun) | 0 | `.......... [100%]`; `10 passed in 3.48s`. |
| `uv run python scripts/audit_snapshot.py` | 0 | SHA `7dd2dc73407706ca6bd3c1ad51d201ac0de35eec5ca129ce320cca639f697f00`; 67,343 rows; delivery 2019-01-01–2026-09-06; 0 price nulls; local-day counts `{23: 8, 24: 2791, 25: 7}`; 25 base / 26 augmented columns; Fold 5 start 2026-01-08; reconciliation `pass`. |
| `uv run python scripts/run_sql_artifact.py` | 0 | `null_price_rows=0`, `duplicate_utc_rows=0`; all four bad-bin counts 0; lag and rolling views each 67,343 rows. |
| `uv run python scripts/generate_spectral_artifacts.py` | 0 | `Generated spectral artifacts from 63,695 EDA-eligible hourly rows through 2026-04-07`. |
| `(cd data && sha256sum -c snapshot.sha256)` | 0 | `snapshot.parquet: OK`. |
| `git status --porcelain=v1 && git rev-parse HEAD && pgrep -af 'pytest -q' || true` | 0 | Empty porcelain output; unchanged SHA; a transient PID was printed. |
| `ps -p 30872 -o pid=,ppid=,stat=,etime=,command= || true` | 0 | No process remained. |
| `jq . data/source_manifest.json; jq . data/partitions.json; jq . data/reconciliation_summary.json; jq . data/reconciliation_sample.json; sed -n '1,240p' data/README.md; sed -n '1,260p' README.md; sed -n '1,300p' docs/data-leakage-audit.md; sed -n '1,220p' reports/README.md` | 0 | Inspected source mapping/coverage/bin counts, exact partitions, reconciliation specification/results, attribution/cutoff documentation, the leakage table, and the four-sentence spectral interpretation. |
| `sed -n '1,320p' src/delu_forecast/ingest.py; sed -n '1,380p' src/delu_forecast/features.py; sed -n '1,280p' src/delu_forecast/schema.py; sed -n '1,300p' src/delu_forecast/partitions.py; sed -n '1,260p' src/delu_forecast/spectral.py; sed -n '1,220p' src/delu_forecast/postprocess.py` | 0 | Inspected implementation of stitching, aggregation/fail-closed behavior, UTC identity, the frozen catalogs, residual proxy, schema firewall, partitions, spectral labels, and isotonic-last. |
| `for f in tests/test_*.py; do echo "FILE $f"; sed -n '1,320p' "$f"; done; sed -n '1,220p' .github/workflows/tests.yml; sed -n '1,300p' sql/feature_queries.sql; sed -n '1,320p' scripts/audit_snapshot.py; sed -n '1,280p' scripts/run_sql_artifact.py; sed -n '1,340p' scripts/generate_spectral_artifacts.py` | 0 | Inspected all ten test functions across the eight invariant areas, push CI, SQL views/checks, and reproduction scripts. |
| `curl -fsSL https://www.smard.de/app/chart_configuration/market_data_configuration.json \| jq 'keys'` | 0 | Official mapping document keys: `main`, `meta_data`. |
| `curl -fsSL https://www.smard.de/app/chart_configuration/market_data_configuration.json \| jq '.main \| type, (if type=="array" then length else keys end)'` | 0 | `main` is an array of four sections. |
| `curl -fsSL https://www.smard.de/app/chart_configuration/market_data_configuration.json \| jq '.main[0]' \| sed -n '1,220p'` | 0 | Inspected official realized-generation hierarchy and filter objects. |
| `curl -fsSL https://www.smard.de/app/chart_configuration/market_data_configuration.json \| jq '[.. \| objects \| select(.data_id? \| IN(4169,411,123,125,3791,4067,4068,1225)) \| {data_id,name,source_resolution,unit,region}] \| unique_by(.data_id)'` | 0 | All eight IDs exist, include `DE-LU`, and report the expected MWh or EUR/MWh unit and quarter-hour source resolution. |
| `curl -fsSL https://www.smard.de/app/chart_configuration/market_data_configuration.json \| jq '[.main[] as $main \| $main.sub[] as $sub \| $sub.module[]?[]? \| select(.data_id? \| IN(4169,411,123,125,3791,4067,4068,1225)) \| {data_id, main:$main.name, sub:$sub.name, item:.name, source_resolution, unit}] \| sort_by(.data_id)'` | 0 | Official hierarchy confirms price 4169, forecast load 411, forecast VRE 123/125/3791, and realized VRE 4067/4068/1225. |
| `sed -n '1,380p' scripts/pull_smard_snapshot.py; sed -n '1,340p' scripts/reconcile_entsoe_smard.py; git show --stat --oneline 6a52118; git show --stat --oneline e18d1e1; git show --stat --oneline cd9b6dd; git show --stat --oneline 368822d` | 0 | Inspected pull and reconciliation implementations and verified artifact-by-artifact commit ordering. |
| Read-only `uv run python` audit heredoc over the committed Parquet, features, partitions, reconciliation CSV/JSON, and spectral-bin CSV | 0 | Independently obtained: 67,343 unique continuous UTC rows; 2019-01-01–2026-09-06; 23/25 DST rows; zero price nulls; zero bad source-bin/value combinations; A69/A75 composites exact; base 25 and augmented 26 with sole addition `residual_load_proxy`; manual lag/rolling differences 0; proxy manual mean differences 0 with spring/fall counts 41/43; tail lengths 90/1/60/1/90, union 242, overlap 0; final-calibration and holdout overlap with any fold/diagnostic 0; reconciliation 120/120, max difference 0; all three spectral labels within one FFT bin. |
| `sed -n '1,220p' pyproject.toml; rg -n 'name = "entsoe-py"\|version = "0\\.8\\.0"\|name = "duckdb"\|name = "pytest"' uv.lock; file reports/*.png data/snapshot.parquet; wc -c reports/*.png data/snapshot.parquet; sed -n '1,8p' reports/spectral_peak_bins.csv; sed -n '1,8p' data/reconciliation.csv; sed -n '115,125p' data/reconciliation.csv` | 0 | Confirmed the `entsoe-py==0.8.0` lock, valid 1600×880 PNGs, Parquet type/size, exact spectral bins, and representative pre-/post-transition reconciliation rows. |
| `git merge-base --is-ancestor cd9b6dd 368822de297e71f24fd08569815cae0289e65b96; git show cd9b6dd:data/reconciliation_sample.json \| sha256sum; git cat-file -e cd9b6dd:data/reconciliation.csv; rc=$?; echo "reconciliation.csv at sample-pin commit exit=$rc"; git diff --name-only cd9b6dd..368822de297e71f24fd08569815cae0289e65b96 -- data/reconciliation_sample.json data/reconciliation.csv data/reconciliation_summary.json data/source_manifest.json` | 0 overall | Ancestor check passed; pinned sample hash `3278dc6b61ac135f676a7065cf2006ee1e6a7bcb28fdced425069fbc1d0b1f0c`; expected `git cat-file` exit 128 proved the result CSV did not yet exist at the sample-pin commit; later diff added result CSV/summary/manifest only and did not change the sample. |
| `sed -n '1,220p' tests/conftest.py; sed -n '1,180p' Makefile; git status --porcelain=v1; git rev-parse HEAD` | 0 | Inspected DST-aware fixture and Make targets; checkout remained clean and at the candidate SHA. |
| Read-only `python3` exact-string check containing the supplied ten-item excerpt | 0 | `{'exact_excerpt_present': True, 'occurrences': 1}`. |
| `git status --porcelain=v1 && git rev-parse HEAD` (final pre-verdict check) | 0 | Empty porcelain output; `368822de297e71f24fd08569815cae0289e65b96`. |

The exact official SMARD mapping URL was also attempted through the browser fetch interface; that interface rejected the JSON URL as unsafe to open, so the same exact URL—and no other external endpoint—was verified read-only with the successful `curl` commands above. The three committed PNGs were opened at original resolution with the local image viewer; this was not a shell command.

## Evidence actually inspected

- Controlling text: `capstone_V6_7.md` §§3, 4.0–4.2, 5.1–5.2, 9.3–9.4, 9.6, and 12 M1/CP-1; `docs/track-b/gauntlet-templates.md` §2.
- Snapshot and source record: `data/snapshot.parquet`, `data/snapshot.sha256`, `data/source_manifest.json`, `data/README.md`; official `https://www.smard.de/app/chart_configuration/market_data_configuration.json` for the eight recorded IDs only.
- Partition and reconciliation evidence: `data/partitions.json`, `data/reconciliation_sample.json`, `data/reconciliation.csv`, `data/reconciliation_summary.json`, plus the relevant commit history and sample hash.
- Implementation: `src/delu_forecast/ingest.py`, `features.py`, `schema.py`, `partitions.py`, `spectral.py`, and `postprocess.py`.
- Tests/automation: `tests/conftest.py`, all eight `tests/test_*.py` files (ten test functions), `.github/workflows/tests.yml`, `Makefile`, `pyproject.toml`, and `uv.lock`.
- Reproduction/SQL: `scripts/audit_snapshot.py`, `scripts/run_sql_artifact.py`, `scripts/generate_spectral_artifacts.py`, `scripts/pull_smard_snapshot.py`, `scripts/reconcile_entsoe_smard.py`, and `sql/feature_queries.sql`.
- Documentation: `README.md`, `docs/data-leakage-audit.md`, and `reports/README.md`.
- Spectral artifacts, opened visually at 1600×880: `reports/fig_welch_periodogram.png`, `reports/fig_per_regime_periodogram.png`, `reports/fig_acf_24_168.png`; also `reports/spectral_peak_bins.csv`.

## Checklist verdict

| # | Checklist item | Verdict | Evidence |
|---:|---|---|---|
| 1 | Required A44, A65/A01, benchmark-only A69, and A75 snapshot; start/cutoff, head-gap handling, hash | PASS | Parquet has all mapped component and aggregate columns on 67,343 continuous hourly rows from delivery 2019-01-01 through the source-complete 2026-09-06 cutoff. SHA is `7dd2dc…f697f00` in both hash file and manifest. The SMARD fallback route has no A65 head gap; `data/README.md` explicitly distinguishes that from the ENTSO-E ~25-hour archive property and documents remaining missing bins. The docs describe the pull as snapshot/archive evidence and A65 pre-gate status only as an assumption. |
| 2 | UTC/DST identity, complete PT15M aggregation/stitching/fail-closed behavior, transition, no DA-price nulls | PASS | UTC index is unique, monotone, gap-free; Berlin identity is unique and yields 8×23-hour, 2791×24-hour, 7×25-hour days. Price has 0 nulls; pre-transition interval count is always 1 and post-transition count always 4. Every populated feature-feed hour has four source quarters; the two observed partial A75 component bins are null, not partial sums. Unit-aware aggregation, overlap-safe stitching, transition assembly, and missing-quarter rejection/fail-closed behavior were inspected and tested. SQL bad-bin counts are all 0. |
| 3 | Frozen base/augmented catalogs and champion schema firewall | PASS | `BASE_FEATURES` contains 25 calendar/regime, A65, lagged-price, and closed-left rolling features. `AUGMENTED_FEATURES` has 26 columns and adds only `residual_load_proxy`. Independent lag/rolling spot checks differ from manual values by 0. The proxy uses D-43 through D-2 and manual means match exactly. Champion schema rejects all named A69 derivatives and same-day actuals; the separate historical-input guard admits A75 only through D-2; benchmark schema adds only the six named A69 fields. |
| 4 | Five pinned tail partitions and exclusions | PASS | `data/partitions.json` has exact lengths 90/1/60/1/90, 242 distinct contiguous days, zero overlap, and Fold 5 starts 2026-01-08 (post-transition). Each fold has its own one-day calibration embargo and evaluation embargo. Independent set recomputation found zero final-calibration or holdout overlap with any development-fold component or diagnostic window. Holdout is absent from all fit/calibration/evaluation partitions. `test_08_partition_integrity.py` passes. |
| 5 | Fixed stratified ENTSO-E↔SMARD reconciliation | PASS | The sample was committed at `cd9b6dd` before result artifacts existed and is unchanged at the candidate. Its hash matches the summary. Five pre-pinned 24-hour days cover pre-crisis, crisis peak, post-crisis, and both sides of the MTU transition. Independent recomputation gives 120/120 within €0.01/MWh and max absolute difference €0.00. No whole-archive comparison was committed. |
| 6 | Targeted §9.4 repository tests | PASS | `uv run pytest -q` reports 10 passed. Inspected tests cover CQR→isotonic monotonicity, closed-left rolling behavior, spring/fall UTC-DST lags and identity, chunk stitching, missing-quarter rejection, series-specific PT15M aggregation and transition continuity, mapping IDs, champion/benchmark firewall, D-2/42-day residual window plus repeated/absent target hour and 43/41 window counts, FFT-bin labels, and partition integrity. Push CI runs the locked environment and full test set. |
| 7 | Complete leakage audit and honest assumptions/boundaries | PASS | `docs/data-leakage-audit.md` classifies every champion feature group as KFT or LAG with justification, calls A65 pre-gate availability an explicit unproven assumption including archive revision, states the A75-revision caveat separately, and confines delivery-day A69 to §7.2 while allowing its named scarcity derivative only as the §8.3 evaluation stratum. |
| 8 | CC BY 4.0 attribution and cutoff | PASS | Root README, `data/README.md`, and source manifest carry the exact attribution `Data: ENTSO-E Transparency Platform; Bundesnetzagentur \| SMARD.de — CC BY 4.0.` and identify the 2026-09-06 snapshot cutoff (pulled 2026-09-08). |
| 9 | Three spectral figures and 3–4 sentence interpretation | PASS | All three committed 1600×880 figures opened cleanly and show the required Welch peaks, regime overlay, and ACF references. `README.md` contains a four-sentence interpretation linking price periodicity to calendar structure and regime spectral change to stratified evaluation, and explicitly says the figures do not justify retaining the proxy. Reproduction uses 63,695 rows only through the 2026-04-07 EDA cutoff; all three labels are within one FFT bin. |
| 10 | Fresh Integration Critic PASS at final candidate | PASS | This fresh read-only review was performed from the clean detached checkout at exact SHA `368822de297e71f24fd08569815cae0289e65b96`; items 1–9 pass. The Lead can now create the required verdict-only evidence-tip commit. |
