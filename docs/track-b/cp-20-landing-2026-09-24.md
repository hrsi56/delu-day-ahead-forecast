# CP-20 landing and closure — 2026-09-24

**LAND complete; CP-20 administratively closed. Research result only; no model promotion.**

In chat on 2026-09-24 the Owner explicitly asked the assistant to perform the CP-20 LAND in
the prescribed order, to carry out the post-landing decisions directly instead of routing them
through the Orchestrator, and to commit and push the result. This task-specific instruction
authorized Git and publication operations normally reserved to the Owner; it does not amend the
standing governance rules.

## Receipt checks

The templates §4 receipt was run before disposition:

- The return status is PASS, and `docs/track-b/evidence/cp-20/integration.md` reads PASS at the
  final candidate `3e9ff8b500c2c655fea810ae11886503927f176c`.
- The candidate-to-evidence-tip delta is 57 paths, all under `docs/track-b/evidence/cp-20/`.
- The branch held 23 commits ahead of main. Relative to starting main it added 286 files and
  modified or deleted none; every path is inside the §15.7 write paths.
- `main` equalled `origin/main` at the start, and the Lead checkout was clean at the evidence tip.
- The 286 files and all 23 commits were scanned: no credential patterns and no references to
  retired governance tooling.
  **Correction, same day:** that pattern scan missed a DagsHub token in
  `integration-attempt-1/logs/critic-guards.log`. The token was found by a value-based scan and
  rotated; see the [credential exposure record](credential-exposure-2026-09-24.md).
- The headline figures in `reports/weather-ablation/report.md` match the return.

Record-level scientific claims are taken as reported by the return and the independent verdict.
No experiment, fit or scientific verification was rerun for this landing.

## Landing and preservation

| Identity | Value |
|---|---|
| Starting main | `88c68cf6d8a520374d4b7ec60753a38a7b576544` |
| Failed candidate (Integration attempt 1) | `a7943fb6262c1a50b729bd92fe701c8be9428038` |
| Final candidate | `3e9ff8b500c2c655fea810ae11886503927f176c` |
| Evidence tip / `evidence/cp-20` | `a7a9b2e3a4d0147a0c82835a853277e9c81c7945` |
| Squash landing / `land/cp-20` | `f450bc1a98c70544d8b1fb84c3496986551abb7f` |

- The staged squash tree and the landing tree both equal the evidence-tip tree
  `0b46b5d413765ac2e40cbb89e60e13e5ba25d9b6`.
- In the landed checkout, `reports/weather-ablation/prerun-attempts.csv` hashes to the value the
  frozen protocol records (`859ba632…`). That byte mismatch was the defect behind Integration
  attempt 1.
- Both lightweight tags were created. Before any deletion, both candidates and every branch commit
  were verified reachable from `evidence/cp-20`.
- A pre-operation Git bundle, a ref snapshot and a copy of the Lead checkout's
  `settings.local.json` are in `.local/artifacts/cp20-landing-2026-09-24/`.

## Owner decisions, 2026-09-24

1. **Disposition:** LAND.
2. **Pasted-block decisions:** the records in `reports/weather-ablation/extraction-repairs.json`
   are confirmed as the Owner's own decisions. They are O1, O2, O2-A1, S1, O3, O4 and the
   2026-09-24 run plan, all of which reached the Lead as pasted text. Under O2, target messages
   stay within the counted per-message allowance.
3. **Weather attribution:** added to `DATA-LICENSE.md`, pointing to
   `reports/weather-ablation/rights-notice.md`.
4. **Scoring:** no further CP-20 scoring. Both analysis and reference passes are exhausted, and no
   cap change is made.
5. **Unused branch:** `claude/cp-20-weather-augmentation-7c081b` was deleted. The desktop app
   created it at `88c68cf`, which main contains, and it was never used.
6. **Local weather material:** retained, as listed below.

## Reclamation and citation map

After the tag checks, the clean Lead checkout `.claude/worktrees/cp-20-weather-augmentation-7c081b`,
the branch `gauntlet/cp-20` and the empty `.local/worktrees/cp-20/` were removed. The Lead had
already removed its Critic and pre-check worktrees. The Lead checkout's only ignored files were:

- Python bytecode;
- a `.DS_Store`;
- a `settings.local.json` identical to the primary checkout's.

| Historical reference | Current retrieval |
|---|---|
| `gauntlet/cp-20` reviewed chain | `evidence/cp-20` |
| `.claude/worktrees/cp-20-weather-augmentation-7c081b/<path>` | The same relative path in the primary checkout; exact historical bytes via `git show evidence/cp-20:<path>` |
| `.local/worktrees/cp-20/critic`, `critic-2` | Removed during CP-20; committed outputs are under `docs/track-b/evidence/cp-20/` |
| Final or failed candidate SHA | Unchanged SHA, reachable through `evidence/cp-20` |
| Squashed experiment | `land/cp-20` |

The following documents name `gauntlet/cp-20` as the authorized execution branch:

- the ratified anchor `capstone_v21.md` (§15.7);
- the v21-r3 → v21-r4 amendment;
- the issued CP-20 brief.

They are hash-bound, locked records of the completed run and keep their original bytes. So do
the reviewed evidence and the checkpoint return. Use the map above to retrieve what they cite;
current status is in `progress.md` and this document.

The other session's `claude/word-rtl-issues-3286df` branch and its detached worktree are
untouched, and so is the pre-existing GitHub Desktop stash. They are declared here, not reclaimed.

## Retained local material

`.local/` is ignored by Git and is not an off-device backup. Nothing under it was deleted by
this landing.

| Path | Content | Size |
|---|---|---|
| `.local/artifacts/cp-20/weather/runs/` | Decoded box grids for all 2,476 runs: five fields × ten leads × 34 × 41 points (`.npz`), with per-run metadata | 684 MB |
| `.local/artifacts/cp-20/weather/raw/` | Retained original GRIB sample messages for 23 dates | 980 MB |
| `.local/artifacts/cp-20/weather/*.jsonl` | Request, attempt and failure logs | ~150 MB |
| `.local/artifacts/cp-20/wx-venv/` | Extraction environment (ecCodes) | 145 MB |
| `.local/artifacts/cp-20/hg-components/` | Cached HG component fits | 60 MB |
| `.local/artifacts/cp-20/{ledger,logs,markers,critic,critic-2}/` | Cumulative ledger, job logs and markers, Critic outputs | ~28 MB |
| `.local/weather-admission/` | Admission samples and recovery files | 1.7 GB |
| `.local/artifacts/cp20-landing-2026-09-24/` | Landing recovery bundle and snapshots | 19 MB |

- **Global fields were never stored.** Each GFS message was verified, decoded and cropped to the
  47–55.25°N, 5.5–15.5°E box in memory. The 136 GiB transfer therefore left at most 2.4 GiB of
  added disk.
- **Reuse.** New features from the same five fields, leads and box can be computed from `runs/`
  without downloading.
- **What needs a fresh retrieval:** other variables, heights or regions; the 177 delivery days
  from 2022-09-29 to 2023-03-23, which no fold used; and any date after 2026-04-07. The final
  chain run retrieved about 2,100 runs in 5.7 hours.
- **Rights.** Per the rights notice, decoded grids and raw samples are not redistributed.

## Scientific status and next routing

Engineering PASS is accepted. HG−H0 meets the observed-joint-improvement rule:

| Measure | Difference | 95% interval |
|---|---|---|
| ΔS_WIS | −0.0838 | [−0.1044, −0.0655] |
| ΔS_MAE | −0.0783 | [−0.1006, −0.0570] |

- **Per fold:** all five folds favour HG. The fold-3 MAE interval crosses zero.
- **H0:** reproduces CP-16 V2-H bitwise on 10,747 keys.
- **§8 diagnostics:** HG meets all six original criteria; H0 misses criteria 1 and 2.
- **Labels:** every result is `development_post_selection`, and economics stay descriptive.

Landing implies no product qualification, promotion, CP-17 freeze or prospective clock. Two
post-freeze repairs stay disclosed in `reports/weather-ablation/post-freeze-repairs.json`:

- r13: supplementary positive controls, because the frozen ×3 control is scale-invariant;
- r14: the byte-exact CSV.

**Inherited test debt:** the CP-20 return reports 3 failures and 3 errors in the full
`tests/cp16` directory on main, with and without CP-20. The listed inherited guards pass.
**Resolved the same day** by the harness-only CI repair recorded in `progress.md`.

**Next routing:**

- Record CP-20's measured-quality finding in the 4.7 quality/delivery disposition.
- Decide whether the 4.3C/4.10R research content should cover it.
- Optional 4.4V, 4.5, 4.6 or 4.8 work and any live route each need their own authorization.
- The return names no interview trigger, so no Q&A entry is filed.

## Publication

The Owner authorized pushing `main` with `land/cp-20` and `evidence/cp-20` to `origin`; the push
follows the commit that adds this record. The other session's branch and worktree and the
GitHub Desktop stash are not pushed.
