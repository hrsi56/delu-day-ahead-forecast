# PRES-1, presentation release 1: Engineering Lead brief

**Engineering Lead · PRES-1 · issued 2026-09-24 at the Owner's instruction.** The Owner carries this
brief to a new Engineering Lead session and authorizes bounded execution under it. Repository:
`/Users/djourno/Downloads/PJM`.

## 1. Target

Execute the approved [presentation and tracking plan](presentation-and-tracking-plan-2026-09-24.md),
revision 3 ("the plan"), in full. That means every phase: 0, A, B, C, D1, D2, E and F.

- **The plan is the controlling specification.** This brief adds authority, stops, ceilings and
  the return.
- **If the plan and this brief disagree, stop and ask.** Do not reinterpret either document.

**Before any other work, verify:**

- **The plan's SHA256:**
  `281193740a256a99a1676d53ef61c9babb5f744a7d0fb038927e305d1149812c`
- **This brief's SHA256:** given in the Owner's prompt.
- **The starting `main`:**
  - it is the Owner's commit that adds this brief; record its SHA;
  - `main` must be clean;
  - `main` should equal `origin/main`; if it does not, report the difference.

## 2. Read first

1. **The governance documents:**
   - `AGENTS.md` in full, including § Credentials and § Branch and ref lifecycle;
   - `engineering-role.md` in full;
   - `docs/track-b/gauntlet-templates.md` §2 (assignment and verdict) and §3 (return).
2. **The plan, in full.** Its §16 records the Owner's decisions, and they bind.
3. **For context only,** the two reviews the plan absorbed:
   [presentation review](presentation-review-and-corrections-2026-09-24.md) and
   [design review](presentation-design-review-v2-2026-09-24.md). The plan governs.
4. **`capstone_v21.md` (v21-r4), read-only:** §§6–8 for score definitions and criteria, and
   §§14–15 for the CP-16 and CP-20 claims.
5. **Evidence, read-only:**
   - `reports/{cp2,cp10,cp15,v2-causal,weather-ablation,weather-admission}/`;
   - `docs/track-b/research-content/`;
   - the landing records of CP-15, CP-16 and CP-20;
   - `docs/track-b/evidence/`;
   - `docs/deploy.md`.

**Engineering isolation:** do not read `progress.md`, `orchestrator-role.md` or the syllabus
(`AGENTS.md`, role router).

## 3. Owner decisions that bind this task

These are recorded in the plan's §16, dated 2026-09-24.

1. **Naming.**
   - Adopted generations are `vN · <adopted change>`.
   - A number is given only after adoption; a rejected experiment is a branch with a descriptive
     name.
   - No number is reserved in advance.
   - "Final candidate" and "Live" are statuses, not names.
2. **Independent check.**
   - It is mandatory, and it is done by a checker that did not write the changes.
   - It covers the data, the claims, the charts, the links and the reading experience (§9).
   - The Owner's visual approvals at D1 and at the end come in addition to it.
3. **The MLflow step** is carried in this brief as an explicit section (§6). Do not edit the
   templates. Propose the exact template text in the return.
4. **The capability probe** is a local rehearsal plus read-only checks against DagsHub.
   - There is no public write probe.
   - Capabilities are verified against DagsHub after the authorized upload.
5. **The experiment is `delu-generations`.**
   - Update every surface.
   - `delu-cp2` and its history stay untouched.
6. **The contribution statement is required.**
   - Display the Owner's wording from plan §8.9 exactly as written.
   - The Owner confirms the English at D1.
7. **The Owner completed the demo device test on 2026-09-24.** Record it in Phase 0. If its device,
   browser and outcome are not yet recorded, ask for them at Stop 1.
8. **The visual tokens in plan §7.3** are the D1 starting point.

## 4. Git, worktrees and paths

**Lifecycle.** PRES-1 is treated as a checkpoint for lifecycle purposes (`AGENTS.md` § Branch and ref
lifecycle).

- Create the local branch `gauntlet/pres-1` from the starting `main`, in the worktree
  `.local/worktrees/pres-1/lead`.
- Commit candidate work there, one commit after another.
- Never commit to `main`, merge, push or create tags.

**If this session opened inside an app-managed worktree under `.claude/worktrees/`,** do not work
there. Create the worktree above, and report where the session was opened.

**Other locations:**

- **Independent checks:** clean, detached worktrees at `.local/worktrees/pres-1/check-<n>`, each at
  the exact candidate SHA. Remove each one when done.
- **Evidence:** `docs/track-b/evidence/pres-1/`, holding the assignments, verdicts, reader-task
  records, release-check records and the return.
- **Tools** live in separate environments under `.local/tools/`: the MLflow 3.5.1 rehearsal server,
  and Playwright if you use it.
- **Screenshots** go in `.local/artifacts/presentation/`, and **scratch work** in
  `.local/tmp/pres-1/`.

**Write allowlist, on the branch:**

- **Source modules:**
  - `src/delu_forecast/research.py` and `research_claims.py` (new);
  - `claims.py`, but only for the reconciliation in plan §8.7, the `licensing` claim's GFS line
    and the `delu-generations` text;
  - `surfaces.py` and `tracking.py`, only where the plan requires it.
- **Scripts:**
  - `scripts/build_pages.py`, `scripts/check_links.py` and `scripts/build_wasm_space.py`;
  - `scripts/cp3_readme.py`, or a new README research generator;
  - the new scripts the plan names: `mlflow_export.py`, `mlflow_publish.py`,
    `verify_mlflow_mirror.py`, the rebuild command, and optionally `check_reader_paths.py`;
  - `Makefile` targets for these.
- **Generated surfaces and build records:**
  - `docs/index.html`;
  - `README.md`, generator-owned blocks only;
  - `space-wasm/README.md` and `space/README.md`;
  - `reports/cp3/pages_build.json`, `reports/cp3/link_check.json`,
    `reports/cp3b/space_wasm_bundle.json`, and any other record that these generators rewrite.

  `app/public/` and `dist/` are ignored build outputs.
- **New documents and records:**
  - `reports/presentation/**`;
  - `docs/track-b/mlflow-tracking-spec.md`;
  - `docs/track-b/research-content/cp20-update.md` and `cp20-claims.md`;
  - the publication-pass edits to `cp15-cp16-update.md` and `cp15-cp16-claims.md`.
- **Tests:**
  - the new `tests/test_29_*` to `tests/test_34_*`;
  - the extension of `tests/test_23_static_space.py`;
  - minimal updates to existing surface tests, only where the plan changes the text they assert.
    Report each such update.
- **Evidence:** `docs/track-b/evidence/pres-1/**`.

**Never touch:**

- the locked set in `AGENTS.md`, including the templates and every capstone anchor and amendment;
- `progress.md`, `pyproject.toml` and `uv.lock`;
- the plan and this brief;
- `DATA-LICENSE.md`, `models/` and `data/`;
- any committed report or evidence file from CP-2, CP-3, CP-3B, CP-10, CP-15, CP-16 or CP-20,
  except the build records listed above.

**The v1 registry's tags.** Do not change any claim that is recorded in the v1 registry's version
tags (`reports/cp3/mlflow_registration.json`, `tags_applied`). Re-tagging the registry is a public
write that this brief does not cover.

## 5. Stops and authority

| Point | The Lead delivers | The Owner then |
|---|---|---|
| **Stop 1, after D1** | Phases 0, A and B complete, with C complete or its status reported. The D1 specimen: the local page, the token sheet, screenshots at the widths in plan §11.3 and the reader-task record. Active hours used so far. | Approves or amends the desktop and phone compositions. Confirms the English contribution statement. Supplies the demo test's outcome if it is missing. |
| **Stop 2, after E** | A candidate SHA with a PASS from the independent check. The CI-equivalent run, the release checks and the MLflow export packet. | Approves the content. Gives, or withholds, the instruction for the public MLflow upload (F1). |
| **Stop 3, after F1–F4** | The final candidate with real links, rechecked independently. The public mirror verification and route checks, passed. | Gives the final visual approval. Decides the disposition of `gauntlet/pres-1` and pushes. Instructs the Space redeploy (F7). |
| **Terminal** | The F8 post-deploy checks, and the return | Records the receipt |

**Public actions** (the F1 upload, the F7 Space redeploy, any push) happen only when the Owner
explicitly names that action in this session. Approving the plan or a phase is never such an
instruction.

**Landing on `main` and pushing belong to the Owner** (`AGENTS.md` § Git and publication
authority). The Lead performs them only if the Owner explicitly delegates that specific action in
this session.

## 6. The MLflow step: deliverables and acceptance

| Deliverable | Acceptance |
|---|---|
| `docs/track-b/mlflow-tracking-spec.md`, implementing plan §10 | The names, units, tags, provenance, comparability IDs and the 23-run manifest match the plan. |
| A read-only DagsHub probe, recorded in `reports/presentation/mlflow-capabilities.json` | `/version`, the fields returned and the anonymous routes are recorded, each with its date. |
| A local rehearsal: an MLflow 3.5.1 server under `.local/tools/`, with the project's 3.16.0 client | It covers nested runs, metric timestamps and histories, notes, search by tag, and `log_input` (or its recorded fallback). An interrupted upload resumes without duplicates. |
| A committed export: `reports/presentation/mlflow-export/{cp10,cp15,cp16,cp20}.json` and `manifest.json` | Deterministic. Exactly 23 runs: 4 parents and 19 children. Metric names carry their units. Provenance tags are kept separate. It passes the pre-commit secret guard. |
| `scripts/mlflow_publish.py` | It uploads only committed export files and is idempotent by `delu.run_key`, so it resumes cleanly. It sets `delu.package_complete` only after verification. It scans every outbound field and artifact byte against the local credential values, without printing them. |
| `scripts/verify_mlflow_mirror.py` | It checks: each run once, the parents, parameters and tags, the full metric histories (key, step, timestamp and value) and the artifact hashes. In the rehearsal it catches a deleted history point and an altered digest. |
| After F1, on instruction: `reports/presentation/mlflow_index.json` | The public mirror verification passes. Every advertised route passes both its REST check and its browser check. Features that DagsHub does not support are recorded and not advertised. |
| A template proposal | The exact text for the MLflow landing step in `docs/track-b/gauntlet-templates.md`, included in the return. The template file itself is not edited. |

## 7. Ceilings

- **Effort:** a target of 40 active hours and a hard stop at 56. Time spent waiting for the Owner
  does not count. Report the hours at each stop.
- **Cost:** $0. No paid service and no new project dependency. `pyproject.toml` and `uv.lock` stay
  byte-identical.
- **Research budget:** none. No fits, no scoring passes, no data retrieval; saved outputs only.
- **Network:** read-only HTTP for checks. Public writes happen only at F1 and F7, and only on
  instruction.
- **Disk:** at most 5 GiB added under `.local/`.
- **Credentials:** follow `AGENTS.md` § Credentials.
  - Never display a value.
  - Never bypass the secret guard; `core.hooksPath` must stay `.githooks`.
  - **Before F1:** confirm inside the process that `MLFLOW_TRACKING_USERNAME` and
    `MLFLOW_TRACKING_PASSWORD` are set. Report only "set" or "unset".
  - **The Space redeploy** follows `docs/deploy.md`: use `HfApi.upload_folder`, because
    `hf upload` returns 402. `HF_TOKEN` is used only as a stored variable.

## 8. Acceptance

A PASS for PRES-1 requires all of the following:

- each phase's acceptance in plan §12;
- the invariants in plan §6;
- the plan's §9.5 tests and the existing suite, green both in a clean Python 3.12 CI-equivalent
  run and in the local Python 3.13 environment;
- the plan's §11 gates: the release checks, the browser and accessibility checklist, and the
  reader tasks;
- a fresh independent check that passes and binds the final candidate SHA;
- the Owner's visual approvals, at D1 and at the end.

If any item cannot be met within this brief, return INCOMPLETE or BLOCKED and name the exact
blocker.

## 9. Independent check

**Who.** A fresh agent that did not write the changes. Spawn it as `engineering-role.md` describes,
with the assignment form in templates §2, in a clean detached worktree at the exact candidate
SHA.

**What its assignment covers:**

- **The data:** every rendered research number is re-derived from its source record.
- **The claims:**
  - the claim maps;
  - the withheld phrases;
  - the labels;
  - the exact H−P endpoint;
  - v1's label.
- **The charts:**
  - units, direction and intervals;
  - scales;
  - mobile variants;
  - the meaning survives without colour or hover.
- **The links:** internal anchors, the repaired link checker and the MLflow routes.
- **The reading experience:** the six reader tasks in plan §11.4.

**The verdict** goes in `docs/track-b/evidence/pres-1/independent-check.md`, as an evidence-only
commit.

**Rechecking after F2–F4:**

- If those steps change more than the MLflow index and the link targets, rerun the full check on
  the final candidate.
- Otherwise, a focused recheck of those changes binds the final SHA.

## 10. Return

Use the checkpoint-return form in templates §3, at `docs/track-b/evidence/pres-1/return.md`. It
includes:

- **Status and identity:**
  - PASS, INCOMPLETE or BLOCKED;
  - the starting `main`, the final candidate SHA and the evidence-tip SHA.
- **Execution:**
  - each phase, with its acceptance evidence;
  - the independent check's verdict;
  - the Owner's approvals, with when each was given.
- **Resources:** active hours, disk used, and every network write performed, each with the
  instruction that authorized it.
- **Repository:**
  - the files changed, each with one line of reason;
  - an accounting of branches, worktrees and tags: declare everything you opened.
- **Landing report:**
  - the proposed disposition;
  - the evidence tip;
  - the live documents that cite `gauntlet/pres-1`;
  - a proposed commit message.
- **Follow-up:**
  - the proposed template text for the MLflow step (§6);
  - open issues, including anything the plan could not deliver as written;
  - interview-capture triggers, one line each (`AGENTS.md` § Interview-answer capture). Name
    them only; file nothing yourself.
  - post-return reads.
