# Engineering Plan — German DE-LU Day-Ahead Price Forecasting Tool, v6.6 — Lean DS Capstone with Cloud-Backed Showcase

## 0. Ratified decisions (five; final)

Decisions ratified by the project owner. Items 2–5 were adjudicated in the 2026-06-11 dataset-conversion research (see `de-lu-conversion-memo`); item 1 carries over from v5.5. **Item 3's single reopen condition was adjudicated closed at the 2026-06-12 Month-0 spike** (`docs/spike-feed-status.md`). They are final and binding on M1.

1. **Out-of-sample length vs. compute — RATIFIED (carried from v5.5): per-fold recalibration + ~450 test-days, mixed across regimes** (Section 5.1). Narrowing the training window to extend the test span is rejected — it would cost the pre-/crisis/post-crisis regime span the methodology depends on. The ~450-day, per-fold, mixed-regime design is the deliberate $0-compute balance, re-pinned to DE-LU regime dates.
2. **Market and data source — RATIFIED 2026-06-11: German DE-LU bidding zone via ENTSO-E Transparency Platform (primary) + SMARD (secondary/cross-check).** PJM is structurally geo-blocked from Israel (edge-WAF) and its Data Miner 2 terms prohibit snapshot redistribution; ENTSO-E and SMARD data are CC BY 4.0 — the committed-snapshot reproducibility story strengthens. ENTSO-E EIC for DE-LU: `10Y1001A1001A82H`.
3. **Gas marker — OMITTED; reopen condition adjudicated NOT MET (2026-06-12), omission FINAL.** No free, daily, legally-redistributable TTF/THE series exists (Yahoo `TTF=F` ToS prohibits redistribution; Ember's underlying gas series is Montel-licensed), and the Month-0 spike confirmed SMARD's `chart_data` API exposes gas-fired **generation volume** (filter 4071) only — no THE price series. The merit-order price signal is carried, **if the M2/CP-2 ablation retains it**, by **a gate-feasible residual-load proxy** (Section 4.1; **v6.3:** the A69-based residual-load construction is post-gate and confined to the §7.2 post-gate benchmark). If no proxy clears, the champion ships on strict-gate calendar / load / lag / regime features and this signal is left unrepresented — an accepted, benchmark-quantified outcome. Residual load is the literature-standard German EPF driver. *Narrow residual:* an optional 10-minute browser check of SMARD's JS-rendered gas page remains available before M1; it re-opens this decision only if it reveals a CC BY, API-accessible **daily THE price** series — otherwise the boundary stands.
4. **Hourly target preserved across the 15-minute MTU transition.** SDAC moved to 15-minute products on trading day 2025-09-30 (delivery 2025-10-01); the DE-LU day-ahead price is 24 hourly values before and 96 quarter-hourly values after. The target stays **hourly end-to-end**: post-transition quarter-hours are averaged to hourly (SDAC's no-arbitrage constraint defines the hourly equivalent as exactly this average). A quarter-hourly target is named future work (Section 13). **Spike-verified:** the boundary averaging path validated on samples to 1e-9 (Section 4.0).
5. **External weather dropped entirely.** The gate-available ENTSO-E load forecast embeds weather information; the residual-load/scarcity candidates use the single pinned, gate-feasible VRE expectation in Section 4.1: a seasonal-diurnal climatology fit per fold on historical A75 actual VRE from the **proper-training partition only**, never on delivery-day A69 and never on calibration/eval rows. No actual value is a runtime model input. **If either proxy arm is retained at M2/CP-2**, the shared VRE-expectation coefficients enter the champion; if neither arm is retained, that preprocessing layer does not. No DWD dependency, no station→zone crosswalk, and no external-weather archive risk.

---

## 1. Purpose and Scope

This is a portfolio-grade probabilistic forecasting project built for an industry Data Scientist hiring audience. The tool produces, on demand, an hourly forecast of the **German–Luxembourg (DE-LU) bidding-zone day-ahead electricity price** for the **next delivery day (24 hourly values, D+1)**, with calibrated 50% / 80% / 95% prediction intervals.

The target is the next delivery day specifically because that is what the day-ahead market clears: the EPEX SPOT day-ahead auction (within the EU Single Day-Ahead Coupling, cleared by EUPHEMIA) closes its order book at **12:00 CET on D-1, with preliminary results ~12:45 CET and final results ~12:57 CET**. At that gate the prices for all hours of D+1 are forecast, and every lagged-price feature is legitimately known. A multi-day-ahead target would force the model to consume values that do not yet exist at forecast time — look-ahead leakage in the one project whose centerpiece is a leakage audit. The 24h horizon keeps the feature catalog clean and the methodology airtight. (See Section 5.2.)

*Why the German market, for an Israeli interviewer:* the domain is a methodology vehicle, not a domain commitment — a real, noisy, public time-series with a textbook regime shift (the 2021–2023 European energy crisis), the world's most-studied negative-price phenomenon, and a published benchmark literature (EPEX-DE is one of the five canonical Lago et al. 2021 open-access markets), chosen so the work is verifiable rather than a toy. The methodology transfers to any consequential time-series. The full interview answer is prepared in interview prep, not here.

The constraints, named up front:

- **Interactive marimo showcase, statically deployed.** A reactive marimo notebook (server mode, not WASM) with live UI sliders **drawn from the strict-gate champion's actually-retained features** (**v6.3:** the quantile-level selector is fixed; the remaining controls are set at CP-4 from the retained champion catalog, not pre-ratified — §9.2), containerized via a multi-stage Dockerfile and deployed to Hugging Face Spaces free tier. Data is a fixed weekly snapshot manually refreshed during the active application phase, not live API pulls during user sessions. **(v6.2)** The recruiter-facing **primary URL** is a statically exported HTML report of the full reading order on **GitHub Pages** — no container, no sleep state; the interactive Space is the deep-dive linked from it with an explicit wake label (Section 9.2).
- **Cloud-backed experiment tracking via DagsHub-hosted MLflow.** Public read-only MLflow UI linked from CV and LinkedIn. No DVC. Data reproducibility achieved via a single committed Parquet snapshot (CC BY 4.0 with attribution) plus pinned dependencies plus tagged commit.
- **Single model.** A monolithic LightGBM quantile ensemble (nine native quantile heads). No model menagerie, no neural challenger, no blending, no shadow logging.
- **CQR-calibrated quantile intervals.** Conformalized Quantile Regression (Romano et al. 2019) on the four symmetric quantile pairs, with isotonic monotonicity enforced last. Per-interval finite-sample marginal coverage **attaches to the post-CQR/pre-isotonic output; the final post-isotonic intervals are verified empirically** (**v6.3** three-stage coverage — §6.2, §8.4). No sequential conformal apparatus (EnbPI / SPCI) and no Giacomini-White gating.
- **Forecast + diagnostic report, not a trading product.** No LONG/SHORT/FLAT layer, no battery-arbitrage optimization, no bid-reference logic, no utility/risk-aversion parameter, no auto-generated natural-language commentary.

The audience is a Data Science hiring manager. The artifact must demonstrate: domain understanding, defensible feature engineering, correct time-series validation, sensible model choice, explainability, cloud-backed experiment tracking via a publicly inspectable MLflow UI, honest uncertainty communication, and a polished interactive interface deployed to a recognizable ML hosting target. Total compute footprint: trains end-to-end in minutes on an Apple Silicon M3, no GPU required. Total hosting cost: $0 (Hugging Face Spaces free tier + DagsHub free tier).

**v5.5 → v6.0 delta (market conversion).** PJM Western Hub → German DE-LU, triggered by PJM's structural geo-block from Israel and adjudicated in the 2026-06-11 conversion research memo. Rewritten: §0 (re-ratified, five decisions), §2 (market context: 2021–23 crisis + negative-price era replace Elliott + data-center growth), §3 (ENTSO-E + SMARD replace Data Miner 2 + EIA + NOAA), §4 (feature catalog: residual load replaces temperature/gas; Dunkelflaute flag replaces cold-weather alert; 15-min MTU handling joins DST handling), §5.1 (fold scheme re-pinned to DE-LU regimes), §11 (risk register: forecast-vintage trap, platform migration, 15-min transition replace the PJM risks), §12 (CP-1 re-pinned; LMP-decomposition reconciliation → ENTSO-E↔SMARD cross-source reconciliation). **Survives verbatim:** §6 (LightGBM 9-head quantile ensemble + CQR + isotonic-last, incl. the negative-Q subtlety and the pinned calibration set), §7's DM machinery and the pre-committed CP-2 secondary gate, §8 (SHAP + permutation + regime-stratified + reliability), §9 (DagsHub MLflow, marimo server mode, HF Spaces, multi-stage Docker, precomputed slider lookup with per-cell OOD flag, invariant-test pattern, thin CI), §10's reading order and stranger-test gate, the budget/hardware constraints, and all §13 boundaries not touched by the market swap (two new boundaries added: no fuel-price layer, no quarter-hourly target). The redistribution risk is **resolved**, not mitigated: both sources are CC BY 4.0.

**v6.0 → v6.1 delta (Month-0 spike adjudication, 2026-06-12).** Surgical release folding the de-risking spike's verified findings into the spec; no scope or methodology change. (1) §0 item 3's gas reopen condition **adjudicated NOT MET** — SMARD's API exposes gas-fired generation volume only, no THE price series; gas omission is final, with a narrow optional pre-M1 browser re-check. (2) Feature feeds (A65/A01, A65/A16, A69) confirmed **natively PT15M across the entire 2019→present archive** — uniform quarter-hour→hour aggregation at ingest from day one; only A44 is date-conditional at 2025-10-01 (§3, §4.0). (3) **Chunk-boundary bin integrity** named as a mandatory ingest property after the spike surfaced the partial-final-bin pull artifact — invariant test 4 extended, CP-1 item 2 amended (§4.0, §9.4). (4) **R-2 sharpened by direct evidence:** at 15:35 CEST D-1 the load forecast for D was published (post its 10:00 deadline — KFT empirically confirmed) while A69/A01 was not — the archived VRE vintage **post-dates the 12:00 gate by initial publication**; the pre-authorized as-archived treatment is now the expected operative path, the snapshot/compare harness (committed; first snapshot taken) closes the remaining overwrite question before M1, and revision metadata is confirmed a dead end (§3, §5.2, §11). (5) `entsoe-py` **pinned at 0.8.0**; ENTSO-E↔SMARD sample reconciliation 96/96 hours within €0.01; 2019 archive depth verified with a **~25h A65/A01 head gap** to handle at M1 (§3, §9.3, CP-1). Spike record: `docs/spike-feed-status.md`, branch `claude/modest-bardeen-96zee0` @ `20fc1ff`.

**v6.1 → v6.2 (2026-07-12):** additive only. Spectral-EDA extension merged per `spectral-extension-spec.md` v1.0 — new §4.2 (Welch periodogram / per-regime spectra / ACF), CP-1 checklist expanded, R-5 added, one showcase section added to the §10 reading order and the M4 scope, three interview Q&As added to §13. One stage-gated pointer to the companion Binary Classification Mini-Capstone added at the end of §12. No v6.1 text removed or reworded; all §0 ratified decisions, boundaries, and rejected-item records carry forward unchanged.

**v6.2 + the D7 static first-touch rider (2026-07-20; absorbed into this edition, no version bump).** Ratified as a standalone M5 rider, extracted from decision **D7** of the AWS extension proposal (now `aws-extension-spec_v1_1.md`); the remainder of that proposal — the AWS production backbone, decisions D1–D6 + D8 — is **not ratified**: it is parked as the pre-committed decision point **DEC-AWS at the Month-5 gate** and, if ever ratified, amends this edition v6.2 → v6.3. Substance: the recruiter-facing **primary portfolio URL becomes a statically exported HTML report of the full §10 reading order, hosted on GitHub Pages** ($0, CDN-served, no container, no sleep state); the interactive marimo Space becomes the deep-dive linked from it with an explicit wake label; **no keep-alive of any kind, on any platform** (the GH-Actions cron option, already distrusted at v6.2, is deleted). Touched: §1 (one clause), §9.2 (title, one new block, the sleep paragraph rewritten), §10 (intro + item 12), §10.1, §12 (M5 scope + one amended and three new CP-5 items), §13 (one Q&A added). Effort ~3–5 h inside M5's window; budget unchanged at $0. Everything else — all §0 ratified decisions, the M0–M4 arc, the model and methodology core, the three-slider set — carries forward verbatim, and the §13 boundary **"No enterprise production pipeline or live trading layer" stands unchanged.**

**v6.2 → v6.3 (2026-07-22; strict-gate methodology amendment + audit-driven corrections).** Absorbs the external Codex audit under owner ratification. **Core change: A69 (the DA wind/solar forecast) is not known at the 12:00 gate** — the Month-0 spike proved its initial publication is post-gate (§3, §5.2) — so v6.3 **forbids delivery-day A69 and every feature computed from that post-gate vintage from the shipped champion's runtime schema.** The sole historical-VRE use permitted in champion preprocessing is the pinned proper-training-only A75 climatology in §4.1; A75 is a fit target, never a runtime feature. (1) **Strict-gate champion:** the deployed model uses only genuinely pre-gate runtime inputs; `gate_feasible_residual_load_proxy` and `expected_scarcity_proxy` survive only as **gate-feasible candidates**, each requiring **point-in-time provenance** (§4.1, §5.2). (2) **No pre-ratified headline:** proxy value is decided at **M2/CP-2 by exactly four pre-specified matched catalogs** (strict base; +residual arm; +scarcity proxy; +both) against a **retention rule fixed before results**; the winning strict-gate catalog is **frozen at CP-2**; SHAP at M3 *explains* retained features and is **not** the incremental-value test; **"no proxy survives" is an acceptable outcome** and ships a champion with no forced headline (§4.1, §12). (3) **Post-gate information benchmark** (§7.2): from the frozen CP-2 catalog, an otherwise-identical delivery-day-A69-augmented model, calibrated separately, reports **MAE / mean pinball loss / empirical coverage / interval width / calibration error** at **M3/CP-3** — a benchmark that quantifies the value of post-gate information, **never** the champion; `dunkelflaute_flag` is benchmark-only or a post-hoc evaluation stratum. (4) **Champion/benchmark schema separation** enforced as an invariant (§9.4): delivery-day A69 is forbidden in the champion runtime schema and permitted only in the explicitly-labeled benchmark; the proper-training-only A75 fit lineage is checked separately. (5) **Three-stage coverage** (raw / post-CQR-pre-isotonic / final): the **formal CQR guarantee attaches only to the post-CQR/pre-isotonic intermediate; final post-isotonic coverage is empirical** (§1, §6.2, §8.4, §9.4, §10, CP-3, §13). (6) **SHAP gates de-biased:** the CP-3 named-feature requirement is removed and the SHAP↔permutation overlap is descriptive, not a pass/fail gate; §4.1 allows "no headline domain feature." (7) **DuckDB SQL mart** (§9.6): the feature + data-quality mart is authored in SQL against **DuckDB** (the SQL-A production subset integrated at M1) — the SQL supplementary block's real database, not a toy. (8) **Offline batch data/output-health report** (§9.5): freshness / schema conformance / feature-aware matched distribution shift / matched interval width / quantile sanity, generated at **M4/CP-4**, static-rendered at **CP-5** (no labels → no performance-drift claim; ≤~8 h; no new services / dashboard / scheduled retraining / AWS). The parked AWS backbone (DEC-AWS), if ratified, now amends **v6.3 → v6.4**. Everything not touched by the strict-gate reframe carries forward from v6.2 verbatim: the D7 static-page rider, the LightGBM + CQR + isotonic-last core, the three-regime folds, the DM machinery, and every surviving §13 boundary.

**v6.5 → v6.6 (2026-08-05; Gauntlet contract amendment, AMD-G1 … G14).** Owner-ratified, **execution-contract only**. §§2–3, §§5–11 and §13 are byte-identical to v6.5; §1 carries this delta block, and **§4.1's canonicalization pointer was corrected** — it named three retired `blind-*` commands and cited templates §4–§6 for a contract those sections do not hold, so **a CP-2 Lead must re-read §4.1**. No bar, checklist item, invariant, acceptance criterion, feed identifier, fold scheme, metric or scope boundary moved. The version bump exists because three changes alter what `PASS` *means*: the SHA a bar binds to (`final_candidate_sha` split from `evidence_tip_sha`, with a verdict-only delta between them, closing a circularity that made the old item 7 unsatisfiable by any ordering), the statuses a checkpoint may return (`BRIEF_INVALID` added as a fifth, pre-work status; abandonment named as owner-side and explicitly not `BUDGET_EXHAUSTED`), and the conditions that invalidate a `PASS`. Mandatory-surface scope is now declared with a reason rather than inferred. The mechanics live in `engineering-role.md`, `AGENTS.md` § *Branch and ref lifecycle*, and `docs/track-b/gauntlet-templates.md` §§7–10; the full record is `capstone_V6_5-to-V6_6-amendments.md`, driven by the seventeen defects in `docs/track-b/cp-0-defects.md`. **DEC-1 requisitioned this number, so DEC-AWS now cascades to v6.7 + map v9.**

**v6.4 → v6.5 (2026-08-04; capture-schedule feasibility amendment).** Owner-ratified, scope-preserving. The §3 point-in-time capture contract is unchanged in substance — same three non-consecutive delivery days, same two pre-gate instants, same fail-closed rule on partial or rate-limited requests — but it no longer executes *inside* CP-1. The capture spans at least five calendar days, while a checkpoint's clock measures active-elapsed *work* rather than calendar span, so the two are not co-schedulable at any plausible ceiling: treating the wait as an eligible pause would freeze all CP-1 work for those days, and not treating it as one would run roughly 120 h of clock and exhaust the checkpoint on day one. (CP-1's 6 h in §12 is a **planning reserve** the Orchestrator combines with the milestone's existing allocation at issue time to derive the brief's ceiling — not a stand-alone runtime ceiling; the collision is structural and does not turn on that number.) The work is therefore split three ways. **M0.5/CP-0** is a new bounded Gauntlet checkpoint whose only scope is authoring and independently reviewing the capture instrument. **B-Man-PIT** is an owner-run manual block that executes it on the qualifying days; running and waiting need no agent and no Gauntlet clock. **CP-1** keeps the bar but verifies the resulting ledger rather than producing it, so a fresh Critic still independently checks hashes, UTC provenance, DST handling and D+1 completeness. No scientific criterion is weakened or removed; CP-1 cannot close without a complete, independently verified ledger.

**v6.3 → v6.4 (2026-08-02; Gauntlet execution contract + statistical-audit corrections).** Owner-ratified, documentation/governance release; no M1 code was started and CP-1 remains pending. Operational validation waits for the first authorized CP-1 run. The scientific scope and strict-gate architecture are unchanged. (1) **Forward confirmatory audit (AMD-1):** the five pinned folds remain development, catalog-selection, and stress-diagnosis evidence; their DM results are explicitly post-selection/descriptive. A sealed future window opens once, only after the catalog/model/hyperparameters are frozen and the pre-registered sample/power rule in §7.1 is met. An underpowered result is never forced into a pass/fail p-value. (2) **Point-in-time load evidence (AMD-2):** the 15:35 spike proves observed post-gate presence, not empirical pre-gate presence; M1 now requires a hashed pre-gate capture ledger across multiple delivery days. (3) **Experiment lineage (AMD-3):** the gameable `≥5 runs` checkbox is replaced by a complete hypothesis → parent → controlled delta/budget → result → keep/reject lineage. (4) **Independent artifact criticism:** the Engineering Lead runs each authorized checkpoint as a bounded Gauntlet, with fresh Builder/Critic contexts, mandatory clean-detached Critic checkouts and provenance, five named high-risk critic surfaces, a fresh Integration Critic, a single return packet, and a hard stop before the next checkpoint (§12 and the role docs). (5) **Bounded cost:** the Orchestrator alone supplies the total checkpoint active-elapsed wall-clock ceiling; exhaustion produces a non-PASS return, never a weakened bar. The net **planning** reserve is exactly **22 h** across M1–M5. The parked DEC-AWS proposal, if later ratified at G5, amends the then-current plan and rebuilds stage map **v6 → v7**. *(Superseded numbering: v6.5 was taken by the 2026-08-04 capture-schedule amendment, so DEC-AWS would produce v6.7 + map v9 (v6.6 was requisitioned by the 2026-08-05 Gauntlet contract amendment).)*

## 2. The DE-LU Day-Ahead Market: Context and Regime Shifts

DE-LU is the German–Luxembourg bidding zone (split from DE-AT-LU on **2018-10-01**; the project's 2019-01-01 window start sits cleanly inside the continuous DE-LU series). The day-ahead price clears in the EPEX SPOT auction within the EU Single Day-Ahead Coupling (SDAC), solved by the EUPHEMIA algorithm: order book closes **12:00 CET D-1**, results publish ~12:45–12:57 CET. Harmonized clearing-price bounds are **+4,000 / −500 EUR/MWh**; the cap auto-increases by +1,000 EUR/MWh after near-cap events (it rose +3,000 → +4,000 effective 2022-05-10, triggered by French hours of €2,712.99 and €2,987.78 on 2022-04-03; a further step to +5,000 was announced and then frozen). **The floor moves to −600 EUR/MWh from 2026-05-28** — a data note for the snapshot era, since the deployed demo's weekly snapshots will straddle that change.

The model must absorb a **three-regime non-stationarity**. These are not historical curiosities — they are the live structure of current pricing behavior.

### 2.1 The 2021–2023 energy crisis and its normalization

German day-ahead prices averaged ~€30/MWh across 2015–2020, then ~€97 in 2021, **~€235 in 2022** — with the **August 2022 monthly average near €469/MWh** as gas-fired plants set the marginal price in the vast majority of hours (day-ahead price vs. gas price correlation ~93% through the crisis; TTF peaked at €339/MWh on 2022-08-26). Normalization followed: €95.18 (2023), €78.51 (2024), €89.32 (2025, Bundesnetzagentur). The crisis is an exogenous structural break of textbook clarity — it ran for ~18 months and is cleaner than any single-storm event.

The model carries two explicit regime features encoding this timeline: `crisis_period` (binary, 2021-09-01 ≤ t ≤ 2022-12-31 — the gas run-up through the peak year; the invasion date 2022-02-24 is the hard exogenous anchor inside it) and `post_crisis` (binary, t ≥ 2023-01-01). Together they encode the pre-crisis / crisis / post-crisis partition. Both are first-class features (Section 4), and the Month-1 change-point block validates the boundaries empirically rather than assuming them — the detector should land near the autumn-2021 run-up and/or the early-2022 shock, and near the late-2022 normalization.

### 2.2 Solar build-out and the negative-price era

The post-crisis regime is not a return to 2019. German PV capacity reached ~100 GW by early 2026 (wind ~73 GW); the last three nuclear plants closed **2023-04-15** (treated as a secondary covariate, not a hard regime boundary — the crisis dominates the price record). The structural consequence is **solar cannibalization and a negative-price explosion**: official Bundesnetzagentur counts of negative day-ahead hours run 139 (2021) → 69 (2022, crisis-suppressed) → 301 (2023) → 457 (2024) → **573 (2025, a record)**, with a single hour at the **−500 floor in 2023** and a 36-consecutive-hour negative run the same year. The mechanism — high renewable infeed meeting must-run and subsidized inflexible generation — is reinforced by market-premium rules that historically paid producers through negative hours (the rule has tightened stepwise; the Solarspitzengesetz era continues that direction). Scarcity has its own modern face: **Dunkelflaute** (low wind + low solar + high load), e.g., the December 2024 / January 2025 events that pushed day-ahead hours toward €583/MWh and triggered a Bundeskartellamt probe (which found no abuse).

**Implication for the model.** The price distribution is non-stationary in three regimes with two distinct stress directions — high-price scarcity (crisis, Dunkelflaute) and negative-price abundance (solar middays). The system therefore mandates walk-forward CV spanning all three regimes with both stress directions represented in eval folds, explicit regime indicator features, and per-regime calibration checks — all covered below. The 2019→pull-date training window (Section 3) spans the full arc while keeping the pre-crisis era from dominating the data the forecast actually serves.

## 3. Data Sources

All data is free and **CC BY 4.0** (committed snapshot is legally redistributable with attribution — Section 9.3). The training window is **2019-01-01 → the actual M1 data-pull date (~7.5 years; endpoint finalized at the M1 pull)** — inside the continuous DE-LU zone (post-2018-10-01 split), long enough for a three-regime fold scheme, recent enough that the headline eval fold ends at current data, and small on a 16 GB machine (~66k hourly rows + ~26k native quarter-hourly rows post-transition; single-digit-MB compressed Parquet).

| Source | Content | Exact identifiers / gotchas |
|---|---|---|
| **ENTSO-E Transparency Platform via `entsoe-py`** (**pinned 0.8.0** — satisfies the ≥0.7.5 15-min-MTU-aware line) | DA prices; DA total load forecast; DA wind-onshore / wind-offshore / solar generation forecasts (**v6.3: delivery-day A69 is post-gate → benchmark-only, never a champion runtime feature; §5.2/§7**); actual load and actual generation per type (**diagnostics only, except aggregate A75 VRE is the proper-training-only fit target for the §4.1 climatology; no actual is a runtime feature**) | DE-LU EIC **`10Y1001A1001A82H`**. Document types: prices **A44**; load forecast **A65 / processType A01** (actual load = A65/A16); wind & solar forecast **A69** (A01); aggregated generation forecast A71; actual generation per type **A75**. `entsoe-py` getters: `query_day_ahead_prices`, `query_load_forecast`, `query_wind_and_solar_forecast`, `query_load`, and **`query_generation` for A75** — all accept `DE_LU`, return tz-aware `Europe/Berlin` (convert to UTC on ingest). **Requests cover ≤ 1 year each** (chunk the bulk pull by year); ≤100 TimeSeries per XML response. Token: env var, never commit. **Spike-verified (2026-06-12):** archive depth to 2019-01-01 confirmed for A44 / A65-A01 / A69 (**A65/A01 begins ~25h after the window start** — trim or accept documented head NaNs at M1); **A75 archive depth and coverage for wind-onshore / wind-offshore / solar are an explicit M1 gate because the new climatology depends on them**; feature feeds are natively PT15M across the entire archive — aggregate to hourly on ingest; **A44 alone is date-conditional** (PT60M → PT15M at 2025-10-01, auto-switched by `entsoe-py`; `resolution=` deprecated). **Pull-window artifact (spike-surfaced):** a request ending exactly on a quarter-hour yields a partial final hourly bin — chunked pulls must complete or drop boundary bins (§4.0). No migration symptoms observed in-session (R-3 monitored, not closed). SFTP bulk path (monthly CSVs, account-only) remains the second ingestion route; port-22 reachability untested from the spike sandbox — verify from the M3 only if the API path degrades. |
| **SMARD (Bundesnetzagentur) JSON API** | Same series family for DE/LU: DA prices, load, forecasts, generation by source | No auth, no key. `https://www.smard.de/app/chart_data/{filter}/{region}/...json`; numeric filter codes (e.g., 4169 = DA price); resolutions quarterhour/hour/day. Sources from ENTSO-E per Regulation 543/2013 → **cross-validation source** (CP-1 reconciliation gate) and **fallback-primary** if ENTSO-E access degrades. **Spike-verified:** cross-pull works; 96/96 sample hours within €0.01/MWh of ENTSO-E (max diff 0.005 — 2-decimal rounding noise). **Gas probe adjudicated (2026-06-12):** `chart_data` exposes gas-fired generation volume (filter 4071) only — **no THE price series**; §0 item 3 reopen closed. |
| **Ember / ICAP (optional)** | Daily EUA carbon allowance price | CC BY; optional LAG feature (coal/gas switching driver). Wired only if trivially clean at M1 — never load-bearing. |

**Forecast publication deadlines (the KFT foundation, from Commission Regulation 543/2013):** the day-ahead **total load forecast** must be published **no later than two hours before day-ahead gate closure** (Art. 6(2)(b)) — i.e., by ~10:00 CET D-1, so it is KFT **by regulation**. The Month-0 spike observed the D+1 A65/A01 vector at 15:35 CEST D-1; that corroborates feed availability but occurred after the 12:00 gate and therefore does **not**, by itself, constitute empirical pre-gate proof. M1 supplies the missing point-in-time evidence under the capture contract below. The day-ahead **wind & solar forecast** deadline is **18:00 Brussels time D-1** (Art. 14(2)) — *after* the 12:00 gate in clock time. **Spike-observed (2026-06-12):** at 15:35 CEST D-1, A69/A01 for the delivery day returned no data — the TP's archived VRE vintage **post-dates the gate by initial publication** (published in the 15:35–18:00 D-1 window). These series remain the standard exogenous covariates across the EPF benchmark literature (including the canonical Lago EPEX-DE dataset), **but v6.3 treats their post-gate status as disqualifying for the shipped champion:** A69 is **not KFT**, so it is **excluded from the champion feature schema** (§5.2) and enters only the explicitly-labeled **post-gate information benchmark** (§7). The champion instead relies on gate-feasible features — the proxies **where the M2/CP-2 ablation retains them** (§4.1), else the strict-gate base alone. The remaining archive-overwrite question — whether values change after initial publication — is closed by the committed snapshot/compare harness before M1; it is now **moot for the champion** (which never consumes A69) and retained only for the benchmark's provenance. **This is risk R-2 (Section 11).**

**Point-in-time knowledge/freshness capture contract (v6.4; AMD-2).** A65/A01 is captured on at least **three non-consecutive delivery days**, at two pre-gate instants per day: approximately **10:30 and 11:45 Europe/Berlin on D-1**. A failed, partial, or rate-limited request does not count; repeat that capture on another day. **(v6.5)** The instrument is authored and independently reviewed at M0.5/CP-0; the captures themselves run in the owner-run B-Man-PIT block on the qualifying days; CP-1 independently verifies the resulting ledger. The evidence contract below is unchanged by that split. Any other external feed admitted to the champion runtime must satisfy the same evidence contract.

Every attempt retains the raw response and appends an immutable ledger row containing: source; feed/document/process identifier; delivery date; `gate_at_utc`; `pulled_at_utc`; local-market pull time and UTC offset; HTTP/result status; expected and observed row counts; completeness ratio; first and last delivery timestamps; latest fully populated timestamp; payload SHA-256; raw-artifact path; and one of `present_complete`, `present_partial`, `absent`, or `request_error`. Expected PT15M row counts are DST-aware (92/96/100 before hourly aggregation).

A successful capture proves only **observed available by `pulled_at`**; it is not rewritten as the source's exact first-publication time. Regulatory-deadline evidence and observed point-in-time evidence remain separate fields and separate claims. A65/A01 earns the label **empirically pre-gate** only if a complete D+1 vector is present before 12:00 on each of the three qualifying days. The 2026-06-12 15:35 observation remains post-gate corroboration, not pre-gate proof. A69 remains post-gate and benchmark-only; this capture work does not reopen it for the champion.

Secondary feeds present in the PJM version (gas spot trio, external weather, LMP decomposition components) are dropped: gas per §0 item 3, weather per §0 item 5, and decomposition because the DE-LU day-ahead price is a single zonal clearing price with no SMP/MCC/MLC split — the cross-source reconciliation role moves to the ENTSO-E↔SMARD price comparison (CP-1).

## 4. Feature Catalog

Approximately 25 features, grouped by source. Every feature is classified as either **known-at-forecast-time (KFT)** — publicly available before the 12:00 CET D-1 gate — or **lagged-only (LAG)**. This distinction is the foundation of the leakage audit in Section 5.2. **v6.3:** the DA VRE forecast (A69) and everything derived from it are **neither KFT nor LAG-eligible for the champion** — A69 is published *after* the gate (§3, §5.2), so it is **excluded from the champion schema** and appears only in the §7.2 post-gate benchmark; the champion substitutes gate-feasible proxies **where the M2/CP-2 ablation retains them** (§4.1), else ships on the strict-gate base. Because the target is the next delivery day (D+1, Section 1), every LAG feature below is legitimately available at forecast time: the most recent cleared DA price is for day `D0`, and a 24h lag of a `D0+1` target lands on `D0`.

### 4.0 Time indexing, DST, and the 15-minute transition (mandatory)

The entire pipeline is indexed on **UTC**. `entsoe-py` returns tz-aware `Europe/Berlin` timestamps; convert to UTC on ingest and compute all lags in UTC so 24h = 24 rows, 168h = 168 rows, always. The spring-forward day carries **23 local hours** and the fall-back day **25** (a repeated local hour) — hours are never dropped or duplicated ad hoc; the user-facing "24-value D+1 vector" renders as 23 or 25 values on the two DST-transition days, published as UTC-derived local hours with an explicit DST note. **15-minute MTU boundary (2025-10-01):** post-transition quarter-hours are averaged to hourly per §0 item 4; the hourly series must be continuous across the boundary with the post-transition hourly value equal to the mean of its four quarter-hours (**spike-validated on samples to 1e-9**).

**Spike-established (2026-06-12):** the load and VRE feeds are **natively PT15M across the whole 2019→present archive** — the quarter-hour→hour mean aggregation applies to the feature feeds **uniformly from day one**; only the price needs date-conditional handling. **Chunk-boundary bin integrity (mandatory):** the yearly chunked pulls produce partial hourly bins where a pull window starts or ends mid-hour on a PT15M series; ingestion must complete bins across chunk edges or drop-and-recover them, and an assertion guards that every aggregated hourly value is built from exactly four quarter-hours (extended invariant test 4, Section 9.4). Left unhandled, this silently loses ~1 hour per chunk boundary. All calendar and aggregation invariants are guarded by tests (Section 9.4) — they correct the integer-lag corruption that otherwise silently hits ~2 shoulder-season days per year plus every PT15M row.

**Calendar (KFT)** — hour-of-day, day-of-week, month, **German federal holiday flag** (the `holidays` package "DE" federal subset — the standard simplification in German EPF papers; state holidays out of scope), `is_day_after_holiday`, **`is_bridge_day`** (Brückentag), 3-level day-type {weekday, Saturday, Sunday/holiday}, daylight-saving transition flag, summer-peak / winter-peak season flags.

**Load forecast (KFT)** — ENTSO-E DA total load forecast for DE-LU (A65/A01; published ≥2h pre-gate by regulation, spike-confirmed). Same-hour-tomorrow value and rolling 24h forecast level.

**VRE forecasts (post-gate — NOT KFT; benchmark-only, v6.3)** — DA wind-onshore, wind-offshore, and solar generation forecasts (A69/A01). Post-gate at the 12:00 gate (§3, §5.2), therefore **excluded from the champion**; used only inside the §7 post-gate information benchmark and its residual-load composite.

**Residual load — two constructions (v6.3).** The delivery-day-A69-based `residual_load_fc(d,h) = load_fc(d,h) − wind_onshore_fc(d,h) − wind_offshore_fc(d,h) − solar_fc(d,h)` is **post-gate → benchmark-only**. For the **champion**, a **gate-feasible residual-load proxy** substitutes the single pinned seasonal-diurnal VRE expectation defined in Section 4.1 — a *candidate* feature whose value is decided by matched ablation at M2/CP-2, **not** an assumed headline. This is the literature-standard German merit-order driver in its gate-feasible form.

**Lagged price (LAG)** — lagged DA price at lag-24h, lag-48h, lag-168h.

**Rolling price statistics (LAG)** — 7-day and 30-day rolling mean and standard deviation of the DA price; 168h rolling 5/50/95 quantiles; **7-day rolling count of negative-price hours** (the DE-specific abundance-regime memory feature). **All rolling windows use a strict `t−1` right edge and are closed-left**, so the current target is never inside its own feature window (leakage guard, Section 5.2).

**Regime indicators (KFT)** — `crisis_period` (2021-09-01 → 2022-12-31) and `post_crisis` (≥ 2023-01-01) static-date binaries (validated, not assumed — Month-1 change-point block); **`dunkelflaute_flag` (post-gate — benchmark-only or evaluation stratum, v6.3)** — the derived scarcity flag: flag = 1 when the forecast VRE share (`(wind_on + wind_off + solar)_fc / load_fc`) falls below a low train-set quantile **and** the load forecast exceeds its train-set median; thresholds are **train-only, refit per fold**. **Because it consumes the A69 VRE forecast it is post-gate → NOT KFT → excluded from the champion.** It survives only as a §7 benchmark feature or a post-hoc regime-stratified evaluation stratum (§8.3) — never a claimed gate-known champion feature. Its gate-feasible **candidate** counterpart is **`expected_scarcity_proxy`** (§4.1), included in the champion only if retained at M2/CP-2 and honestly named because a pre-gate proxy cannot identify tomorrow's *actual* Dunkelflaute. There is no official timestamped Dunkelflaute alert (the v5.5 R-4 alert-sourcing risk class dies with the market switch); mining TSO/BNetzA scarcity notices is named as a stretch goal, not scope.

**EUA carbon (LAG, optional)** — daily EUA close at `t−1` relative to the delivery day, broadcast across the 24 hours; wired only if the Ember/ICAP pull is trivially clean at M1.

### 4.1 Domain-insight candidates (gate-feasible; headline decided at M2/CP-2) — v6.3

**v6.3 framing.** These are strict-gate *candidate* features, not a pre-ratified headline. Both arms share **one deterministic VRE-expectation construction** fixed here before results; there is no hidden climatology-vs.-lagged-vintage choice. Each is retained only if it earns its place by matched ablation at M2/CP-2 (selection rule below).

**Pinned gate-feasible VRE expectation (shared by both arms).** For each fold, aggregate historical A75 actual generation for wind-onshore + wind-offshore + solar on the **proper-training partition only** (never the held-out calibration slice and never eval), and fit the seasonal-diurnal OLS model

> `vre_exp(d,h) = δ0 + Σ_{k=1..3}[c_k sin(2πk·DOY/365.25) + d_k cos(2πk·DOY/365.25)] + Σ_{j=1..23}η_j·1[hour=j] + Σ_{k=1..2}Σ_{u∈{sin,cos}}Σ_{v∈{sin,cos}}ρ_{k,u,v}·u(2πk·DOY/365.25)·v(2πh/24)`

This is a fixed **38-coefficient** basis: intercept, six annual-harmonic terms, 23 reference-coded hour dummies (hour 0 omitted), and eight interactions between the first two annual harmonic pairs and the first diurnal sine/cosine pair. The hourly aggregation follows §4.0. The fit is re-estimated per fold; rows with missing or incomplete A75 VRE are excluded and their count is logged. At inference, `vre_exp(d,h)` consumes **only known calendar coordinates and the frozen coefficient vector** — no live feed, no delivery-day A69, and no actual value. Historical A75 is therefore a **fit target, not a model feature**. A lagged-A69/vintage proxy is outside the frozen four-catalog experiment and belongs only in future work. The VRE-expectation coefficients are persisted in every candidate artifact and in the champion if **either** proxy arm wins.

The gate-feasible residual arm contains exactly one feature:

> `gate_feasible_residual_load_proxy(d,h) = load_fc(d,h) − vre_exp(d,h)`

No second residual-seasonal-normal feature is fitted. With the same seasonal-diurnal OLS basis used for `vre_exp`, subtracting a second fitted normal from this proxy would algebraically cancel the VRE projection and reduce to de-seasonalized `load_fc`; labeling that result residual-load signal would be misleading. The one-feature arm preserves the intended gate-feasible merit-order proxy directly and removes a redundant preprocessing layer.

**Naming (v6.3, disambiguated):** `gate_feasible_residual_load_proxy` is the **champion candidate** built on a pre-gate VRE expectation; the A69-based `residual_load_fc` and its A69-derived diagnostics are **§7.2-benchmark-only** and never enter the champion.

A **secondary candidate**, `expected_scarcity_proxy`, encodes the joint scarcity condition as a gate-feasible composite:

> `expected_scarcity_proxy = relu(load_z) · relu(−vre_exp_z)`

where `load_z` is the gate-available load forecast and `vre_exp_z` is the **pinned §4.1 VRE expectation**, both standardized on **proper-training statistics only** (refit per fold). The product fires only when load is *above* its mean **and** seasonally expected renewables are *below* theirs — the *expected* scarcity corner. Honestly named: it cannot identify tomorrow's *actual* Dunkelflaute, only expected scarcity from calendar-known information. Its interview defense (why `relu`, why a product — a ratio cannot encode an AND condition; a product can) carries from v5.5 and is rehearsed if the candidate is retained.

**Selection experiment (v6.4 operational precision; scientific rule unchanged).** The strict-gate candidates enter from M1. Their value is decided at **M2/CP-2 by exactly four pre-specified matched catalogs — (1) `strict_base`; (2) `residual_arm`, the base plus {`gate_feasible_residual_load_proxy`}; (3) `scarcity_arm`, the base plus {`expected_scarcity_proxy`}; and (4) `both_arms`, the base plus both arms** — run on the same five pinned folds with all non-catalog inputs, row identities, targets, budgets, seeds and fitting/calibration boundaries matched. Two arms that pass individually may be redundant or harmful together, so the full 2×2 is mandatory.

**Exact raw-head metric boundary.** Let `T={0.025,0.05,0.10,0.25,0.50,0.75,0.90,0.95,0.975}` and `ρ_τ(u)=max(τu,(τ−1)u)`. For catalog `c`, fold `f`, and its `n_f` frozen evaluation rows, `L_c,f = Σ_iΣ_τ ρ_τ(y_i−q̂_c,i,τ)/(9n_f)`. The pooled loss is **observation-weighted across every row, fold and all nine quantiles**, `L_c = Σ_fΣ_iΣ_τ ρ_τ(y_i−q̂_c,i,τ)/(9Σ_f n_f)`; it is not an unweighted mean of five fold means. Raw 80% coverage is `C_c = Σ_fΣ_i 1[q̂_c,i,0.10 ≤ y_i ≤ q̂_c,i,0.90]/Σ_f n_f`, with both endpoints inclusive and the submitted q10/q90 values used as-is. **Raw quantile crossing is allowed at M2:** the blind protocol neither repairs nor rejects crossing and performs no isotonic rearrangement. CQR and isotonic belong to M3 and never enter these metrics.

**Canonical numeric, CSV and JSON contract.** Every identity-bearing source CSV has the exact header `fold_id,row_id,y,q025,q05,q10,q25,q50,q75,q90,q95,q975` and sort `(fold_id numeric, row_id UTF-8 byte order)`. The anonymous `blind-public-input.csv` adds only a leading `label` column and uses the exact header `label,fold_id,row_id,y,q025,q05,q10,q25,q50,q75,q90,q95,q975` and sort `(fold_id numeric, row_id UTF-8 byte order, label A→D)`. Both are UTF-8/LF. `row_id` is an opaque token matching exactly `^r[0-9]{6,}$`; it contains no timestamp or catalog identity. Any timestamp/semantic-identity lookup used to produce those tokens remains in the identity-bearing source/preparation surface and outside the Blind Critic context. Numeric fields are finite base-10 strings; NaN, infinity, duplicate/missing matched rows, binary-float-derived decision comparisons and presentation-rounded inputs are invalid. The recomputation parses numbers as `Decimal`, normalizes serialized zero to `0`, emits deterministic non-exponent decimal strings, and performs threshold/tie comparisons by exact Decimal or integer cross-products before any display rounding. Every JSON artifact required by this section, including the two committed identity inputs, is UTF-8 serialized exactly as sorted keys with two-space indentation, LF line endings and one final newline; semantic-but-byte-noncanonical JSON is invalid. Integer and Boolean fields in those artifacts are also representation-strict: an integer must decode as a non-Boolean JSON integer, a Boolean must decode as a JSON Boolean, and `true`, `1`, and `1.0` are never interchangeable. The numeric, CSV and JSON contract stated in this paragraph is itself the canonical contract; it is applied by hand. **There is no protocol tool, JSON schema, or `blind-*` command** — that machinery was retired, and the CP-2 procedure is the plain, procedural one in `engineering-role.md` § *CP-2 label-blind four-catalog review*: the Lead withholds the label→catalog mapping, hands over only the anonymised predictions, and reveals after the Blind verdict is written.

**Eligibility and deterministic winner.** If `L_strict_base = 0`, `strict_base` wins and no augmented catalog is eligible. Otherwise, augmented catalog `c` is eligible only when all three exact comparisons pass: **(i)** `(L_strict_base−L_c)/L_strict_base ≥ 0.01` (equality at exactly 1% passes); **(ii)** `C_strict_base−C_c ≤ 0.02` (equality at exactly 2 percentage points passes); and **(iii)** `L_strict_base,f−L_c,f > 0` on at least four of the five folds (zero is not positive). If no augmented catalog is eligible, `strict_base` wins. Otherwise let `L_min` be the lowest pooled loss among eligible augmented catalogs. A catalog joins the tie set when `(L_c−L_min)/L_min ≤ 0.001` (equality at exactly 0.1% passes); if `L_min=0`, only catalogs with exactly zero pooled loss are tied. Resolve that set by **fewer added features** (`residual_arm=1`, `scarcity_arm=1`, `both_arms=2`), then smaller exact `|C_c−0.80|`, then the fixed order **`residual_arm` → `scarcity_arm` → `both_arms`**. The winner is frozen at CP-2 before M3. SHAP at M3 explains retained features; it is not the incremental-value test. **"No catalog clears" is fully acceptable:** the champion then ships as `strict_base`, with no forced headline. `forecast_uncertainty_proxy` is future work (§13), not a fifth catalog; `gas_to_load_ratio` remains dead.

**Mandatory independence split at CP-2.** The final candidate commits the identity-bearing four-catalog source manifest at `artifacts/cp2/blind/source-manifest.json` and the identity-bearing selection declaration at `artifacts/cp2/blind/selection-declaration.json`, both blob-hash-bound. A separate **label-blind four-catalog review** receives only anonymous `A/B/C/D` rows in `blind-public-input.csv` and identity-free public metadata, independently recomputes the exact metrics above, and records a `PASS` without choosing or asserting a winner. Only after that verdict is written and committed may the mapping be revealed; a revealed mapping is never reused. A fresh Integration Critic then applies the identities, eligibility rule and tie-breaks above and verifies that the mechanically adjudicated winner equals the committed selection declaration. Blinding is **procedural** — the Lead withholds the mapping file — and is labelled `COOPERATIVE_PROCEDURAL`, never cryptographically enforced; §12 and `engineering-role.md` bind the handover mechanics.

### 4.2 Spectral EDA (dominant-frequency confirmation) — merged at v6.2

Before freezing the calendar/seasonal feature catalog and the regime-stratified reporting plan, we confirm the **price series'** dominant periodicities empirically. We compute a **Welch power-spectral-density estimate** of the detrended, log-untransformed hourly DE-LU price series (Hann window, 50% overlap, `nperseg` a power of two ≥ 2×168 so the daily and weekly bins are both resolved) and label the peaks at f = 1/24 h⁻¹ (daily), 1/168 h⁻¹ (weekly), and 1/12 h⁻¹ (half-day harmonic). These confirm the calendar features (hour-of-day, day-of-week) and motivate testing seasonal candidate constructions; because the periodogram is computed on price (A44), it **does not validate a residual-load proxy or justify retaining it** — that decision belongs exclusively to the M2/CP-2 ablation. The same frequency triad is documented for European day-ahead prices (Weron 2014; Lago et al. 2018). To show that the spectrum is **non-stationary across regimes** — diagnostic evidence that performance must be evaluated across the change-point-defined regimes (§5.1) — we compute per-regime Welch periodograms (pre-crisis 2019–2021; crisis 2021–2022; normalization late-2022→) and note the elevated broadband floor and altered seasonal amplitude in the crisis regime (the crisis-era variance and negative-price spikes inflating the floor is a *feature* of the per-regime plot, not a defect to correct). An ACF plot with reference lines at lags 24h and 168h serves as the time-domain cross-check; an MSTL decomposition (periods 24, 168) is optional.

**Implementation:** module `spectral_eda.py` + thin rendering notebook; detrend/difference before the periodogram so low-frequency trend does not swamp the peaks; irregular timestamps/DST are already resolved upstream by the §4.0 UTC discipline. **Committed artifacts (three mandatory, one optional):** `fig_welch_periodogram.png` (labeled 24h/168h/12h peaks), `fig_per_regime_periodogram.png` (three overlaid curves), `fig_acf_24_168.png`, optional `fig_mstl_decomposition.png`. Acceptance: runs end-to-end on the ingested series in <60s on the M3/16GB CPU stack; `annotate_peaks` returns rows within one FFT bin of 1/24 and 1/168 h⁻¹; no dependency beyond scipy/statsmodels/pandas/matplotlib. A 3–4 sentence interpretation paragraph is written into the project README linking price peaks → calendar structure, treating the residual-load proxy as an ablation candidate rather than a spectral conclusion, and linking per-regime spectral change → regime-stratified, multi-regime evaluation.

**Scope guardrails (defended boundary, same class as §13):** descriptive analysis only. No filter design, no wavelet/EMD work, no forecasting in the frequency domain, and no spectral features in the operative v6.3 model catalog. The periodogram justifies the measured calendar periodicities and motivates regime-stratified diagnostics; it does not determine fold boundaries; it proves no causal claim — the README paragraph says "confirms/justifies," never "proves." Budget 6–10h inside M1's window; overrun escalates per R-5 (§11). Depth [APPLIED-AUTH]: Yarden defends Welch-vs-raw-periodogram, windowing, leakage, and detrending in interview — cheap given the signal-processing background, and the whole strategic point of the extension.

## 5. Methodology: Walk-Forward CV and Data Leakage Audit

The two methodology choices below are the top sources of overstated results in the EPF literature (Lago et al. 2021) and the top questions a DS interviewer asks about a time-series project. Both are spelled out explicitly so they live in the final report.

### 5.1 Walk-forward expanding-window CV with 24h embargo — pinned three-regime scheme

Validation uses **expanding-window walk-forward CV with five folds**, each with a **24-hour embargo gap** and a **90-day eval block**, anchored so the eval blocks span **all three regimes and both stress directions** (≥2 pre-crisis, 1 crisis-peak, ≥2 post-crisis including a negative-price-stress block). Concentrating the blocks in the recent tail would make the regime-stratified table (Section 8.3) un-fillable from eval folds and would violate the EPF principle of testing across regimes.

**Pinned scheme** (training window 2019-01-01 → the M1 pull date; each fold trains on all data up to its eval start minus the 24h embargo; expanding — each training set is a superset of the prior):

| Fold | Eval block | Regime |
|---|---|---|
| 1 | 2020-07-01 → 2020-09-28 | pre-crisis (COVID-recovery summer — flagged as atypical-demand in the stratified read; training span ~1.5y, the thinnest fold by design) |
| 2 | 2021-04-01 → 2021-06-29 | pre-crisis — the last clean window before the autumn-2021 gas run-up |
| 3 | 2022-07-01 → 2022-09-28 | **crisis peak** — contains the ~€469/MWh August-2022 month; the high-price stress block |
| 4 | 2025-05-01 → 2025-07-29 | post-crisis; **solar/negative-price stress block** — the record-negative-hours summer (573 negative hours in 2025, summer-clustered) |
| 5 | final 90 days ending at the most-recent available data at the M1 pull (dates pinned at the pull) | post-crisis current tail; **fully post-15-min-transition** — the headline eval is deliberately evaluated on the quarter-hour-averaged hourly series the deployed product actually serves |

The regime-stratified error table is populated from eval folds for all three regimes. Finer stress rows (e.g., the December-2024 Dunkelflaute week) are sourced from **training-period backtesting** — re-predicting in-window days with a model trained only on data prior to those days — standard, and not an out-of-sample claim.

**Recalibration cadence (carried from v5.5).** At each fold boundary, re-fit the LightGBM heads and CQR thresholds; re-fit the shared VRE climatology and proxy standardizations **only for catalogs that contain those candidates and, after CP-2, only if retained by the champion**; for the §7.2 benchmark only, also re-fit the A69-derived Dunkelflaute/VRE-share thresholds. Within a 90-day block, predict D+1 sequentially without peeking. Per-day recalibration is out of scope under the $0 compute ceiling; per-fold recalibration is the stated lean substitute (§0 item 1).

**Why the 24h embargo suffices given 48h/168h lags (verbatim reasoning, market-relabeled).** The embargo's only job is to break the autocorrelation adjacency between the **last training target** and the **first validation feature**. A 168h-lagged feature of a validation row reaches back into the training period — but using a known, already-cleared historical price as an input feature is not leakage; it is exactly what the production model has at the 12:00 CET gate. A 24h embargo (one full target horizon) removes the only true adjacency. Document this reasoning verbatim for the interviewer.

**Selection/confirmatory separation (v6.4; AMD-1).** At CP-1, after the point-in-time capture work is complete, freeze `selection_cutoff` as the final complete delivery date included in the ratified M1 snapshot. Every delivery date strictly after that cutoff belongs to the **forward-audit partition**. Forward-audit targets, losses, metrics, and model comparisons are quarantined from feature construction, catalog selection, hyperparameter tuning, calibration fitting, threshold choice, narrative selection, and all five development folds. The five pinned folds remain unchanged and serve catalog selection, tuning, and regime/stress diagnosis.

Before any forward-audit outcomes are opened, CP-2 freezes the winning catalog, model hyperparameters, preprocessing lineage, seeds, comparator, daily-loss construction, and selection-data manifest; CP-3 freezes the complete CQR-then-isotonic champion and its tagged code artifact. The forward audit is then run once, on the first eligible window meeting the pre-registered duration and power rule in §7.1. Forward-audit rows are never recycled into training, calibration, model selection, or remediation for this release. A revision made after seeing the audit requires a new model version and a new future audit window.

### 5.2 Data leakage audit

Every feature in Section 4 is classified KFT or LAG with explicit justification. The D+1 target makes the LAG features legitimate: no LAG feature consumes a value dated on or after the delivery day.

- **KFT features.** Calendar and static-regime features are known by construction. A65/A01 is pre-gate by Regulation 543/2013 and is treated as empirically demonstrated only after the §3 capture contract passes. Until then, the record distinguishes the regulatory deadline from the Month-0 post-gate observation. Every retained runtime proxy inherits only inputs whose pre-gate status is established under this rule. **v6.3:** `dunkelflaute_flag` is **removed from the champion's KFT set** — it consumes the post-gate A69 forecast, so it is not gate-known (see the VRE bullet).
- **VRE forecasts — the strict-gate resolution (R-2), v6.3.** Observed 2026-06-12: the TP's A69/A01 for delivery day D is **not yet published at 15:35 CEST D-1** — initial publication lands in the 15:35–18:00 window, *after* the 12:00 gate. **v6.3 resolves this by construction, not by convention:** delivery-day A69 is **post-gate → not KFT → excluded from the champion runtime schema.** The champion carries only gate-feasible runtime features — the proxies (§4.1) built from the KFT load forecast, calendar coordinates, and frozen proper-training-only A75 climatology parameters **where the M2/CP-2 ablation retains them**, else the strict-gate base alone. Delivery-day A69 and features computed from it enter **only** the explicitly-labeled **post-gate information benchmark** (§7.2), which quantifies what post-gate information would be worth but never ships. There is **no lagged-vintage implementation choice** inside the four catalogs: a lagged-A69 alternative is future work. The archive-overwrite harness (first snapshot 2026-06-12 for delivery 2026-06-13) remains benchmark provenance evidence; the champion's historical-VRE lineage is instead the explicit proper-training-only A75 fit in §4.1. Revision metadata (`revisionNumber`, `createdDateTime`) is a confirmed dead end (constant / equal to pull wall-clock). The strict-gate posture is a stronger interview asset than as-archived honesty: the shipped model is feasible at 12:00 by construction.
- **LAG features** use `t−1` or earlier relative to the forecast origin `D0`. Rolling statistics (including the negative-hour count) use a strict `t−1` right edge, closed-left.
- **Three audited vectors:** (i) rolling-quantile and rolling-count features computed closed-left so the target is never in its own window; (ii) the VRE-forecast vintage (above); (iii) `crisis_period`/`post_crisis` leak nothing as constants but are collinear with time inside any single-regime fold — a further reason for the three-regime scheme (Section 5.1).
- **Champion/benchmark schema separation (v6.3, replaces the forecast-not-actual guard).** Two enforced **runtime** feature schemas. The **champion runtime schema forbids delivery-day A69, every feature computed from that post-gate vintage, and every actual-value column (A65/A16, A74, A75)**. The only actual-data use is outside the runtime schema: aggregate historical A75 VRE is the §4.1 proper-training-only climatology fit target, and only its frozen coefficients may enter a retained proxy's preprocessing. The **post-gate benchmark runtime schema** explicitly permits delivery-day A69 and its derivatives and nothing else new. The invariant asserts runtime separation plus preprocessing lineage — champion prediction accepts no A69/actual column; the A75 fit uses proper-training rows only; and the benchmark differs from the frozen strict-gate runtime catalog only by delivery-day A69 and its derivatives (§9.4). This subsumes v5.5's forecast-not-actual guard while making the fit-target/runtime distinction explicit.
- **Target transform.** Do **not** apply log/Box-Cox to the target — the DE-LU DA price is *routinely* negative (573 hours in 2025) and has hit the −500 floor. Pinball loss, the CQR score, the additive conformal shift, and monotone rearrangement are all location-equivariant and behave correctly with negative targets and zero-crossing intervals (Section 6). If variance stabilization is ever wanted, use `asinh` (defined on ℝ); with LightGBM quantile heads no transform is needed. In this market the no-transform line is not an edge-case caveat — it is a demonstrated capability.

This audit is the single best preparation against the standard interview question *"How did you make sure your features weren't leaking?"*

## 6. Model: LightGBM Quantile Ensemble with CQR Calibration

*(This section transfers from v5.5 essentially verbatim — it is the methodological core the conversion deliberately preserves.)*

### 6.1 Single monolithic LightGBM, packaged as one model

The target is the hourly DE-LU day-ahead price for the next delivery day. The model is a **single LightGBM regressor family** predicting the nine native quantiles `{p2.5, p5, p10, p25, p50, p75, p90, p95, p97.5}` — one head per level, each fit independently with `objective='quantile'`.

**The champion is one artifact, not nine.** The nine quantile boosters, **only the preprocessing required by the frozen catalog** (the shared VRE-expectation coefficients if either proxy arm is retained; the relevant proper-training standardizations), the **CQR thresholds** (Section 6.2), the final isotonic step, and the feature pipeline are wrapped in a single custom `mlflow.pyfunc.PythonModel` exposing one `predict` that returns the nine calibrated, monotone quantiles. No delivery-day A69 or actual-value column enters `predict` (§5.2/§9.4). This is what is logged, registered, tagged `Production`, and loaded — one model, one Production tag, one `models:/delu_forecast/Production` load path. **The §7.2 post-gate benchmark is a separate, explicitly-labeled artifact with its own runtime schema and its own separate calibration — never registered as the champion.**

### 6.2 CQR calibration, and the post-processing order

LightGBM's native quantile heads have no finite-sample coverage guarantee, and independently fit heads can cross. Both are handled in order:

1. **Raw LightGBM** produces nine quantile predictions per row (may cross).
2. **CQR (Romano, Patterson & Candès 2019)** on each symmetric pair. For each interval `(lo, hi) ∈ {(p2.5,p97.5),(p5,p95),(p10,p90),(p25,p75)}` at miscoverage `α ∈ {0.05, 0.10, 0.20, 0.50}`, compute on the calibration set the conformity score `E_i = max{ q̂_lo(x_i) − y_i , y_i − q̂_hi(x_i) }`, take the threshold `Q_(lo,hi)` as the `⌈(n_cal+1)(1−α)⌉`-th smallest `E_i`, and form `[q̂_lo − Q, q̂_hi + Q]`. The median (p50) is left un-shifted for the point forecast. **Binding indexing rule (v6.4):** `k = ceil((n_cal+1)(1−α))` is a one-based rank; after ascending sort a zero-based implementation reads `scores[k−1]`, never `scores[k]`, and never substitutes an interpolated default quantile. Require `1 ≤ k ≤ n_cal`; an undersized calibration set raises a clear error instead of silently clipping. Under the formal conformal construction an out-of-range upper rank corresponds to the augmented `+∞` order statistic, so clipping would overstate the guarantee. The pinned production calibration slices are far above the finite-threshold minimum for `α=0.05`.
3. **Isotonic regression (last)** across the nine conformalized endpoints per row, as the final ordering guard.

**Calibration set (pinned, carried from v5.5):** the **final 60–90 days of each fold's training partition (~1,440–2,160 hourly rows)**, held out and disjoint from both proper-training and eval, refit per fold. The tradeoff is stated once: those rows leave proper-training — negligible against years of training data, and it keeps the conformity scores on the most-recent (most regime-relevant) data.

**Mandatory hand-checkable CQR fixture (v6.4).** The raw synthetic fixture has rows `j=0,…,19` with `(q̂_lo,j, q̂_hi,j, y_j)=(100,140,111−j)`. Therefore `E_j=max{100−(111−j),(111−j)−140}=j−11`, giving the ascending scores `−11, −10, …, 8` without supplying the answer as the only input. For `n_cal=20`, the four `α={0.05,0.10,0.20,0.50}` pairs must produce one-based ranks `{20,19,17,11}`, zero-based positions `{19,18,16,10}`, and thresholds `Q={8,7,5,−1}`. A fresh read-only M3 Critic recomputes the scores from the raw `(q̂_lo,q̂_hi,y)` rows and all four thresholds independently, verifies that negative `Q` narrows its pair, verifies that p50 is unchanged by CQR, and then recomputes the four real-fold thresholds from persisted calibration predictions. Isotonic remains a separate final operation.

**Why CQR, and why isotonic is still last (verbatim).** For a **single already-ordered interval pair**, a nonnegative CQR threshold widens the pair and cannot invert its two endpoints; a negative threshold narrows it and can. That statement is only pairwise: the raw heads may already cross, and the four pair-specific thresholds can leave the **nine** post-CQR endpoints globally unordered for either threshold sign. Isotonic therefore remains the unconditional final ordering guard. **Coverage claim (v6.3 — three stages, stated precisely):** the finite-sample, distribution-free **per-interval marginal guarantee ≥ 1−α under exchangeability attaches to the post-CQR / pre-isotonic output** (stage 2). Isotonic is a monotone rearrangement applied last (stage 3) and is inactive only when all nine post-CQR endpoints are already non-decreasing. Whenever it moves an endpoint it perturbs the interval, so **the coverage of the final post-isotonic output (stage 3) is reported as empirical**, verified on the reliability diagram (§8.4) at all three stages (raw / post-CQR / final). Do **not** claim the formal guarantee for the final output, and do **not** claim joint/simultaneous coverage across the four intervals; both caveats are stated.

Stated in the final report verbatim, for interview defense:

> *CQR provides finite-sample marginal coverage guarantees under exchangeability. The walk-forward CV mildly violates exchangeability — the crisis regime is not exchangeable with the pre-crisis regime, and the solar-driven negative-price era is not exchangeable with either — so empirical coverage may diverge from nominal on regime-shift folds. This is documented in the reliability diagram (Section 8.4).*

That paragraph is the heart of the project's uncertainty story.

### 6.3 Why a single zonal target, not components

The PJM version defended a monolithic target against an SMP/MCC/MLC decomposition. DE-LU needs no such defense: the day-ahead clearing price is a **single zonal price by construction** — there is no congestion/loss decomposition to model separately at the bidding-zone level. The architectural simplicity is inherited from the market design; the section is retained so the interview answer exists: *"DE-LU is one coupled zonal price; decomposition modeling is a nodal-market question, not a bidding-zone question."* (Intra-zonal redispatch costs are a grid-operations quantity, not a component of the DA clearing price, and are out of scope.)

## 7. Benchmarks and Statistical Testing

LightGBM is compared against:

- **Similar-day naïve (headline comparator)** — the exact Lago et al. (2021) EPF convention: for Tue–Fri, forecast = same hour of the previous day; for Mon/Sat/Sun, forecast = same hour of the same weekday one week prior. Matching the published convention keeps the DM results comparable to the benchmark literature.
- **Seasonal-naïve 168h** — forecast = price 168 hours ago. Week-over-week seasonality baseline.
- **Ridge regression** with the same engineered feature catalog. Classical baseline; checks whether the boosted-tree complexity earns its place (and reuses the Month-0 OLS/normal-equation work).

Metrics: **MAE** on the median forecast, **mean pinball loss** across the nine quantiles, **empirical coverage** at the 50% / 80% / 95% nominal levels.

### 7.1 Diebold-Mariano tests (point and probabilistic)

*(Machinery carried verbatim from v5.5; comparator relabeled.)* Two DM tests are reported, both against the similar-day naïve:

**(a) Point accuracy — DM on median absolute error.** Loss differential on the daily median forecast.

**(b) Probabilistic skill — DM on the mean pinball-loss differential.** Per hour `h` and day `t`, per quantile `τ`, pinball loss `L_τ(t,h) = max(τ·(y−q̂_τ), (τ−1)·(y−q̂_τ))`; day-level model loss `L_model(t) = (1/24)Σ_h (1/9)Σ_τ L_τ(t,h)`; differential `d_t = L_model(t) − L_naive(t)`. One-sided DM `= mean(d)/sqrt(LRV(d)/N)`, **LRV via Newey–West** with lag truncation `⌊N^{1/3}⌋`; reject (model better) if DM < −1.645. Use the **multivariate, pooled-across-hours** daily-vector form as the headline; report per-hour DM only as a diagnostic. Averaging pinball across the nine levels is a discrete approximation to CRPS; explicit CRPS is optional. (On the two DST days and post-transition days the daily average runs over the actual local hour count; the UTC index makes this mechanical.)

**Development evidence versus confirmatory evidence (v6.4; AMD-1).** Because the five pinned folds are also used for catalog selection and model development, their Diebold–Mariano p-values are explicitly labeled **descriptive post-selection evidence**, not a clean confirmatory test. They retain the existing internal M2 development rule: Fold 5 is the primary development check; **if Fold-5 probabilistic-DM `p ∈ [0.05, 0.15]` but the pooled-across-folds daily-vector DM is significant at `p < 0.05`, the development check passes and both numbers are reported.** This rule controls M2 advancement but supports no claim of confirmatory superiority.

The sole confirmatory-superiority test is a **one-shot forward audit** on delivery dates strictly after the frozen CP-1 `selection_cutoff`, using the final post-isotonic champion against the similar-day naïve. It uses the same one-sided daily-vector probabilistic DM defined above, Newey–West LRV, `α=0.05`, and no per-hour multiplicity claim.

**Power and duration contract.** Before any audit outcome is opened, define the minimum practically relevant effect as `δ* = 10%` of the similar-day naïve's mean daily pinball loss on development Folds 4–5. Estimate `LRV_ref` conservatively as the larger of the Fold-4 and Fold-5 Newey–West long-run variances from the frozen final pipeline, and pre-compute `N_80 = ceil(((z_0.95 + z_0.80)^2 × LRV_ref) / δ*^2)`. The fixed audit size is `N_required = max(90 complete delivery days, N_80)` and must span at least twelve complete calendar weeks. A complete delivery day may contain 23, 24, or 25 local hours under §4.0, but must contain the complete target and all required champion-runtime inputs. Eligibility is determined from completeness only, before losses are inspected. The audit uses the first `N_required` eligible delivery days after the cutoff.

If fewer than `N_required` eligible days exist at a checkpoint, the result is **PENDING_UNDERPOWERED**: outcomes remain sealed, no inferential p-value is used as a gate, and the checkpoint packet reports only `N_available` and `N_required`. Once the threshold is met, the audit is opened and run exactly once. `CONFIRMED` requires `mean(d)<0` and one-sided `p<0.05`; otherwise the result is `NOT_CONFIRMED`. No optional stopping, repeated testing, window extension after inspecting outcomes, or tuning against the audit is permitted. `PENDING_UNDERPOWERED` and `NOT_CONFIRMED` forbid a confirmatory-superiority claim but do not convert honest negative evidence into a fabricated checkpoint failure.

The probabilistic DM (b) is the one aligned with the project's thesis — it tests whether the *intervals* beat persistence, not just the median.

### 7.2 Post-gate information benchmark (v6.3)

A controlled ablation that **quantifies the value of post-gate information without ever shipping it.** It runs **once, at M3/CP-3**, on the **strict-gate catalog frozen at CP-2** (§4.1):

- **Two variants only.** *Strict* = the frozen CP-2 champion runtime catalog (no delivery-day A69). *A69-augmented* = the same catalog **plus delivery-day A69 and its derivatives** (the A69-based residual load, `dunkelflaute_flag`, and any A69-based standardizations) — and nothing else different. Both retain the same proper-training-only preprocessing already present in the strict arm.
- **Everything else identical.** Same evaluation rows, same five pinned folds, same base features, same preprocessing, same seeds, same hyperparameter search space and tuning budget, same calibration windows. **Each variant is calibrated separately** through the identical CQR-then-isotonic protocol on its own calibration slice — so the comparison isolates the A69 information, not a calibration artifact.
- **Reported metrics (five, by name):** **MAE** (median forecast); **mean pinball loss** (nine quantiles); **empirical coverage** (50/80/95); **mean interval width** (50/80/95); and **calibration error** — defined as the **mean absolute nominal-minus-empirical coverage gap across the 50/80/95 levels** on the pooled eval (report both per-level gaps and their mean).
- **It is a benchmark, never the champion.** The A69-augmented model is not registered, not deployed, and cannot be promoted; A69 is never reclassified KFT. The result is a single honest reading in the report — *"post-gate VRE information is worth X % pinball loss / Y pp coverage"* — that makes the strict-gate cost explicit.

The benchmark is the audit-driven upgrade of the old as-archived posture: instead of quietly using post-gate data and disclosing it, v6.3 ships the feasible model and *measures* what feasibility costs.

## 8. Diagnostics and Explainability

*(Structure carried verbatim from v5.5; features and strata relabeled.)* These diagnostics are the heart of the portfolio artifact.

### 8.1 SHAP on the median model

- Global SHAP summary plot for the `p50` head **of the champion** on the validation tail; top-10 ranking; two dependence plots drawn from the **retained** champion features (**v6.3** — e.g. `gate_feasible_residual_load_proxy` × hour-of-day, and the retained headline proxy × a calendar/regime feature). The plots use only features actually retained at CP-2; if no domain proxy is retained, they fall back to the strongest retained features. The A69-based `dunkelflaute_flag` appears only in the §7.2 benchmark's SHAP, never the champion's.
- **Scoping (state explicitly):** SHAP on the p50 head explains the **central tendency**, not interval *width*. Interval width is driven by the inter-quantile spread and the CQR shift `Q`. If asked "what drives your uncertainty," the answer is the spread + `Q`, not the p50 SHAP. **v6.3:** SHAP *explains* the retained champion and may *name* a headline among already-retained features — it is **not** the incremental-value test (that is the M2/CP-2 matched ablation, §4.1). Trebbien et al. (2023, *Energy & AI*) — SHAP on a gradient-boosted model of German day-ahead prices — is the published methodological twin to cite.

### 8.2 Permutation importance

Permutation importance on the validation tail via `sklearn.inspection.permutation_importance`. SHAP attributes predictions in expectation; permutation importance measures degradation under shuffling. Reporting both signals maturity and surfaces any SHAP-vs-permutation rank disagreement.

### 8.3 Regime-stratified error analysis

A table of MAE and mean pinball loss on subsets: all validation; **Dunkelflaute days** (**v6.3: a post-hoc evaluation stratum** — using A69-based scarcity to *stratify errors* is legitimate because it never feeds the champion); non-flag days; **crisis (2021-09-01 → 2022-12-31); pre-crisis; post-crisis (≥2023-01-01); negative-price hours** (the DE-specific stratum); weekday; weekend. **`n_obs` is reported prominently for every row, and thin subsets (notably Dunkelflaute days and crisis-fold rows) carry bootstrap confidence intervals** (the Month-1 bootstrap) and are read qualitatively. All three regimes are populated from eval folds (Section 5.1); finer stress rows (the Dec-2024 Dunkelflaute week, the Aug-2022 peak weeks) come from training-period backtesting. If MAE on Dunkelflaute days or coverage on negative-price hours is materially worse, the report says so and ties it to the exchangeability paragraph (Section 6.2).

### 8.4 Reliability diagram

Empirical vs. nominal coverage at 50% / 80% / 95% on the validation tail, plotted at **all three stages (v6.3): the raw LightGBM heads; the post-CQR / pre-isotonic output (the stage carrying the formal marginal guarantee, §6.2); and the final post-isotonic intervals (empirical)**. This quantifies **whether, where, and by how much** isotonic-last changes coverage; no direction is assumed before measurement. It also makes the §6.2 three-stage claim visible. **Bounded-target tail caveat (two-sided in this market):** the DA price is bounded at **+4,000** and **−500 EUR/MWh** (−600 from 2026-05-28). The upper bound is almost never approached at DE-LU hourly resolution — documented as negligible, exactly as in v5.5. The **lower bound is live**: the floor was touched in 2023, and on extreme-solar days the p2.5/p5 conformity residuals can be truncated, so the lowest intervals can under-cover conditionally near the floor. This is documented as a known tail caveat, surfaced by the negative-price stratum in the stratified table, and deliberately not engineered around (a floor-aware tail would reopen scope).

## 9. Engineering Infrastructure

*(Carried from v5.5 with three renames and one resolved risk; the stack is market-agnostic.)*

### 9.1 Cloud-Backed Experiment Tracking via DagsHub-Hosted MLflow

MLflow runs against a remote tracking URI pointing to a public DagsHub repository. Runs log hyperparameters, per-fold metrics (MAE, pinball loss across all nine quantiles, both DM statistics vs. the similar-day naïve), and artifacts (SHAP summary, permutation tables, reliability diagrams, the regime-stratified table). The champion — the single `mlflow.pyfunc` (Section 6.1) wrapping the nine heads, the preprocessing and proper-training parameters required by the **frozen winning catalog** (**shared VRE-expectation coefficients if either proxy arm is retained**, plus its proper-training standardizations), CQR thresholds, and the isotonic step — is registered and tagged (`Staging`, `Production`). The DagsHub repo is publicly viewable; README, CV, and LinkedIn link the MLflow UI so hiring managers can inspect the decision trail without cloning. **Data versioning is intentionally lightweight** — a single committed Parquet snapshot at a tagged commit; DVC was evaluated and rejected; pinned deps + tagged commit + committed snapshot give equivalent reproducibility with less operational risk.

**Experiment lineage (v6.4; AMD-3 — replaces the run-count bar).** MLflow quality is judged by a complete, reproducible decision trail, not an artificial minimum number of runs. Every decision-bearing run or grouped search records: a pre-run `hypothesis_id` and hypothesis statement; `parent_run_id` or named baseline; intended change from the parent; dataset snapshot hash and training cutoff; forward-audit exclusion-manifest hash; fold-spec and feature-catalog identifiers; code commit; seed; fixed search/tuning budget; metrics and artifact links; and a terminal `keep`, `reject`, or `invalid` decision with rationale and limitations. Every DM artifact also records `evidence_class = development_post_selection | forward_confirmatory`.

All four matched catalog experiments, the selected CP-2 model, the M3 calibration run, and the registered champion are connected through this lineage. A generated public lineage index makes the champion traceable to one exact snapshot, catalog, fold definition, code commit, preprocessing lineage, and calibration artifact. Runs are added only when a real hypothesis or necessary search requires them; there is no minimum-count target and no cosmetic run generation.

### 9.2 Containerized Marimo Showcase on Hugging Face Spaces + Static First-Touch Page

The presentation layer is a reactive marimo notebook **run in server mode** (`marimo run` inside the container — not WASM), containerized via a multi-stage Dockerfile (base layer with locked dependencies; application layer with notebook code, the data snapshot, the precomputed slider lookup, and the bundled champion model) and deployed to Hugging Face Spaces free tier (Docker SDK; `cpu-basic` = 16 GB RAM, 2 vCPU).

On container startup the app: (1) **loads the champion — registry-first, bundle-fallback** (`models:/delu_forecast/Production` from the DagsHub-hosted MLflow Registry, token via HF Spaces Secret; on *any* failure it falls back to the champion bundled in the image — the demo never hard-depends on a live registry fetch); (2) loads the bundled weekly data snapshot (`data/snapshot.parquet`, refreshed manually each Monday during the application phase — no live calls to ENTSO-E/SMARD during user sessions); (3) renders the next-day (UTC-derived local-hour) forecast with a quantile fan chart, applying the model's calibrated, monotone output.

**Interactive sliders (v6.3 — drawn from the retained champion features, set at CP-4, not pre-ratified):** the **quantile-level selector** (50/80/95, annotating the reliability diagram's empirical coverage for that level) is fixed and model-agnostic; the remaining one or two controls are the **champion's retained domain proxies** (e.g. a `gate_feasible_residual_load_proxy` perturbation; an `expected_scarcity_proxy` control **only if its final construction is genuinely binary**, otherwise a continuous slider). If no domain proxy is retained at CP-2, a different meaningful champion control is used or the control count is reduced — **the UI never justifies retaining a weak feature.** No A69-based `dunkelflaute_flag` toggle, since it is not a champion feature (§5.2).

**Slider updates are served from a precomputed lookup, not live compute (verbatim pattern).** At build time the forecast curve and SHAP attributions are precomputed over the discretized grid (**the retained champion controls**, ~20–50 points each, × 3 quantile levels; v6.3) and stored as Parquet in the image; on interaction the app indexes into the lookup — instant, zero live compute, guaranteed on the free tier. **The slider is a *ceteris-paribus sensitivity probe* with a dynamic out-of-distribution indicator:** the lookup carries a **per-cell in/out-of-distribution flag** computed at build time (distance to nearest real training observation in the perturbed features); when the user lands on a flagged cell the UI lights a banner ("out-of-distribution combination — model is extrapolating"). Zero live compute; demonstrates the limitation is engineered around, not merely known. If a residual-load control is retained, perturbing it while calendar and lagged prices stay fixed is physically OOD on parts of the grid — the flag makes that visible. CPU Upgrade ($0.03/hr ≈ $22/month) is reserved for cold-start mitigation only.

**Static first-touch page (added at v6.2 — the primary portfolio URL).** The URL a recruiter lands on from the CV, LinkedIn, or the README is a **statically exported HTML report of the full Section-10 reading order** — every figure, table, and paragraph rendered as plain static assets — hosted on **GitHub Pages** from the project repo: $0, CDN-served, no container, no runtime, no sleep state, ~1–2 s typical load, **gated < 3 s at CP-5**. Build path: `marimo export html` of the showcase content at M5 build time; **fallback** if any Section-10 element degrades in static export: a purpose-built static page assembled from the same committed figures (~2 h delta, same cost, same gates). The page performs **no runtime calls** to the Space or any external service, and links two things beneath the report: the **interactive Space**, labeled *"interactive demo — may take ~30 s to wake if asleep,"* and the **public MLflow UI**. The weekly Monday snapshot refresh (application phase) gains one step: **re-export and commit the static page alongside the snapshot**, so the static and interactive surfaces never drift.

**Sleep and first-touch (rewritten at v6.2).** HF free tier sleeps after 48h of inactivity (~20–30s cold start). The GitHub-Actions cron-ping option is **deleted: no keep-alive of any kind, on any platform.** The first-touch guarantee is carried by the static page above — the primary URL cannot sleep — and the Space's cold start is paid only by a viewer who explicitly follows the labeled interactive-demo link, with the wait announced in advance; the Space keeps its CP-5 posture (warm < 5 s, cold ≤ 30 s). The weekly Monday commit keeps the repo active as a side effect, but nothing depends on it. A standalone CLI inference script (**`predict_next_day.py`**) ships inside the container for cold-start regeneration and local dev; it is not the user-facing artifact.

### 9.3 Reproducibility

Fixed seeds (NumPy, scikit-learn, LightGBM); `pyproject.toml` pinned via `uv` (including **`entsoe-py` pinned at 0.8.0** — satisfies the ≥0.7.5 15-min-MTU-aware line; the spike's `uv.lock` is the baseline); a single Parquet snapshot committed at a tagged commit (reviewers run `make train` after checking out the tag, no extra fetch); the champion `pyfunc` bundled in the image; every contributing MLflow run logged to DagsHub with a permalink and complete §9.1 lineage; compute footprint stated.

**Attribution statement (replaces the v5.5 redistribution check — the risk is resolved, not mitigated):** the committed snapshot is **legally redistributable under CC BY 4.0**. The README and the data folder carry the attribution line: *"Data: ENTSO-E Transparency Platform; Bundesnetzagentur | SMARD.de — CC BY 4.0."* CP-1 carries the checklist item.

**Model-staleness disclosure (carried verbatim):** the reproducibility statement, the honest-limitations section, and the README each state the **training-data cutoff** (the M1 pull date) and that the deployed demo applies a **frozen model** to later weekly snapshots it never trained on. One sentence each; honesty, not apology. A second one-liner notes the **floor change to −600 EUR/MWh from 2026-05-28** as a known environment shift the frozen model predates.

### 9.4 Targeted invariant tests

A small set of **five mandatory property tests** asserts the invariants that must hold — deliberately **not** a regression matrix (Section 13). The first four carry forward; the fifth is the **v6.3 champion/benchmark schema separation**:

1. **Monotonicity after CQR + isotonic.** Assert the nine post-pipeline quantiles are non-decreasing per row — zero crossing violations, including on rows with negative and zero-crossing intervals. Guards the post-processing-order bug (Section 6.2).
2. **Rolling-window leakage.** Assert every rolling/lag feature (including the negative-hour count) is closed-left with a strict `t−1` right edge — construct a row whose target value would change a rolling feature and assert the feature does not move. Guards the §5.2 leakage vector.
3. **UTC/DST lag alignment.** On the spring-forward (23-local-hour) and fall-back (25-local-hour) days, assert that a 24h/168h lag in UTC retrieves the correct row and that no hour is dropped or duplicated. Guards the §4.0 integer-lag corruption.
4. **15-minute aggregation integrity (extended at v6.1).** Assert the hourly price series is continuous across 2025-10-01 with no gap or duplicate; that every post-transition hourly price equals the mean of its four quarter-hours; **and that every hourly value aggregated from any PT15M feed (price post-transition; load/VRE feeds across the whole archive) is built from exactly four quarter-hours — no partial bins at chunk or pull-window edges** (the spike-surfaced boundary artifact). Guards the §0-item-4 averaging path and the §4.0 chunk-boundary property.

5. **Champion/benchmark schema and fit-lineage separation (mandatory, v6.3).** Assert that (a) the champion's `predict` runtime schema rejects **delivery-day A69, every feature computed from it, and all actual-value columns** (A65/A16, A74, A75); (b) the shared VRE climatology, if either proxy arm is present, was fit only on aggregate A75 VRE from the fold's proper-training partition — never calibration/eval; and (c) the **§7.2 benchmark runtime schema differs from the frozen strict-gate runtime catalog only by delivery-day A69 and its derivatives.** The frozen coefficients are permitted preprocessing artifacts, not runtime data columns. This subsumes the old forecast-not-actual guard and makes both the strict-gate boundary and the train-only lineage tested invariants.

These tests run in seconds and live in the repo as proof that correctness was engineered, not assumed. The separate §6.2 CQR order-statistic fixture is a mandatory M3 acceptance fixture and Critic surface; it does not change the count of five property tests above.

**Mandatory independent M1 acceptance-oracle pack (v6.4 hardening).** The M1 Critic must materialize the following five inputs outside the candidate checkout from this frozen specification, independently compute the expected output before inspecting pipeline output, and record fixture SHA-256, exact commands, observed result, and disposition. Builder-authored tests alone cannot satisfy these oracles:

1. **M1-O1 — misaligned chunk stitch.** Two adjacent PT15M chunks split at `00:15` jointly contain `{00:00,00:15,00:30,00:45}` with distinct known values. After stitch-before-aggregate (or an equivalent pad/trim implementation), exactly one `00:00` hourly value equals the arithmetic mean of all four quarters. An isolated three-quarter fragment is never averaged.
2. **M1-O2 — missing quarter.** An otherwise valid PT15M hour missing one quarter emits no hourly value and an explicit incomplete/recovery result; it never emits a three-quarter mean.
3. **M1-O3 — fall-back DST.** On `2025-10-26 Europe/Berlin`, both local `02:00` rows (`+02:00` and `+01:00`) survive as distinct UTC instants and the local day contains 25 hourly rows. A true duplicate UTC instant is rejected.
4. **M1-O4 — A75 fit-lineage poisoning.** Replacing A75 only in calibration/evaluation leaves the proper-training climatology coefficients and downstream predictions unchanged, and the fit manifest contains proper-training rows only. A separate proper-training poison control must change the fit, proving the oracle exercised the real path.
5. **M1-O5 — runtime-schema poisoning.** A valid strict champion input succeeds; injecting delivery-day A69, an A69-derived field, or any actual-value column fails closed before prediction. The benchmark schema differs from the champion only by approved A69 fields/derivatives and still rejects actuals.

This oracle pack serves the temporal-normalization, champion/benchmark-firewall, and A75-lineage Critic surfaces. It is independent of—and does not replace or change the count of—the five repository property tests above.

**Thin CI (lands at M2; CQR fixture joins at M3):** a minimal GitHub Actions workflow runs the five invariant tests **on every push** and, from M3, also runs the deterministic §6.2 CQR fixture — a visible green check on the public repo, at $0. Deliberately not a regression matrix: no test pyramid, no scheduled retraining, no drift gates, no rollback machinery (Section 13).

### 9.5 Offline batch data/output-health report (v6.3)

A single **offline batch** report — **not** production monitoring, and deliberately **not** a model-performance/drift claim (no realized outcomes or labels exist at generation time, so it cannot speak to accuracy drift). Generated at **M4/CP-4**; its static rendering is verified at **CP-5**; it lives on the static Pages showcase as one more table + small figures.

- **Reference data, comparison window, as-of boundary, completeness (v6.3, pinned).** *Reference data* = the frozen M1 training rows (never recomputed). *Comparison window* = the **most recent complete 28-day span ending at the as-of boundary**. *As-of boundary* = the latest hour for which every required feed is fully populated; partial trailing hours are excluded. A feature-level comparison computes only if the 28-day window is **≥90% non-null**, else it emits **"insufficient data" (amber)**, never a spurious green.
- **Feature-aware distribution reference.** Deterministic calendar/regime columns are schema-checked but excluded from PSI. For continuous retained features, the frozen training reference is reweighted to the comparison window's joint **calendar-month × hour-of-week** mix; bins use deduplicated training-quantile edges (up to deciles). After binning, both continuous distributions receive **ε = 1e-6 in every bin and are renormalized** before `(p−q)·log(p/q)`, so zero-mass current or reference bins remain finite. Binary/categorical features and numeric features with **≤10 distinct training values** use category-frequency PSI instead: take the union of reference/current categories, apply the same ε smoothing, and renormalize. A nominally continuous feature with fewer than three usable bins falls back to category-frequency PSI **only if it has ≤10 distinct training values; otherwise it reports `insufficient data` (amber)**. This prevents expected seasonality, zero cells, duplicate bin edges, or an after-results fallback choice from masquerading as shift.
- **Checks (five):** (1) **freshness** — age of the latest row per feed vs. its documented expected-lag threshold; (2) **schema conformance** — exact column set, dtypes, UTC index continuity, and the 15-min full-bin invariant; (3) **selected-feature distribution shift** — feature-aware PSI on the **champion's actually-retained non-deterministic features** against the matched frozen reference above; (4) **interval width** — the deployed forecast's mean 50/80/95 width vs. the corresponding season/hour-matched training norm; (5) **quantile sanity** — zero crossings / monotonicity on the emitted forecast.
- **Outputs & acceptance.** A committed static table + small figures. PSI uses the pre-registered descriptive bands **<0.10 green, 0.10–0.25 amber, >0.25 red**; missing/ineligible comparisons are amber, never green. Freshness and schema thresholds are the feed contract; quantile crossings must equal zero; interval-width bands are frozen from the matched training reference before the current window is inspected. Acceptance = the report **generates and renders statically**, every check returns a value or explicit insufficient-data status, and all thresholds/reference metadata are rendered. It raises no alarms, schedules nothing, calls no service.
- **Hard cap:** ≤ ~8 h inside M4 (planning figure). **No new services, no live dashboard, no scheduled retraining, no AWS dependency** — a batch script emitting static assets, run by hand alongside the Monday refresh. *(If the parked DEC-AWS is ever ratified, this offline report is the baseline the AWS backbone operationalizes inside its manually-triggered pipeline — not net-new monitoring; see `aws-extension-spec_v1_1.md`.)*

### 9.6 DuckDB SQL feature + data-quality mart (v6.3)

The feature and data-quality mart is authored in **SQL against DuckDB** (which reads the committed Parquet natively) — the **real database of the syllabus SQL supplementary block**, not a throwaway toy table. Two integration points:

- **SQL-A (production subset, integrated at M1, before CP-1):** the minimal set of hand-authored window/data-quality queries that build the mart the pipeline consumes — the lag-24h/168h and rolling-statistic features expressed as SQL window functions (`LAG`, `OVER (… ROWS …)`), plus data-quality checks (null scans, the 15-min full-bin count, duplicate-timestamp scans). These are integrated and **tested** — the §9.4 leakage and aggregation invariants apply equally to the SQL-built columns — and become the **canonical feature source** the model consumes.
- **SQL-B (interview-fluency, through G3):** the remaining practice queries live in the SQL block, not the capstone.

This makes the SQL block a **second deliberate exception** to the supplementary-block firewall (the first being spectral EDA, §4.2): its production subset enters the capstone by design. The block's 20-hour cap is preserved and split SQL-A / SQL-B. No new service — DuckDB is an in-process library, $0, local.

## 10. Artifact and Reading Order

The artifact is the **deployed marimo showcase on Hugging Face Spaces** (Section 9.2). marimo is mandated because the reactive sliders are the centerpiece. At v6.2, the **canonical entry point is the static GitHub Pages export of this reading order** (Section 9.2); the deployed Space remains the interactive artifact it fronts.

Reading order: (1) data story; (2) compressed regime context — the 2021–23 crisis arc and the negative-price era, with the ~€30 → ~€235 → ~€89 annual-average sweep and the 139→69→301→457→573 negative-hour series; (3) feature catalog — gate-feasible by construction (v6.3), with the retained domain proxy highlighted if one survives selection; (3b) **"Why these seasonal features? (spectral view)"** — the labeled Welch periodogram (or the per-regime overlay) with a one-paragraph interpretation: the 24h/168h/12h price peaks justify calendar structure and motivate—but do not validate—the seasonal residual candidate; the per-regime spectral change supports reporting performance across all three regimes rather than collapsing them. One static figure + one paragraph; **no spectral slider** (a live spectral control would add compute and code surface for zero interview payoff — the §9.2 slider set is unchanged); (4) CV methodology including the 24h embargo, the D+1 target rationale anchored at the 12:00 CET gate, the §3 point-in-time ledger, and the KFT/LAG leakage audit with the **strict-gate design stated plainly (A69 excluded from the champion; §5.2)**; (5) results table — LightGBM vs. similar-day naïve, naïve-168h, Ridge, with the five-fold DM results labeled **descriptive post-selection evidence**, the forward-audit status shown as `CONFIRMED`, `NOT_CONFIRMED`, or `PENDING_UNDERPOWERED`, and **the §7.2 post-gate information benchmark (strict vs. A69-augmented, five metrics)**; (6) SHAP summary + top-10 + two dependence plots; (7) permutation importance; (8) regime-stratified error table (n_obs + bootstrap CIs on thin subsets, negative-price stratum included); (9) reliability diagram — **three stages (raw / post-CQR / final)**, all three levels; (10) representative next-day forecast with a quantile fan chart and the **retained-feature sliders (§9.2)**; (11) honest limitations — exchangeability under regime shift, the development/confirmatory evidence distinction, **the strict-gate design (A69 excluded from the champion) and the §7.2 measured cost of that feasibility**, the two-sided bounded-target tail (live at the floor), any coverage divergence, model staleness, and the 15-min-MTU averaging choice; (11b) **the offline data/output-health snapshot (§9.5)**; (12) reproducibility statement — tagged commit, complete §9.1 experiment-lineage index and DagsHub MLflow permalinks, **the static GitHub Pages URL as the canonical entry point (v6.2)** with the deployed HF Spaces URL linked from it, the **DuckDB SQL feature/data-quality mart (§9.6)**, training-data cutoff, CC BY attribution.

### 10.1 Stranger-test gate

Passes when one technical and one non-technical reviewer can each articulate the data, method, and result in under 20 minutes, **starting at the static GitHub Pages URL (v6.2)** and following the labeled link to the deployed Space for the interactive portion. A **< 3 s cold-cache load check on the static page from an independent connection** runs alongside the test; the Space-side load checks stay in CP-5. Reviewer recruitment is a Track C-Manual block before M5.

## 11. Risk Register

### R-1 — Three-regime fragility
**Description.** Pre-crisis, crisis, and post-crisis price distributions are structurally distinct, with two opposing stress directions (high-price scarcity vs. negative-price abundance); training spans all three but the forecast serves the post-crisis regime. **Mitigations:** three-regime walk-forward CV with both stress folds (Section 5.1); `crisis_period` and `post_crisis` as first-class champion features (**v6.3:** `dunkelflaute_flag` is a §8.3 evaluation stratum / §7.2 benchmark feature, not a champion feature); per-regime and negative-price-stratum error analysis (Section 8.3); if coverage diverges on regime-shift folds (reliability diagram), the report names it as a regime-shift failure rather than glossing it. R-1 is the project's best interview narrative.

### R-2 — VRE-forecast vintage (the KFT trap) — resolved by strict-gate design at v6.3
**Description.** The day-ahead wind/solar forecast's regulatory publication deadline (18:00 D-1) post-dates the 12:00 CET gate, and **the spike observed directly (2026-06-12) that initial TP publication itself is post-gate** (A69/A01 absent at a 15:35 CEST D-1 pull, present after the 18:00 deadline window). The same spike observed A65/A01 at 15:35 CEST D-1, consistent with its regulatory deadline but not itself a pre-gate empirical capture; M1 closes that evidence gap under the §3 point-in-time contract. **Resolution (v6.3): the risk is eliminated for the champion runtime path by construction** — delivery-day A69 is not KFT, so it and its derivatives are **excluded from the champion runtime schema** (§4, §5.2, §9.4); the champion uses gate-feasible runtime features only (the proxies where the M2/CP-2 ablation retains them, else the strict-gate base), and the value of the post-gate information is *measured* by the §7.2 benchmark rather than quietly consumed. The proxy has one pre-registered historical-VRE lineage: aggregate A75 is used only as a proper-training fit target for frozen calendar-driven expectation coefficients (§4.1), never as a runtime input. **Residual concern, benchmark-only:** the archive-overwrite question is tracked by the committed snapshot/compare harness (`scripts/q2_vintage_probe.py`; first snapshot 2026-06-12 for delivery 2026-06-13); revision metadata is a confirmed dead end (constant `revisionNumber`; `createdDateTime` = pull wall-clock). A lagged-vintage proxy is not part of v6.3. R-2 shifts from *"a caveat we disclose"* to *"a runtime design boundary plus fit-lineage rule we enforce and test."*

### R-3 — Platform migration, API churn, and data quality
**Description.** Three concrete risks: (i) the ENTSO-E Transparency Platform was migrating through 2025–26 with community-reported endpoint timeouts, 403/429s, and response-format changes; (ii) known TP data-quality issues (missing TSO submissions, aggregation inconsistencies across Germany's four TSOs — 50Hertz, Amprion, TenneT DE, TransnetBW); (iii) token/access friction. **Spike observation (2026-06-12):** a clean session — no 403/429/migration symptoms; monitored, not closed. **Mitigations:** `entsoe-py` pinned (0.8.0) and upgraded deliberately; the **SFTP bulk path** as a second ingestion route (port-22 reachability untested from the spike sandbox — verify from the M3 only if needed; the chunked API is assessed sufficient for the M1 bulk size); **SMARD as cross-check and fallback-primary** (same Reg-543 data, CC BY, no auth) — the CP-1 reconciliation gate (ENTSO-E↔SMARD within €0.01/MWh on >99.9% of overlapping hours; **96/96 sample hours already within €0.01 at the spike**) converts the cross-check into a verified property.

### R-4 — 15-minute MTU transition
**Description.** The SDAC resolution change at delivery 2025-10-01 splits the target series into 24-value and 96-value eras; mishandled, it corrupts every lag and rolling feature after the boundary (the same failure class as DST, occurring once but permanently). **Spike findings (2026-06-12):** the boundary averaging path validated on samples (0 missing/0 duplicated hours; hourly = mean of 4 quarter-hours to 1e-9), **and** the feature feeds are PT15M across the whole archive — the aggregation discipline applies from day one, with the chunk-boundary partial-bin artifact as the live failure mode. **Mitigations:** the §0-item-4 averaging rule; §4.0's chunk-boundary bin-integrity requirement; `entsoe-py` 0.8.0 (date-switched resolution handling); the extended boundary invariant test (Section 9.4 item 4); Fold 5 deliberately evaluates entirely post-transition so the headline metric is measured on the series the product actually serves.

### R-5 — Spectral-EDA scope creep — added at v6.2
**Description.** The §4.2 frequency-domain work expands into filter design, wavelets/EMD, or frequency-domain forecasting, consuming capstone time for no hiring payoff. **Mitigations:** the block is capped at the four §4.2 artifacts and the 6–10h budget; wavelet/EMD/frequency-domain-forecasting work is explicitly out of scope (§4.2 guardrails); any new dependency beyond scipy/statsmodels is a scope violation by definition. **Trigger to escalate:** >10h elapsed or any proposed dependency beyond the named set — stop, ship the mandatory three figures as-is, and carry the residual to the orchestrator. **Owner:** learner.

## 12. Milestones and Checkpoints

**Build-routing note (orchestrator-facing).** No milestone is fully briefed inside this document. Each is briefed by the orchestrator at the session its work begins, as a Type B-Claude block to the engineering lead. The brief **cites the complete checklist of the named CP/FCP as controlling**, cites the supporting architecture sections needed for that block, and includes only the task-specific objective and constraints; a supporting extract cannot narrow the checklist or create a divergent rewrite of the plan. **The Month-0 de-risking spike is DONE (2026-06-12** — `docs/spike-feed-status.md`, branch `claude/modest-bardeen-96zee0` @ `20fc1ff`); **the next brief due is the **M0.5/CP-0 re-run** under the v6.6 contract, which is that amendment's acceptance test (`docs/track-b/gauntlet-amendment-plan.md` Phases 4–6); the M1 brief follows only after CP-0 closes, B-Man-PIT completes, and the ledger closes. The M1 brief must carry the v6.1 decisions binding M1: the **24h D+1 hourly target at the 12:00 CET gate** (§1, §0 item 4); **UTC indexing + DST + 15-min handling with uniform PT15M→hourly feature aggregation and chunk-boundary bin integrity** (§4.0); the **feed identifiers** (A44 / A65-A01 / A69 on `10Y1001A1001A82H`) with A75 allowed only as the proper-training VRE-climatology fit target and all other actual use diagnostic-only (§3, §4.1) and the **A65/A01 ~25h head-gap handling**; the **2019-01-01 → pull-date window, with the endpoint and Fold 5's dates pinned at the pull** (§3, §5.1); the candidate arms' **single pinned proper-training-only A75 VRE expectation**, `gate_feasible_residual_load_proxy`, and `expected_scarcity_proxy`, with every fit parameter refit per fold (§4.1; **v6.3 strict-gate**); the **pinned three-regime fold scheme** (§5.1); the **§0 ratified decisions (gas reopen closed)**; the **R-2 strict-gate resolution** (delivery-day A69 excluded from champion runtime; A75 restricted to proper-training fit lineage; the snapshot/compare rerun bears only on the §7.2 benchmark) (§5.2, §11); the **CC BY attribution statement** (§9.3); the **invariant tests incl. the extended full-bin clause** (§9.4); and **verification of actual repo/branch state** (the spike's work may still sit on its branch — confirm merge state before building); and the **§4.2 spectral-EDA extension** (module, three mandatory figures, acceptance criteria, and the R-5 guardrails) — an internal Lead-chosen piece of the single M1/CP-1 Gauntlet, closing **before CP-1 closes**; it does not create a second Orchestrator brief or mid-checkpoint return.

**v6.3 additions to the M1 brief (2026-07-22).** (a) **Strict-gate point-in-time availability proof** — establish, per feed, what is genuinely available before the 12:00 gate; delivery-day A69 is excluded from the champion runtime path, and the sole VRE substitute is the pinned proper-training-only A75 climatology with explicit fit lineage (§4.1, §5.2). (b) **Gate-feasible candidate construction** — the one-feature residual arm {`gate_feasible_residual_load_proxy`} and `expected_scarcity_proxy` as *candidates*, not a headline (§4.1); the headline is decided at M2/CP-2 by matched ablation. (c) **DuckDB SQL-A mart** — the minimal hand-authored SQL feature/data-quality subset integrated and tested as the canonical feature source (§9.6). (d) The **champion/benchmark schema-and-fit-lineage invariant** (§9.4 item 5) replaces the forecast-not-actual guard.

**v6.4 additions to every Track B checkpoint brief (2026-08-02).** Every checkpoint brief names exactly one target repo and one M/CP, supplies the exact ratified plan anchor, cites the **complete named CP/FCP checklist** as controlling, and states one **numeric total checkpoint active-elapsed wall-clock ceiling** derived by the Orchestrator from the program load math. The brief states the goal and the bar — never decomposition, file layout, agent count, workstream budgets, or a fixed number of review rounds. A brief missing **any** required field, contradicting the named plan, or naming more than one repository or checkpoint is invalid and returns terminal `BRIEF_INVALID` before any repository edit; the complete field list is `engineering-role.md` § *Required brief fields*. The full required-field list, the clock-accounting rules, and the brief form are in `engineering-role.md` and `docs/track-b/gauntlet-templates.md` §1.

### Checkpoint execution contract — bounded Gauntlet (v6.4)

**Process authority — stated once, elsewhere.** How a checkpoint *executes* — decomposition, Builder/Critic isolation, verdict records, staleness, evidence storage, clock accounting, `workbench.md`, and the Return Packet form — is defined once in the repo-root `engineering-role.md` and is deliberately **not** restated here. This section states only what must be **true** for a checkpoint to close. Where the two touch, `engineering-role.md` owns the mechanism and this section owns the bar; on any conflict about a bar, this document wins.

**Authority split.** The Orchestrator alone decides **when** Track B runs, which single checkpoint is authorized, and the numeric active-elapsed wall-clock ceiling. The Engineering Lead decides **how**. Yarden carries one brief into Track B and one terminal packet back; he never routes Builder/Critic messages. A brief cannot reduce a checkpoint bar: that requires an owner-ratified capstone/checkpoint amendment, a new exact plan anchor, and then a replacement brief.

**No self-certification.** Neither a Builder nor the Lead may certify its own work. Every checkpoint item closes only on a separate fresh-context Critic's written verdict, and every verdict returns the single largest meaningful gap and the exact next acceptance test. A supporting extract cannot narrow, weaken, strengthen, or replace the complete named CP/FCP checklist.

**The five mandatory component-level critic surfaces**, when their subject matter is in scope: **(1) temporal normalization; (2) champion/benchmark schema firewall; (3) A75 climatology fit lineage; (4) the CP-2 label-blind four-catalog metric-only recomputation (procedural blinding — `engineering-role.md`); (5) M3 CQR threshold recomputation on the §6.2 hand-checkable fixture and persisted calibration predictions.** At M1, surfaces 1–3 are not satisfied until a fresh Critic independently executes all five §9.4 acceptance oracles; that Critic — never the Builder — materializes and hashes every fixture outside the candidate checkout and computes the expected result independently, and repository property tests do not substitute for those verdicts. A Builder may not issue these verdicts for its own work. The Lead may add a further Critic only where the risk justifies its cost inside the supplied ceiling. No comparison is described as **blind** merely because labels were renamed.

**Scope is declared, never inferred.** The Return Packet states, for **each** of the five surfaces, whether it was in scope at this checkpoint and **why** — naming the verdict file if in scope, and the reason it does not apply if not. "In scope when their subject matter is in scope" is a judgement someone must make and record; a packet silent on a surface gives the receipt gate nothing to check, and an unstated judgement is indistinguishable from an unmade one.

**Terminal statuses.** A checkpoint returns exactly one of `PASS`, `BLOCKED`, `PLATEAU`, `BUDGET_EXHAUSTED`, `BRIEF_INVALID`.

- **`PASS`** requires every item in the complete named CP/FCP checklist, every applicable mandatory independent check, complete valid Critic records for every required review — none of them `BLOCKED` — a current fresh Integration-Critic `PASS` bound to `final_candidate_sha`, a **verdict-only delta** between `final_candidate_sha` and `evidence_tip_sha`, and a computed-current binding for every relied-on component `PASS` (mechanism: `engineering-role.md`).
- **`BLOCKED`** — an owner credential or action, new authority, ratified-methodology change, destructive or public action, a missing/contradictory/untestable acceptance bar, or a plan/reality resolution is required.
- **`PLATEAU`** — the next improvement is not worth its cost, or two material repair attempts produced no meaningful improvement.
- **`BUDGET_EXHAUSTED`** — the supplied active-elapsed raw-seconds ceiling is reached before `PASS`. Approaching the ceiling is a prioritization signal, **never** permission to cut or weaken a ratified criterion.
- **`BRIEF_INVALID`** — a **pre-work** status: the authorization was malformed, so no work started, no clock was consumed, and no repository file was edited. It names every missing or contradictory required field. It is the only status that returns no Return Packet, using instead the minimal form in `docs/track-b/gauntlet-templates.md` §10. Distinct from `BLOCKED`, which means work started and hit an owner-only dependency.

**Abandonment is not a status the Lead can return.** A run that exceeds its ceiling in real elapsed time and produces no packet at all — a stalled or dead executor — is **abandoned**, declared owner-side per `orchestrator-role.md`. It is never recorded as `BUDGET_EXHAUSTED`: that status asserts the ceiling was consumed by work, which an abandoned run cannot evidence. Every status above describes a run that finished; silence is not among them.

None of these weakens the bar or advances the checkpoint, and **no terminal status automatically opens the next one**. A non-`PASS` packet may mark Integration `NOT_RUN` only with the exact terminal reason that prevented it; it never implies a pass. At every terminal status Track B stops completely and does not inspect, research, plan, scaffold, or begin the next checkpoint.

**The sole upward artifact** is one Checkpoint Return Packet. Its criteria table maps **every** item in the complete named CP/FCP checklist — never a convenience extract — to direct, reproducible evidence, and it carries **3–5 defense questions** grounded in the actual architecture, tradeoffs, and evidence, so the delivered artifact is interview-defensible. Defense questions never alter engineering CP criteria. Its full form and provenance fields are in `docs/track-b/gauntlet-templates.md` §7.

#### CP-2 label-blind four-catalog review — the scientific bar

Blinding is **procedural**: the Lead withholds the label→catalog mapping from the Blind Critic and
reveals it only after that Critic's verdict is written; a revealed mapping is never reused. The
mechanics live in `engineering-role.md`; the Return Packet must label the run
`COOPERATIVE_PROCEDURAL` and may never describe it as cryptographically enforced. Two requirements
are scientific rather than procedural and therefore bind here:

1. **The Blind Critic recomputes and never adjudicates.** It produces identity-free metrics by
   anonymous label `A/B/C/D` under the exact §4.1 Decimal contract, and it **never** identifies the
   base, applies the eligibility or tie-break rules, selects a label, or asserts a winner. Selection
   is not a Critic judgement call; it is the §4.1 rule applied to metrics the Critic could not
   attribute.
2. **Adjudication happens only in fresh Integration, after reveal, and must match.** The fresh
   Integration Critic applies the real catalog identities and the exact §4.1 eligibility comparisons
   and deterministic tie-breaks, and the adjudicated real winner must equal the winner in the final
   candidate's committed selection declaration. A mismatch is `FAIL`. CP-2 cannot pass without both
   the Blind component `PASS` and a fresh Integration `PASS` at the same `final_candidate_sha`/tree.

**Gauntlet planning reserve (net addition, already reflected in the program envelope):** CP-0 2 h; CP-1 6 h; CP-2 5 h; CP-3 5 h; CP-4 3 h; CP-5 3 h — **24 h total** (22 h at v6.4 across CP-1–CP-5, plus the 2 h CP-0 reserve added at v6.5), matching the per-checkpoint reserve table in `progress.md`; the program-envelope total there is ≈753 h while the raised 48 h headroom stands (open owner question). These are planning-load estimates, not additive agent-hours or Lead-granted budgets. At issue time the Orchestrator combines the relevant reserve with the milestone's existing allocation and writes the resulting active-elapsed wall-clock ceiling into the brief. Only the Orchestrator may issue a replacement brief that extends it; the owner may authorize added program time to the Orchestrator, not directly to the Lead.

### M0 — Plan approved
This document (v6.6; v6.0 ratified 2026-06-11, spike-adjudicated 2026-06-12; v6.2 amendments merged 2026-07-12 — spectral extension + companion pointer, additive; the D7 static-page rider absorbed 2026-07-20; the **v6.3 strict-gate methodology amendment ratified 2026-07-22**; the **v6.4 Gauntlet/statistical-audit amendment ratified and authored 2026-08-02**; the **v6.5 capture-schedule feasibility amendment ratified 2026-08-04; and the v6.6 Gauntlet contract amendment ratified 2026-08-05, execution-contract only**).

### M0.5 — Point-in-time capture instrument

The §3 capture contract needs an instrument before it needs a schedule. This checkpoint authors and
independently reviews that instrument and nothing else. It is deliberately small: one bounded Builder
piece, one fresh component Critic, one fresh Integration Critic, and a **2 h active-elapsed ceiling**.
It is also the first checkpoint executed under the `engineering-role.md` Gauntlet contract, so its
Return Packet is the operational validation of that contract.

The instrument performs one capture attempt for A65/A01 on DE-LU (`10Y1001A1001A82H`) at a supplied
UTC instant and writes, to an append-only ledger: the immutable raw response, its SHA-256, `pulled_at`
in UTC, the resolved Europe/Berlin local time, the delivery day D+1 it covers, whether that D+1 vector
is complete under §4.0 DST rules, and an explicit `qualifying` / `not_qualifying` verdict with reason.

**CP-0**
- [ ] The instrument runs standalone, requires only `ENTSOE_API_TOKEN`, and writes an append-only ledger entry per attempt without mutating or overwriting any prior entry.
- [ ] A failed, partial, or rate-limited request records a `not_qualifying` entry with its reason and never counts toward the three qualifying days (§3). A positive control proves a complete response records `qualifying`.
- [ ] `pulled_at` is recorded in UTC and is the observed-available-by time; it is never rewritten as the source's first-publication time (§3).
- [ ] Berlin DST is handled per §4.0: a capture in either 02:00 offset on a fall-back day resolves to a distinct UTC instant, and D+1 completeness is judged on the correct 23/24/25-hour day length.
- [ ] Raw captures and ledger entries are hash-bound and immutable once written; a rerun cannot silently replace a prior attempt.
- [ ] **Independent Critic PASS (mandatory):** a fresh Critic materializes its own fixtures outside the candidate checkout — including a truncated response, a rate-limit response, and a Berlin fall-back day — computes expected outcomes independently, and confirms each. Builder-authored tests are insufficient.
- [ ] Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`).

**B-Man-PIT (owner-run, between CP-0 and CP-1).** With the reviewed instrument in hand, Yarden runs it
on at least three non-consecutive delivery days at approximately 10:30 and 11:45 Europe/Berlin on D-1,
per §3. This is a manual block: it needs no agent, no Engineering Lead, and no Gauntlet clock, because
it requires no engineering judgement — only that the qualifying days arrive. The Orchestrator supplies
the exact commands and the schedule, tracks the block as a CP-1 prerequisite, and does not open CP-1
until the ledger reports three qualifying days. A non-qualifying attempt is repeated on another day.

### M1 — Data layer
Bulk pull from ENTSO-E (DA prices, DA load forecast, DA VRE forecasts; A75 actual VRE for diagnostics **and the proper-training-only climatology fit target**) + SMARD cross-pull, 2019-01-01 → the pull date (window endpoint and Fold 5 dates pinned here), UTC-indexed, 15-min boundary and uniform feature aggregation handled. Feature catalog v1 includes the **gate-feasible domain-insight candidates** (§4.1), their single pinned A75-based VRE expectation, and the regime indicators (with the **strict-gate R-2 resolution** applied — delivery-day A69 excluded from champion runtime). The **DuckDB SQL-A production mart** is authored and integrated as the canonical feature source (§9.6). KFT/LAG classification is documented with the **champion/benchmark schema-and-fit-lineage invariant** and the three leakage-vector audits.

**Spectral EDA (v6.2, §4.2):** lands within M1's window, after ingestion (it consumes the pulled series) and before the CV-design work it partly justifies. Deliverables and acceptance per §4.2; showcase figure earmarked for M4 (§10 item 3b).

**CP-1**
- [ ] All primary feeds return non-empty results for the most recent 30 days; archive depths to 2019-01-01 confirmed for A44, A65/A01, A69, **and A75 wind-onshore / wind-offshore / solar** on DE-LU (`10Y1001A1001A82H`); SMARD mirrors reachable. A75 coverage and exclusions are logged because §4.1 now depends on it as a fit target. — **A44/A69 status and A65 archive reachability were pre-confirmed at the 2026-06-12 spike (Q1/Q3/Q5/Q6); A65 pre-gate PIT availability and A75 depth remain M1 gates.**
- [ ] Historical bulk pull covers **2019-01-01 → the pull date**, UTC-indexed, DST handled (23/25-local-hour days correct), **15-min handling complete** (hourly price series continuous across 2025-10-01; post-transition hourly = mean of its 4 quarter-hours; **feature feeds aggregated PT15M→hourly across the whole archive; chunk-boundary bins complete — every aggregated hour from exactly 4 quarter-hours, no partial bins at pull edges**), no nulls in the DA price; **A65/A01's ~25h head gap at 2019-01-01 handled** (trim or documented head NaNs). Window endpoint and **Fold 5's eval dates pinned** from the pull date (§5.1).
- [ ] **ENTSO-E↔SMARD reconciliation:** DA prices agree within €0.01/MWh on >99.9% of overlapping hours (both Reg-543 sourced — disagreement indicates an ingestion bug, not a market fact). — **Sample pre-confirmed at the spike (96/96 hours); full history at M1.**
- [ ] **Point-in-time availability evidence verifies (§3; v6.5):** the B-Man-PIT ledger produced by the CP-0-reviewed instrument contains at least three non-consecutive qualifying delivery days for A65/A01 at both planned pre-gate times, and a **fresh Critic independently verifies** it: raw-capture hashes match their ledger entries, `pulled_at` values are UTC and pre-gate, each qualifying day carries a complete DST-aware D+1 vector before 12:00, and no entry was mutated after the fact. CP-1 verifies this ledger; it does not produce it, and it does not wait for capture days. An incomplete ledger returns `BLOCKED` naming the missing qualifying days, never a weakened bar. The report distinguishes regulatory proof, observed-available-by time, latest fully populated timestamp, and post-gate corroboration. Any external champion-runtime feed satisfies the same contract. A69 remains post-gate/benchmark-only and A75 remains a proper-training-only fit target. **Champion/benchmark schema-and-fit-lineage separation passes** (§9.4 item 5).
- [ ] **R-2 strict-gate resolution applied (v6.3):** delivery-day A69 excluded from the champion runtime schema; the gate-feasible proxy substitute uses the exact proper-training-only A75 climatology in §4.1; no lagged-vintage alternative is selected; the archive-overwrite harness rerun is documented as §7.2 benchmark provenance.
- [ ] `crisis_period` flips at 2021-09-01 and 2022-12-31; `post_crisis` at 2023-01-01; the gate-feasible proxy standardizations computed train-only, refit per fold; `dunkelflaute_flag` (**v6.3: benchmark / eval-stratum only**) thresholds computed train-only, spot-checked against known events (e.g., Dec-2024).
- [ ] The **shared VRE-expectation climatology** is fit per fold only on proper-training aggregate A75 VRE; its excluded-row count and coefficients are logged. Candidate artifacts persist that layer; a champion persists it if either proxy arm wins. Runtime computation uses **no external call, actual value, or post-gate input**. No second residual-normal layer is fitted (§4.1).
- [ ] **Forward-audit boundary frozen:** after all M1 point-in-time capture work, `selection_cutoff` equals the final complete delivery date in the ratified selection snapshot; snapshot hash, cutoff, and an audit-quarantine manifest are committed. No post-cutoff target, loss, metric, or comparison is available to M2 selection/tuning code.
- [ ] **DuckDB SQL-A mart** authored, integrated as the canonical feature source, and tested (§9.6); the §9.4 leakage/aggregation invariants pass on the SQL-built columns.
- [ ] Committed snapshot carries the **CC BY 4.0 attribution statement** (ENTSO-E Transparency Platform; Bundesnetzagentur | SMARD.de) in the README and the data folder.
- [ ] Invariant tests pass (Section 9.4): closed-left rolling windows; UTC/DST lag alignment; **15-min aggregation integrity incl. the full-bin clause**.
- [ ] **Independent M1 acceptance-oracle pack PASS (§9.4):** a fresh isolated Critic—not the Builder—materializes and hashes M1-O1…M1-O5, computes expected outcomes independently, and verifies misaligned-chunk stitching, missing-quarter rejection, fall-back DST identity, A75 proper-training-only fit lineage, and champion/benchmark schema poisoning. Builder-authored tests alone are insufficient.
- [ ] Data leakage audit drafted; every feature KFT/LAG with justification; rolling windows closed-left.
- [ ] `spectral_eda.py` runs end-to-end on the ingested series; ≥3 figures committed (Welch periodogram with labeled 24h/168h/12h peaks; per-regime periodogram overlay; ACF cross-check).
- [ ] Dominant peaks at 24h and 168h present and annotated; the half-day (12h) harmonic identified.
- [ ] README interpretation paragraph (3–4 sentences) links price peaks → calendar features, labels the residual-load proxy as a candidate decided only by CP-2 ablation, and links per-regime spectral change → regime-stratified, multi-regime evaluation; wording says "confirms/justifies" only for what the price spectrum measures, never "proves."
- [ ] **Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree**, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`). Restates what the §12 `PASS` definition already requires; it is stated per-checkpoint so no checklist is closable without it in view.

### M2 — Baselines and LightGBM
Similar-day naïve, seasonal-naïve-168h, and Ridge baselines trained. LightGBM quantile ensemble trained end-to-end with the three-regime walk-forward CV (24h embargo, per-fold model refitting) and MLflow logging to DagsHub. Packaged as the single `pyfunc` from the start. **v6.3 — proxy selection:** the four pre-specified matched catalogs (strict base; +one-feature residual arm {`gate_feasible_residual_load_proxy`}; +`expected_scarcity_proxy`; +both arms) are run on the pinned folds against the retention and deterministic winner rules fixed before results (§4.1); the **winning strict-gate catalog is frozen at CP-2 before M3.**

**CP-2.** The LightGBM quantile ensemble passes the **development-stage gate** if, on Fold 5, the one-sided probabilistic DM on the daily loss differential favors the model at `p<0.05`; the pre-committed secondary development condition remains: if Fold-5 `p∈[0.05,0.15]` but pooled-across-folds daily-vector DM is significant at `p<0.05`, M2 passes and both numbers are reported (§7.1). Because the same five folds support development and selection, all of these p-values are labeled **descriptive post-selection evidence**, never confirmatory superiority. The **target** improvement is ≥15% mean pinball loss — a reporting target, **not** a pass/fail threshold. **Honest fallback:** if the development DM is significant but pinball improvement is below 15%, M2 still passes; the report states the true percentage and uses the regime-stratified table to show **where** the gains concentrate. Additional gates:
- [ ] LightGBM trains in under 5 minutes wall-clock on M3 (the laptop).
- [ ] **Proxy selection complete (v6.4 operational precision):** the exact four §4.1 catalogs run on matched frozen rows across the pinned folds; the observation-weighted nine-quantile pinball, inclusive raw q10–q90 coverage, exact 1%/2 pp/4-of-5 eligibility boundaries, 0.1% tie rule and deterministic feature/calibration/fixed-order tie-break are applied under the canonical Decimal contract. Raw crossing is retained as submitted; it is neither repaired nor rejected at M2. The **winning strict-gate catalog is frozen**, and "no proxy survives" remains a valid `strict_base` outcome.
- [ ] Both five-fold development DM tests (median MAE and mean pinball) reported with statistic, p-value, effect size, and `evidence_class=development_post_selection`; no confirmatory language is used.
- [ ] **MLflow experiment lineage complete and publicly viewable:** every decision-bearing run used in catalog selection or tuning carries hypothesis, parent/delta, frozen budget, snapshot/cutoff, audit-exclusion manifest, fold/catalog identifiers, code commit, seed, metrics, artifact links, and `keep`/`reject`/`invalid` decision. The frozen CP-2 model is traceable end-to-end. No minimum run count applies.
- [ ] **Forward-audit contract pre-registered before unsealing:** α, comparator, probabilistic daily loss, Newey–West rule, δ*, power formula, eligibility rule, selection cutoff, and the prohibition on reuse are frozen. Catalog, hyperparameters, preprocessing lineage, seeds, code commit, and selection-data manifest are frozen at CP-2.
- [ ] **Committed selection identity:** the final candidate contains `artifacts/cp2/blind/source-manifest.json` and `artifacts/cp2/blind/selection-declaration.json` as required by §4.1/§12; both committed blob hashes are recorded, all four frozen prediction sources are hash-bound, and the declaration names exactly one of `strict_base | residual_arm | scarcity_arm | both_arms`.
- [ ] **Mandatory label-blind four-catalog review PASS (§12):** a fresh Blind Critic sees only anonymous `A/B/C/D` rows and identity-free metadata, independently recomputes the exact raw-head metrics, and **never** identifies the base, applies eligibility or tie-breaks, selects a label, or asserts a winner. Its `PASS` verdict is written and committed **before** the Lead reveals the label→catalog mapping; a revealed mapping is never reused, and any repeat attempt draws a new permutation and a new mapping file. A fresh Integration Critic then applies the §4.1 identities, eligibility comparisons and deterministic tie-break mechanically, and verifies the adjudicated winner against the committed selection declaration — a mismatch is `FAIL`. The Return Packet records every attempt, the Blind and Integration verdict files with their candidate SHAs, the reveal time, and the honest `COOPERATIVE_PROCEDURAL` blindness label: blinding here is procedural — the Lead withholds the mapping — and is never described as cryptographically enforced.
- [ ] **Thin CI live:** GitHub Actions runs the §9.4 invariant tests on every push; green on `main`. No regression matrix.
- [ ] **Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree**, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`). Restates what the §12 `PASS` definition already requires; it is stated per-checkpoint so no checklist is closable without it in view.

### M3 — Calibration, explainability, regime diagnostics
CQR layer fit on the held-out calibration slice, isotonic enforced last (Section 6.2). SHAP and permutation importance on the median model of the **frozen champion** — SHAP **explains** the retained features and may name a headline among them; it is **not** the selection test (that closed at CP-2, §4.1). The **§7.2 post-gate information benchmark** runs here (strict vs. A69-augmented, five metrics). Regime-stratified error table (n_obs + bootstrap CIs on thin subsets; negative-price stratum included). Reliability diagram at **three stages** (raw / post-CQR / final).

**CP-3**
- [ ] **Empirical coverage of the final post-isotonic output** at the 80% nominal level lands within ±5 percentage points on at least 4 of 5 folds — **the gate is on the shipped intervals** (v6.3 correction). The **post-CQR / pre-isotonic coverage is reported separately** as the intermediate carrying the formal CQR marginal guarantee (§6.2).
- [ ] **Three-stage coverage reported (v6.3):** raw heads / post-CQR-pre-isotonic / final post-isotonic, at 50/80/95. The **formal guarantee is claimed only for the post-CQR stage; the final post-isotonic coverage (the gated one, above) is empirical.** The reliability diagram shows all three and reports the signed coverage change introduced by isotonic-last; no direction is assumed. Coverage is **per-interval marginal**, not joint (documented).
- [ ] **Monotonicity after CQR + isotonic (moved from CP-2, v6.3):** zero quantile-crossing violations on the validation tail after the full CQR-then-isotonic pipeline, including negative and zero-crossing intervals; the §9.4 monotonicity invariant passes.
- [ ] **Independent CQR threshold Critic PASS (mandatory, v6.4):** a fresh read-only Critic recomputes `E_i`, one-based `k`, zero-based positions, and all four thresholds on the §6.2 `n_cal=20` fixture; verifies `Q={8,7,5,−1}`, negative-Q behavior, and unshifted p50; then independently recomputes all four thresholds from persisted calibration predictions for at least one real fold and matches the pipeline within numerical tolerance.
- [ ] **Forward-audit readiness/status:** the complete post-CQR/post-isotonic champion is tagged and frozen before audit outcomes are opened; `N_required` is computed from frozen development artifacts. If enough eligible days exist, the one-shot audit is reported as `CONFIRMED` or `NOT_CONFIRMED`. Otherwise it remains sealed and is reported as `PENDING_UNDERPOWERED` with `N_available` and `N_required`; insufficient power does not become a synthetic p-value gate.
- [ ] **SHAP summary + top-10 reported** for the champion; a headline, **if any, is named among already-retained features** — a low-importance or "no domain feature" result is a **valid, reportable outcome, not a failure** (v6.3: the named-feature requirement is removed).
- [ ] **SHAP↔permutation top-10 overlap reported descriptively** (v6.3: not a pass/fail gate — correlated features make overlap uninformative); any rank disagreement is discussed.
- [ ] **§7.2 post-gate information benchmark reported:** strict vs. A69-augmented (frozen CP-2 catalog + A69, calibrated separately), five metrics — MAE, mean pinball loss, empirical coverage, mean interval width, calibration error — with the strict-gate cost stated. The benchmark is never promoted to champion.
- [ ] Regime-stratified table renders cleanly; the crisis vs. pre-crisis vs. post-crisis differences documented and explained; the negative-price stratum read against the floor caveat; n_obs + bootstrap CIs on thin subsets.
- [ ] **Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree**, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`). Restates what the §12 `PASS` definition already requires; it is stated per-checkpoint so no checklist is closable without it in view.

### M4 — Marimo showcase runs locally; champion registered; slider lookup precomputed
Marimo (server mode) runs end-to-end **locally**, loading the registered Production model (registry-first, bundle-fallback) and applying calibrated quantiles on the snapshot. The precomputed slider lookup (with per-cell OOD flags) is generated and validated by the M4 spike. CLI inference (`predict_next_day.py`) runs inside the container in under 5 minutes. The showcase includes the §10 item-(3b) spectral section — the labeled periodogram figure + one interpretation paragraph, rendered as a static section with **no slider**. **v6.3:** the interactive sliders are drawn from the champion's retained features (§9.2), not a pre-ratified set; and the **offline data/output-health report (§9.5) is generated here (≤ ~8 h cap).**

**CP-4**
- [ ] Champion `pyfunc` registered to the DagsHub MLflow Registry with the `Production` tag, `source_run_id`, exact tagged code commit, snapshot hash, catalog ID, preprocessing lineage, and calibration artifact linked.
- [ ] Local marimo render from cold start completes in under 10 seconds.
- [ ] The **retained-feature sliders (v6.3, §9.2)** update from the precomputed lookup without errors and without live compute (quantile selector + the champion's retained controls; no A69-based control).
- [ ] The precomputed grid covers the interaction space; indexing is instant; OOD flags light correctly on extrapolated cells (M4 spike validated).
- [ ] CLI inference smoke test asserts all upstream pulls returned recent (≤48h) rows in fresh-data mode.
- [ ] **Offline data/output-health report generated (v6.3, §9.5):** freshness / schema conformance / selected-feature (retained) distribution shift / interval width / quantile sanity, each against a pinned threshold; no service, no schedule, ≤ ~8 h.
- [ ] **Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree**, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`). Restates what the §12 `PASS` definition already requires; it is stated per-checkpoint so no checklist is closable without it in view.

### M5 — Deployed showcase on HF Spaces
Marimo (server mode) containerized via multi-stage Dockerfile (bundling snapshot, slider lookup, champion) and deployed to HF Spaces free tier (Docker SDK), with the DagsHub token as an HF Secret. **v6.2 rider:** the static first-touch page is built (`marimo export html`; fallback per §9.2) and goes live on **GitHub Pages**, carrying the full §10 reading order. Final artifact passes the stranger-test gate per §10.1 (reviewers start at the static page; interactive portion on the deployed Space). CV and LinkedIn updated with the **static Pages URL as the primary portfolio URL**, the live Spaces URL and the public MLflow URL linked beneath it (CV-3 carries this set). **v6.3:** the offline data/output-health report (§9.5) and the §7.2 benchmark table are rendered into the static page; the sliders are the champion's retained-feature controls (§9.2).

**CP-5**
- [ ] The deployed app loads within 30 seconds of cold start (warm load under 5 seconds).
- [ ] The Production model loads on startup — registry if available, else the bundled fallback; the demo renders either way.
- [ ] The **retained-feature sliders (v6.3, §9.2)** update the forecast and SHAP from the precomputed lookup on the deployed instance, with the OOD banner firing on flagged cells.
- [ ] A non-technical reviewer reaches a correct directional interpretation within 20 minutes unaided, **starting at the static Pages URL** and following the labeled link to the live Space (§10.1, v6.2).
- [ ] The honest limitations section names the exchangeability violation, **the strict-gate design and the §7.2 measured cost of feasibility (v6.3, replacing the old as-archived VRE caveat)**, the regime-stratified gaps, the two-sided bounded-target tail (floor live; −600 from 2026-05-28 noted), any coverage divergence, the 15-min averaging choice, and the **model-staleness disclosure**.
- [ ] Reproducibility statement present and accurate (seeds, pinned versions incl. `entsoe-py` 0.8.0, snapshot at tagged release, **CC BY attribution**, DagsHub permalinks, compute footprint, training-data cutoff stated).
- [ ] **(v6.2)** The static first-touch page is live on GitHub Pages, carries the full §10 reading order, loads in **< 3 s on a cold cache from an independent connection**, and performs zero runtime calls to the Space or any external service.
- [ ] **(v6.2)** The primary portfolio URL on CV / LinkedIn / README is the Pages URL; the interactive Space (with the explicit wake label) and the public MLflow UI are linked from it.
- [ ] **(v6.2)** The Monday refresh procedure (snapshot + static re-export + commit) is documented and has been exercised once end-to-end with both surfaces in sync.
- [ ] **(v6.3)** The offline data/output-health report (§9.5) renders statically on the Pages page within the < 3 s cold-cache gate; the §7.2 post-gate benchmark table is present.
- [ ] The static report labels all five-fold DM results as descriptive post-selection evidence and displays the forward-audit status — `CONFIRMED`, `NOT_CONFIRMED`, or `PENDING_UNDERPOWERED` — without overstating the claim.
- [ ] README, static page, Space metadata, registered champion, and public MLflow lineage agree on snapshot/cutoff, frozen catalog, metrics, audit status, and champion source run.
- [ ] **Fresh Integration-Critic `PASS` at the exact `final_candidate_sha`/tree**, with the evidence tip above it containing verdict files only (`git diff --name-only <final_candidate_sha>..<evidence_tip_sha>` returns nothing outside `docs/track-b/evidence/<checkpoint>/`). Restates what the §12 `PASS` definition already requires; it is stated per-checkpoint so no checklist is closable without it in view.

The 30-second cold-start threshold is calibrated to HF free-tier reality. Sleep behavior is accepted per Section 9.2; the README disclaimer is the backstop.

### Post-CP-5 — Companion artifact (pointer only)

After CP-5 closes and the program's Month-5 gate passes, a **second, standalone Track B artifact** begins: the **Binary Classification Mini-Capstone** (IEEE-CIS fraud detection — file: `Binary Classification Mini-Capstone.md` in project knowledge). It is the portfolio's classification counterpart to this project's regression/forecasting arc and reuses this project's toolchain (LightGBM, MLflow/DagsHub, HF Spaces, Docker multi-stage, isotonic calibration, bootstrap CIs).

**This pointer is deliberately all this document says about it.** Nothing in M0–M5 depends on that document, and it is **not read, briefed, or consumed before this point** — its execution anchors are the orchestrator instructions and the program-stage-sequence map (rows B-Man3, FM0–FM5, FCP-1–5, G6), which sequence and brief it when its time comes.

## 13. What This Project Is NOT

Each omission is a deliberate scope decision, defended and interview-quotable. The v6.0 conversion **re-ratified** every v5.5 boundary that survives the market swap and added two new ones; v6.1 closes the single gas reopen condition on spike evidence. The only entries that died with PJM are those whose subject matter died with it (gas-fallback machinery, the alert-sourcing risk).

**No multi-day-ahead forecast.** *"The target is the next delivery day, because that's what the 12:00 CET day-ahead auction clears and it's the horizon at which every lagged feature is legitimately known at forecast time. A multi-day forecast made at the gate would have to consume prices that haven't cleared yet — a look-ahead leak. I'd rather ship a clean 24-hour forecast than a leaky multi-day one."*

**"Why isn't the wind/solar forecast a feature?" (strict-gate design, v6.3)** — *"Because it isn't known when the model runs. The day-ahead auction closes at 12:00; I verified at the project's data spike that ENTSO-E's day-ahead wind and solar forecast for the delivery day isn't published until the 15:35–18:00 window — after the gate. Most published EPF work uses the as-archived series anyway and discloses it; I went further and excluded it from the shipped model entirely, so the champion is feasible at 12:00 by construction, using gate-feasible proxies for the merit-order signal **where they earn their place in a pre-registered ablation** — and shipping without that feature if they don't. Then I quantified what I gave up: a controlled benchmark, identical except it adds the post-gate forecast, measures the cost in MAE, pinball loss, coverage, interval width and calibration error. So I ship an honest model and put a number on the value of information I chose not to use."*

**No quarter-hourly target (new, v6.0).** *"SDAC moved to 15-minute products in October 2025. I kept the hourly target end-to-end — averaging post-transition quarter-hours, which SDAC's own no-arbitrage constraint defines as the hourly equivalent — because a 7.5-year continuous hourly series with three regimes teaches the model more than nine months of quarter-hours, and because a 96-step target would have quadrupled the output dimension mid-project for no methodological gain. Quarter-hourly is the natural v2, and the boundary invariant test means the pipeline already handles the dual-resolution data correctly."*

**No standalone fuel-price layer (adjudicated closed, v6.1).** *"There is no free, daily, legally-redistributable TTF or THE gas series — Yahoo's exists but its terms prohibit redistribution, and I wasn't going to build a reproducible open repo on data I can't commit. I even probed SMARD's API at the project's de-risking spike: it exposes gas-fired generation volumes, not THE prices, so the pre-committed reopen condition closed on evidence. Residual load is the literature-standard German merit-order driver, so I tested a gate-feasible residual arm and kept it only if it earned stable out-of-sample value; if it did not, the strict-base champion and the A69 benchmark state that limitation explicitly."*

**No formal causal claim.** *"The model makes a predictive claim with calibrated uncertainty, not a causal one. The perturbation slider is a ceteris-paribus sensitivity probe — it shows how the model responds to a feature holding others fixed, which can be physically out-of-distribution, and the per-cell OOD flag makes that visible. SHAP attributes predictions, it doesn't establish causation. I deliberately stayed on the predictive side of that line."*

**No enterprise production pipeline or live trading layer.** *"Containerized marimo on Hugging Face Spaces backed by a DagsHub MLflow registry is portfolio-grade hosting, not production infrastructure — no CI/CD matrices, no scheduled retraining, no live per-visitor API ingestion, no trading gates, no scheduled/production drift monitoring. **(v6.3: there is one offline batch data/output-health report — freshness, schema conformance, selected-feature distribution shift, interval width, quantile sanity — but it makes no model-performance-drift claim, because no realized outcomes exist at generation time, and it schedules nothing and calls no service.)** I invested in interactive risk communication and transparent experiment tracking, which showcase DS judgment, rather than enterprise MLOps boilerplate. DVC was rejected as collaboration overhead the project doesn't need; live per-visitor pulls as brittleness that would break the demo."*

**No neural challenger — no NBEATSx, TFT, or transformers.** *"The peer-reviewed EPF evidence shows classical and tree-based baselines within a few percent of deep learning on hub- and zone-level day-ahead price; the marginal accuracy doesn't earn the complexity for a portfolio. LightGBM is the published-evidence-backed default — and the recent German-market XAI literature (Trebbien et al.) runs on exactly this model class. I'd rather demonstrate judgment than run a model horse-race. I can build and train networks — there's a shipped CNN in my portfolio — so this is a choice, not a limitation."*

**No LEAR / `epftoolbox` baseline.** *"LEAR is the canonical EPF baseline but a specialized library that obscures the DS skills the project demonstrates. Ridge on the same engineered catalog plus the similar-day naïve and the 168h naïve covers the same transparent-classical-baseline role — and the similar-day naïve follows Lago's exact convention, so the comparison stays anchored to the published benchmark."*

**No sequential conformal (EnbPI / SPCI) and no Giacomini-White.** *"CQR gives finite-sample marginal coverage under exchangeability — the property an interview reader is most likely to recognize and probe. EnbPI/SPCI handle non-exchangeability more aggressively but add conceptual surface that distracts from the core argument. The reliability diagram makes the exchangeability violation visible, and the limitations section names it."* **Pre-committed reopen condition (carried verbatim, the only one):** if the M3 reliability diagram shows empirical coverage on any regime stratum diverging from nominal by **more than 10 percentage points**, EnbPI returns as **remediation** — a documented comparison only; CQR remains the shipped calibration inside the champion `pyfunc`. Below that threshold the boundary stands as written.

**"Does isotonic break your CQR coverage guarantee?" (three-stage coverage, v6.3)** — *"It can, so I'm precise about it. CQR's finite-sample marginal guarantee attaches to the conformalized output, before the isotonic step. Isotonic is a monotone rearrangement I apply last only to kill residual quantile crossings; it does nothing **only when all post-CQR quantiles are already ordered**. The sign of a pair's conformal threshold is not enough to establish global ordering. Because isotonic can move interval edges whenever it acts, I report the final post-isotonic coverage as empirical and show all three stages — raw, post-CQR, final — on the reliability diagram, and I claim the formal guarantee only for the stage that actually has it."*

**No trading layer, battery arbitrage, or bid logic.** *"The deliverable is a forecast and a diagnostic report. Wrapping a forecast in a trading layer either needs real risk infrastructure or it's a toy — and a toy trading layer would undermine credibility. The forecast and its honestly-communicated uncertainty are the artifact."*

**No test matrix, no regression CI — but targeted invariant tests on a thin CI.** *"I wrote five focused property tests on the invariants that silently corrupt results if violated — quantile monotonicity after the CQR-plus-isotonic pipeline, closed-left rolling windows, UTC/DST lag alignment, aggregation integrity across the 15-minute market transition including full-bin completeness at chunk boundaries, and (v6.3) runtime-schema plus fit-lineage separation: delivery-day A69 and actual columns cannot enter champion prediction, while the VRE climatology may use A75 only inside proper training — and a thin GitHub Actions workflow runs them on every push. That's the right testing level for a single-developer portfolio: assert the properties that must hold and verify them automatically on every commit, not a regression matrix on a script that runs in five minutes. Still no scheduled retraining, no drift gates, no rollback machinery."*

**"How did you choose your seasonal features?"** — *"I didn't just assume 24h and weekly cycles — I confirmed them. A Welch periodogram of the detrended price series shows clear peaks at 24h, 168h and a 12h harmonic, which is exactly what the EPF literature reports (Weron 2014; Lago et al. 2018). That's the empirical justification for my hour-of-day and day-of-week features. It only motivates testing a seasonal residual-load candidate; the M2 ablation — not the price spectrum — decides whether that candidate belongs in the champion."*

**"How do you know your walk-forward folds respect the regime changes?"** — *"Change-point detection with `ruptures` defines the pre-crisis, crisis, and normalization boundaries; the folds are not mechanically cut at those dates. I deliberately pin five 90-day evaluation blocks so they sample every regime and both stress directions, while each training window still expands through time with a 24-hour embargo. Per-regime periodograms show why I should not collapse those results into one recent-tail story: seasonal amplitude and the noise floor differ by regime. That is an evaluation-coverage argument, not a claim that spectral analysis prevents leakage."*

**"You have a signal-processing background — where did it help here?"** — *"The spectral EDA. I used Welch's method rather than a raw periodogram because the series is long, noisy and non-stationary; averaging modified periodograms with a Hann window trades a little frequency resolution for much lower variance, which is what you want when you're confirming known cycles rather than hunting faint tones."*

**"Why is your portfolio link a static page and not the live demo?"** — *"Because a recruiter's first impression shouldn't depend on whether a free-tier container happens to be awake. The primary URL is a static export of the full report — CDN-served, loads in under three seconds, can't sleep — and the interactive demo is one labeled click deeper, with the possible ~30-second wake stated up front. Nothing methodological lives only in the live app: the interactivity is served from a precomputed lookup anyway. Decoupling the first impression from the runtime is the design decision."*

---

*End of Engineering Plan — German DE-LU Day-Ahead Price Forecasting Tool, v6.6 — Lean DS Capstone with Cloud-Backed Showcase.*
