.DS_Store

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Track B control and local evidence state (never committed or exposed to Critics)
/workbench.md

# Secrets
.env
*.env

# Scratch data / probe evidence (not committed — see docs/spike-feed-status.md for summaries)
data/

# macOS custom folder icon (filename ends with CR)
Icon?
