"""pit_capture: a point-in-time capture instrument for the ENTSO-E day-ahead
load forecast (documentType A65, processType A01) on DE-LU.

See capture.capture_attempt for the single public entry point that performs
one capture attempt and appends one row to the append-only ledger.
"""
