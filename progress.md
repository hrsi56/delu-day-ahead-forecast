# Yarden's Triple-Track Progress Log

*Living document, maintained under `orchestrator-role.md`. **Regenerated 2026-09-08 after the v6.7 application landed on `main` and a mis-resolved merge was repaired.** Anchors: `capstone_V6_7.md` **v6.7** (ratified 2026-09-07); `program-stage-sequence.md` **v8**. `syllabus_v3_2.md` is an optional resource and is not an anchor. **The v6.7 application is landed and published:** `main` = `origin/main` = `30b1b9f`, a merge of `d65879b` (the application) and `b67e065` (a stale pre-v6.7 snapshot). That merge was mis-resolved; **the repair landed by hand at `c8dd5cc` and was pushed by the owner on 2026-09-08.** `main` = `origin/main` = **`c8dd5cc`**, verified against the remote.*

---

## Current Position

### Track B — Capstone Builds: the critical path, and the only one

- **Anchor:** **`capstone_V6_7.md` v6.7**, owner-ratified 2026-09-07. Amendment record: `docs/track-b/capstone_V6_6-to-V6_7-amendments.md`, whose **post-ratification repair addendum (2026-09-07)** supersedes its *Application scope* bullet for the old Gauntlet plan and acceptance items 1, 12 and 15. Read the addendum before re-running that acceptance test.
- **Position: M0 DONE. M0.5/CP-0 CLOSED and RETIRED.** It executed, landed (`a911191`), was tagged `land/cp-0` / `evidence/cp-0` and reclaimed; an earlier attempt is archived at `archive/cp-0-attempt-1`. **Its requirement was cancelled 2026-09-06 and the instrument was removed from the active tree at v6.7.** Verdicts remain at `docs/track-b/evidence/cp-0/`; the sources remain reachable from `evidence/cp-0`, verified before and after deletion.
- **Next Track B action: exactly one M1/CP-1 brief.** Its only open prerequisite is PRE-1 (below): the token is **restored**, but the required live read-only ENTSO-E request has **not yet succeeded** — the platform returned gateway timeouts, not an auth rejection. **CP-1 is not schedulable until one such request succeeds.**
- **The arc is three checkpoints:** M1/CP-1 (data and fixed features, 10 items) → M2/CP-2 (model, calibration, analysis, 10 items) → M3/CP-3 (showcase and release, 6 items). CP-4 and CP-5 are retired. 56 future checkboxes became 26.
- **What v6.7 changed, in one line each.** Point-in-time capture cancelled — A65/A01 pre-gate availability is a disclosed assumption. The four-catalog selection system retired; two catalogs frozen before fitting and one raw-head comparison decides. The domain feature rebuilt as a 42-day D-2-bounded trailing-window lag proxy — no fit, no partition rule, no poisoning oracle. Empirical outcomes report rather than gate; zero quantile crossing is the one hard gate. The forward audit retired for a **frozen 90-day holdout** inside the existing snapshot, evaluated once. §9.5's health report deleted; spectral EDA trimmed; DuckDB is a visible SQL artifact, not the canonical feature source. The release is frozen and bundled — no registry-first path, no weekly refresh, no load-time gates. The Gauntlet collapsed to one fresh Integration review per checkpoint under a soft hour timebox.
- **Defect ledger.** `docs/track-b/cp-0-defects.md`: 17 ACCEPTED · **D-CP0-18 MOOT** (its requirement was retired by v6.7; nothing left to re-test) · D-CP0-19 REMEDIED, re-test still rides inside CP-1's landing · D-CP0-20 REMEDIED, never exercised (its remedy post-dates the only CP-0 run) · AMD-G5 WAIVED. **The ledger's closure depends on D-CP0-19 and D-CP0-20** — D-CP0-20 was added to that condition on 2026-09-07 after an independent audit found the ledger reporting it un-re-tested while allowing closure without it.
- **Governance Lockdown in force** since 2026-08-08 (`f45e577`). The v6.7 application suspension and the three 2026-09-07 repair authorizations were each spent at their returns. A **fourth task-scoped authorization, granted 2026-09-08**, covers the merge-damage repair — the named line deletions in `orchestrator-role.md` and `program-stage-sequence.md`, deletion of the resurrected `capstone_V6_6.md`, and this regeneration — and is spent at this return. It grants no publication and no unrelated authority.
- **`AGENTS.md` repaired 2026-09-07** under a second owner authorization, after the main application returned. Seven sites carried text v6.7 invalidated — three reported at the return, four more found on a full scan. No policy changed; every edit repointed a citation or named retired machinery as retired. The broader governance record still requires the fresh independent re-verification named in the latest Session Log.

### Track C — Marketing: FROZEN by default

- No Track C block active. The continuous-activation phase trigger fires after flagship M2, but **applications open whenever Yarden chooses**; no G3, Month-5, Track-A or Track-B gate controls permission to apply.
- **Last published artifact:** `hrsi56/Zero-Trust-Hierarchy`, public, carrying the method article, rulebook, forms and a Pages rendering, plus three Code-Graph-RAG documents at `3ab7e1f`. Separate history and remote; portfolio surface, not a Track B artifact.
- CV iteration budget: three slots across the year.
- **A live job posting was assessed 2026-09-06** (Kamada QA Specialist, Beer-Sheva). Recommendation recorded: not a DS role and not a route to one; eligible but a parallel career ladder. **Open question below — the owner's income runway is unknown and it is the fact that decides it.**

---

## Setup State

- **Project Knowledge swap — owner action pending:** `capstone_V6_7.md`, `program-stage-sequence.md` v8, this file, and the current role/router docs. `syllabus_v3_2.md` is optional reference, not an anchor.
- **NotebookLM:** not required. Load the syllabus only if an optional Track A block is actually run.
- **Role routing:** `AGENTS.md` is the canonical router and carries the Governance Lockdown; `CLAUDE.md` points to it only; `orchestrator-role.md` governs program management; repo-root `engineering-role.md` governs Track B execution.
- **Canonical templates:** `docs/track-b/gauntlet-templates.md` — **four forms** (was ten). Its compatibility table resolves old section numbers retained in historical records.
- **PRE-1 — `ENTSOE_API_TOKEN` RESTORED; live verification STILL OPEN (2026-09-08).** The token is present in the current process, in `launchctl`, and in a fresh login shell (36 characters; the value is never printed and no credential is stored in this repository). The required read-only request has **not** succeeded: A44 and A65 return `HTTP 599 uu-gateway-router/connectTimeout` across three rounds (~01:40, ~01:59 and ~02:00 Berlin on 2026-09-08), plus one plain read timeout. **The fault is isolated to the ENTSO-E API gateway and the token is not implicated**, on four controls run 2026-09-08 ~02:00 Berlin: general internet is healthy (github.com and google.com HTTP 200 in ~0.15 s); **ENTSO-E's own `transparency.entsoe.eu` returns HTTP 200 in 0.49 s**, so this is not DNS, routing or geo-blocking; `web-api.tp.entsoe.eu` returns 599 **with no token at all**; and — the decisive control — **a deliberately invalid all-zeros token returns the identical 599 rather than 401**, proving the gateway never reaches the authentication layer. The same host and the same pinned `entsoe-py` 0.8.0 returned HTTP 200 with a valid A44 document on 2026-09-07, so this is a platform availability regression, not a moved endpoint. **Nothing is actionable on our side; PRE-1 closes when one read-only request succeeds.** **Cause confirmed 2026-09-08 from ENTSO-E's own notice board** (`transparency.entsoe.eu` → News): the platform migrated its **production infrastructure on Monday 7 September 2026 from 16:00 CEST, in a window of up to 6 hours**, after an earlier attempt on 2 September was **rolled back**. At 02:09 CEST on 8 September — roughly four hours past the stated window — `web-api.tp.entsoe.eu` had degraded from 599 to **no response at all** (HTTP 000, 30 s timeout), while `transparency.entsoe.eu` served 200 in 0.46 s. The 1 September notice warns in terms that match our symptom exactly: the Web API is down for a technical reason and *"the message currently returned ... may be misleading, as it may suggest a planned outage."* TLS and DNS are healthy — a valid Sectigo certificate to 2027-01-13, and `web-api` resolving via `external-api.tp.entsoe.eu` → `tp.entsoe.eu` → `20.79.236.108` — so the announced IP/certificate changes are not implicated for us. **Resolved 2026-09-08 14:05 CEST: the migration completed and the API is deliberately switched off.** ENTSO-E's notice of 08/09/2026, *Transparency Platform Migrated to New Infrastructure*, states the migration **"has been completed"** and that **"the External API remains temporarily unavailable and will be re-enabled as soon as technically possible"** — no date given. The symptom changed accordingly, from `599 uu-gateway-router/connectTimeout` to a bare **`404 page not found`** (19 bytes, `text/plain`, Go's default handler) on every path of that host including the news RSS feed. **The endpoint has not moved:** DNS and certificate are unchanged, so the pinned `entsoe-py` 0.8.0 base URL stays correct and no client change is warranted. The same notice reports data processing and publication **delayed by several hours**, which will degrade freshness at the pull date for as long as stabilisation lasts. `launchctl setenv` remains session state rather than durable secret storage, so confirm presence again after any reboot.

## Strategic Anchors

- **Target:** industry Data Scientist at NIS 35K. Audience: DS hiring manager, not MLE.
- **Authoritative document:** `capstone_V6_7.md` **v6.7**. **This file decides what is law.**
- **Optional reference:** `syllabus_v3_2.md` v3.2 — demoted 2026-09-06, banner-marked at v6.7, not an anchor, not retired.
- **Planning aid:** `program-stage-sequence.md` **v8**, static/non-anchor — capstone-first, with an optional learning sidecar.
- **Optional future projects, neither scheduled:** the companion `Binary Classification Mini-Capstone.md` and `aws-extension-spec_v1_1.md` (marked **STALE / NOT SCHEDULED**). Each needs a new explicit owner instruction after CP-3.
- **Governance record:** `docs/track-b/rule-inventory.md` and `docs/track-b/cp-0-defects.md`. Both locked. Amendment sheets: `capstone_V6_1-to-V6_2` … `V6_5-to-V6_6` at root, and `docs/track-b/capstone_V6_6-to-V6_7-amendments.md`.
- **Repos:** flagship `hrsi56/delu-day-ahead-forecast` (local `/Users/djourno/Downloads/PJM`); public method repo `hrsi56/Zero-Trust-Hierarchy`.
- **Budget/hardware:** $0 expected run rate, $65/month ceiling; M3, 16 GB, CPU-only.
- **Language:** replies and briefs in English; Hebrew input fine.

## Standing Scope Decisions

- **Point-in-time availability is ASSUMED, not measured (2026-09-06).** A65/A01 is KFT by Regulation 543/2013 Art. 6(2)(b), corroborated by the 2026-06-12 spike. **The assumption is disclosed as an assumption** in §5.2 and in the public limitations; no document may describe it as empirically demonstrated. Reopening requires new evidence that the regulation is not honoured, not a preference.
- **A75 revision is the second disclosed assumption (2026-09-07, new at v6.7).** The 42-day trailing window uses the archive's current actuals rather than the values visible in real time. Disclosed in the same places, in the same class, as the A65 assumption.
- **Track A is optional (2026-09-06).** The syllabus is a parallel resource, not a gated track. No learning checkpoint blocks any Track B or Track C work; applications open whenever Yarden chooses.
- **Results are reported, never gated (2026-09-07).** No checkpoint requires a favorable p-value, effect size, coverage figure, ranking, direction or timing threshold. **Zero quantile crossing after CQR-then-isotonic is the one retained hard gate**, because it is a correctness property rather than an outcome. An unfavorable result constrains the public claim and never blocks completion.
- **The shipped model is the holdout-evaluated model (2026-09-07).** No retrain follows the holdout. The raw-model fit cutoff therefore precedes the snapshot cutoff by 152 delivery days, and all four cutoffs are published separately.
- **Governance Lockdown (2026-08-08, prime directive, owned by `AGENTS.md`).** No agent may modify the rulebook, ratified anchors, governance record, or agent configuration. Halt and request a task-scoped suspension. `progress.md` is Orchestrator-owned state; status/evidence/disposition fields in the defects ledger may be updated without suspension, but acceptance criteria may not.
- **AMD-G5 negative control — WAIVED KNOWINGLY BY THE OWNER, 2026-09-04. Do not raise it again.** v6.7 retired `BRIEF_INVALID` itself; the compensating control — the Orchestrator's mandatory pre-dispatch brief validation — is now the whole front gate.
- **Owner observance constraint.** No scheduled work on Friday or Shabbat. Calendar-bound blocks schedule Sunday–Thursday.
- **A scheduler's advertised behaviour is not its behaviour (2026-09-06).** GitHub Actions' free scheduler delayed all four crons by 3.0–3.7 hours on its first real day. Never place a hard-deadline job on it.
- **Reviews of the execution contract are unscoped by default.** A scope claim is an instruction not to look.
- **Authority limits are stated as scope, never as capability** (D-CP0-20).
- **v6.7 execution contract:** one fresh Integration Critic per checkpoint from a clean detached checkout; Builder ≠ Critic; two terminal SHAs with a verdict-only delta; three terminal statuses (`PASS` / `BLOCKED` / `INCOMPLETE`); an approximate hour timebox instead of a raw-second ceiling; delegation is the Lead's choice; branch accountability and owner-only landing unchanged.
- **Strict-gate architecture:** delivery-day A69 and derivatives forbidden from champion runtime; **lagged actuals through D-2 are permitted LAG inputs**, which is what admits the residual-load proxy.
- **Market/data:** DE-LU via ENTSO-E + SMARD, CC BY 4.0. No gas layer, no external weather.
- **Model/scope:** single LightGBM quantile ensemble; CQR then isotonic; no neural challenger, trading layer, live session pulls, DVC, or enterprise monitoring. The EnbPI reopen is retired — a coverage miss is shown and discussed.
- **Track B authority:** the Orchestrator decides what, when, which repo, which checkpoint, and the timebox; the Engineering Lead decides how; the checkpoint return closes a checkpoint.
- **Publication authority:** all agent work stays local. No push, no PR, no agent commit to `main`. Landing is an owner-authored squash plus **two** tags — `land/<cp>` on the squash commit and `evidence/<cp>` on the reviewed chain.
- **Branch policy:** parallel work expected; every agent-opened branch declared in its terminal return; unaccounted branches escalated, never auto-deleted.

## Session Log — newest first

- **2026-09-08 14:05 CEST (latest) — the migration completed; the External API is off by choice, not broken, and the endpoint did not move.** ENTSO-E's 08/09/2026 notice *Transparency Platform Migrated to New Infrastructure* confirms the production migration **completed**, that services are **still being stabilised** with data publication running **several hours late**, and that **the External API "remains temporarily unavailable and will be re-enabled as soon as technically possible"** — no committed date. Our probe changed shape in step with it: `599 uu-gateway-router/connectTimeout` became a bare **`404 page not found`** (19 bytes, `text/plain`, Go's default handler) on every path of the host, the news RSS feed included. **An earlier reading in this session — that the `external-api` hostname suggested the endpoint had moved — is wrong and is corrected here:** DNS and certificate are unchanged, the API is simply disabled, and the pinned `entsoe-py` 0.8.0 base URL remains correct. **No client change, no token action, no configuration change is warranted.** The 30-day cap stays untestable until the API returns. PRE-1 remains OPEN on a third party with no ETA.
- **2026-09-08 — PRE-1 failure diagnosed to an ENTSO-E production-infrastructure migration; two CP-1 constraints found.** Four controls isolated the fault away from us — healthy general internet, `transparency.entsoe.eu` at 200 in 0.46 s, 599 from the API with **no token at all**, and an identical 599 from a **deliberately invalid all-zeros token** where a real auth layer would return 401. ENTSO-E's own notice board then supplied the cause: **PROD migrated to new infrastructure on Monday 7 September 2026 from 16:00 CEST over a window of up to 6 hours**, after a 2 September attempt was rolled back, and the 1 September Web API disruption notice explicitly warns that the returned message *"may be misleading, as it may suggest a planned outage."* By 02:09 CEST on 8 September the endpoint had degraded from 599 to no response at all. **Two findings carry into the CP-1 brief:** a temporary **30-day-per-request cap** announced 3 September, which would force roughly 375 chunked requests for the 2019-onward pull and must be re-checked when the API returns; and the **File Library / FMS API**, which stayed up throughout and is a viable alternative bulk route. Both are recorded in Notes. **No owner action exists** — this is a wait on a third party.
- **2026-09-08 — the v6.7 application landed on `main`, but through a mis-resolved merge; the damage was found and repaired under a fourth authorization.** `main` = `origin/main` = `30b1b9f` merges `d65879b` (the correct v6.7 application) with `b67e065` — **a pre-v6.7 snapshot that had unresolved `git stash pop` conflict markers committed into it** (`<<<<<<< Updated upstream` / `>>>>>>> Stashed changes`). The resolution left three defects on the published mainline: **six stray `>>>>>>> claude/v6-7-verify-working-tree-f8ab80` markers** (this file ×2, `orchestrator-role.md` ×2, `program-stage-sequence.md` ×1, `aws-extension-spec_v1_1.md` ×1); **eighteen duplicated lines** — this file's three Strategic Anchors bullets appearing three times and six Notes bullets twice, three `orchestrator-role.md` provenance-comment lines, and three AWS-spec banner lines; and **`capstone_V6_6.md` resurrected at repository root**, 605 lines, undoing the v6.7 rename and leaving two capstone plans where the convention allows one. **Nothing was lost.** Both parents were diffed against the merge: everything discarded from `b67e065` was stale pre-v6.7 text (v6.6 anchors, map v7, the five-checkpoint arc, gated Track A), and the one genuinely newer thing it carried — the 2026-09-08 AWS-spec renumbering that stops DEC-AWS reserving a version number — was correctly kept by the merge and is retained by this repair. The repair is **deletion-only: 24 lines and one file.** After it, this file, `orchestrator-role.md` and `program-stage-sequence.md` are **byte-identical to `d65879b`**; `aws-extension-spec_v1_1.md` differs from it only by the three newer renumbering hunks; and no conflict marker remains anywhere in the tree. **The repair landed at `c8dd5cc`**, authored and pushed by the owner the same night. Verified on the published tree: zero conflict markers, one capstone plan at root, `orchestrator-role.md` and `program-stage-sequence.md` byte-identical to `d65879b`, and `aws-extension-spec_v1_1.md` differing from it only by the three retained renumbering hunks. **`30b1b9f` itself remains in the public history with its markers** — a forward fix, not a history rewrite, which is the deliberate choice for a portfolio remote. The repairing agent does not certify its own work.
- **2026-09-08 — PRE-1 re-checked: the token is restored, the live request still fails on the platform side.** Detail in Setup State; recorded here because it is what decides CP-1's schedulability. One credential-handling finding came out of the check and is carried into the CP-1 brief: **`entsoe-py` 0.8.0 passes `securityToken` as a query parameter**, so the token appears verbatim in the request URL *and inside the text of raised exceptions* — any code that logs an exception or a request URL leaks it.
- **2026-09-07 — fourth independent read-only audit returned `FAIL`; all seven findings repaired under a third owner-authorized Lockdown suspension.** The repair made §9.4 and its §13 interview script explicit that the eight tests and thin CI are CP-1 requirements that do not yet exist; removed the last live weekly-snapshot claim; narrowed the Month-0 archive finding to its single sampled day; corrected addendum R-2 to the actual five-hunk CP-0 diff after recording the archive tag as lightweight; restored the ratified companion plan at repository root byte-for-byte from the Owner's source (`SHA-256 1a26f069b0b98a541644559a25f5c838e002761773f1c80563fa98426172d817`); and repointed the stage map to the real *Track C activation rules* heading. Directly necessary consistency edits in `engineering-role.md` and `orchestrator-role.md` now describe the former M1 oracles as CP-1 test requirements rather than already-committed tests. The repair is uncommitted and requires a fresh independent re-verification; its author does not certify it.
- **2026-09-07 (earlier) — third independent audit returned `PASS WITH NON-BLOCKING NOTES`; post-ratification repair applied in two authorized rounds.** A fresh read-only audit re-derived the baseline itself, re-ran all 16 acceptance items, verified the partition arithmetic (242 = 90+1+60+1+90; the 152-day raw-model-fit cutoff), counted the checkboxes (10/10/6), counted the 54 retired rule IDs by hand against `168 − 54 + 13 = 127`, confirmed the four bounded `cp-0-defects.md` regions, confirmed `src/pit_capture/` and `tests/pit_capture/` absent with `evidence/cp-0` still resolving, and checked the ENTSO-E client behaviour from installed code — `entsoe-py 0.8.0` expands `curveType A03` to a gap-free 15-minute grid, and the price path's inclusive right endpoint is what produces 97 rows on a midnight-to-midnight day, confirming the half-open Berlin slice as the correct normalization. **It verified the three Reg. 543/2013 citations against the official EUR-Lex consolidated text** `02013R0543-20200101`: Art. 6(2)(b) two hours before gate closure, Art. 16(2)(b) one hour after the operational period, Art. 14(2)(d) 18:00 Brussels time — all correct as cited. **No methodological, leakage or governance defect was found.** Two P2 and seven P3 findings were raised. **Round 1** (capstone, syllabus, orchestrator, ledger, old amendment plan) reverted three anachronistic anchor repoints in the historical `gauntlet-amendment-plan.md`, replaced the §13 "backed by a DagsHub MLflow registry" phrasing, added D-CP0-20 to the ledger's closure condition, disclosed Art. 6(2)(b)'s revision channel as the second half of the existing A65 assumption, split the regulation citations into data/deadline sub-points, named the v6.2-era stranger-test and `<3 s` gates in the syllabus banner, and cleaned two formatting fragments and one stale "Month 5". **Round 2** closed what round 1 could not reach: `notebooklm-role.md`'s registry-backed sentence and its stale `M0–M5` range, and a dated **post-ratification repair addendum** in the amendment record superseding its *Application scope* bullet and acceptance items 1, 12 and 15 — written as a supersession, not as a rewrite of the ratified text. The audit's one declined finding was a proposed standard-error / minimum-effect rule on the §4.1 two-arm comparison: **rejected by the owner** as reintroducing the selection machinery AMD-2 removed. All of this is uncommitted; the repairing agent does not certify its own work.
- **2026-09-07 — second independent re-verification returned FAIL; complete repair authorized.** The independent read-only pass reproduced all 16 acceptance items and found that the prior repair had not closed the contract: live capstone text still required fitted VRE preprocessing, separately calibrated A69, a fixed `Production` registry load, an EnbPI reopen, hard speed/precomputed-serving claims and a v6.6 footer; the Orchestrator still mandated Track-A checkpoints and component-staleness commands; the rule inventory repeated obsolete Lockdown text; the spectral reading order conflicted with AMD-7; and the new fixed-96 trim rule was wrong on DST days. It also found the token absent from the current process, a fresh login shell and `launchctl`. Yarden authorized repair of every finding. The repair sweep additionally corrected all fixed-24-hour DST wording and the daily DM divisor, moved the CQR fixture's independent recomputation from M3 to CP-2, removed the last historical A65 pre-gate overclaim, and reconciled the defect-ledger status summary with D-CP0-18's MOOT disposition. These repairs are uncommitted and must receive a new independent review; the repairing agent does not certify its own work.
- **2026-09-07 (earlier) — first verification and self-repair, recorded accurately.** The first read-only verification reported twelve passes plus failures on items 9 and 13, accounting for only fourteen of the sixteen items; its record was incomplete. The subsequent repair corrected the template citations, marked the capstone delta historical, completed the template compatibility table, repaired historical version pairings/banners, updated `AGENTS.md` under separate authorization, and added the third AMD-G5 consistency region in `cp-0-defects.md`. It then checked its own repairs, which was not independent certification; the later re-verification above found the remaining semantic defects.
- **2026-09-07 (earlier) — token was verified live; two M1 data-layer findings recorded.** At that time the token returned HTTP 200 with a valid A44 document, but its `launchctl setenv` placement did not persist and it is currently absent. The sampled DE-LU response carried **`curveType A03` variable-sized blocks**, with non-contiguous `<position>` values (94/90/92 points against `max_pos=96` in three of four TimeSeries); pinned `entsoe-py` 0.8.0 expands A03 correctly. `query_day_ahead_prices` also retains the requested right endpoint: an ordinary 24-hour day queried midnight-to-midnight therefore returns 97 timestamped rows. The correct normalization is the half-open Berlin delivery-day interval, yielding 92/96/100 quarter-hours on 23/24/25-hour days — never a fixed trim to 96.
- **2026-09-07 (later) — `AGENTS.md` repaired.** The owner authorized editing the rulebook after the main application returned. A full scan found **seven** invalidated sites rather than the three reported: the provenance-block citation (§ role router), the `workbench.md` control sentence, the `engineering-role.md` § *Gauntlet execution* steps 4–5 citation, **the whole concurrent-writes bullet** (it rested on the retired staleness computation, component verdicts and `started_at_utc` topology record), two `Return Packet` terminology uses, the templates §7/§9 citations, and a "component verdicts" clause in the LAND bullet. **No policy moved** — publication authority, the `main` prohibition, tag-before-delete, citation-follows-ref, branch accountability and the two-tag LAND rule are byte-identical in substance. The templates' compatibility table was reframed: it now serves the historical documents that still cite the old numbering, which are correct to do so.
- **2026-09-07 — v6.7 ratified and applied; D-1/D-2/D-3 all YES.** The owner ratified the scope and governance reduction and granted one task-scoped Lockdown suspension over nine locked files, the anchor rename and the amendment promotion. Applied: the capstone rewritten to v6.7 and renamed `capstone_V6_7.md`; `engineering-role.md` and `gauntlet-templates.md` collapsed to one review and four forms; `orchestrator-role.md` demoted Track A and adopted the three-checkpoint flow; `program-stage-sequence.md` rebuilt as v8; banners added to `syllabus_v3_2.md` and `notebooklm-role.md`; `D-CP0-18` dispositioned MOOT and struck from the ledger's closure condition; 38 live anchor citations repointed; `src/pit_capture/` and `tests/pit_capture/` removed after `evidence/cp-0` was verified before and after; README, AWS spec, the Gauntlet amendment plan and the Hebrew guide banner-corrected; the draft promoted to `docs/track-b/capstone_V6_6-to-V6_7-amendments.md` with its drafting scaffolding collapsed to one provenance paragraph. **Three defects were found and fixed during application**, recorded in Blockers. The work is uncommitted and awaiting owner review.

- **2026-09-06 — structural change: capture dropped, Track A demoted, capstone scope amendment drafted.** The owner ended the point-in-time capture effort after six days of accumulating operational friction — a failed GitHub Actions route, a mis-set secret, an empty token file, and a machine that sleeps after one minute — and directed that the data simply be assumed present on the strength of the regulation. All automation was deleted from both GitHub and the local machine, verified. Track A moved from a mandatory gated track to an optional parallel resource, dissolving the ~6.5-week Month-0 overrun and the G0 blocker along with it. The full capstone was then read for scope reduction; a seven-item amendment is drafted and awaiting authorization (Blockers). **The Orchestrator's contribution to the friction is recorded plainly: recommending GitHub Actions for a hard-deadline job, and directing a token-file command without first checking where the token lived.**
- **2026-09-06 — GitHub Actions capture failed its first real day.** All four scheduled runs fired 3.0–3.7 hours late; every one landed past the 12:00 Berlin gate and the guard skipped all four. Zero captures, one lost delivery day. The delay was uniform rather than jitter — the free-tier scheduler deprioritising. Fail-closed behaviour was the only reason this cost a day rather than a corrupted ledger.
- **2026-09-04 — AMD-G5 waived under a granted suspension; publication-metadata question closed on live evidence.** Live probes proved the platform's update-filter machinery works on A80 but is not wired to A65, `createdDateTime` is response-generation time, and SMARD's `created` is file-build time. Record at `docs/pit-metadata-investigation.md`. No contract changed, because the premise failed.
- **2026-09-03 — owner authority clarification; D-CP0-18 remedied at `6ea6a20`.** Lockdown suspensions became task-scoped; `progress.md` confirmed as Orchestrator-owned; the operational-record exception created for the defects ledger.
- **2026-08-10 — D-CP0-20 found and remedied.** `orchestrator-role.md` denied the Orchestrator a shell while the receipt gate required it to run checks. Third instance of one pattern: the rule was right and the thing making it executable was absent.
- **2026-08-08 — Governance Lockdown ratified; CP-0 landed and reclaimed; Phase-5 matrix returned 11 of 13.**
- **2026-08-05/06 — v6.6 ratified and hardened.** Rule inventory 141 → 168. Seven independent review rounds. The clean-room CP-0 re-run validated what text review could not.
- **2026-08-04 — v6.5 capture-schedule amendment** (now superseded by the 2026-09-06 cancellation).
- **2026-07-22 — v6.3 strict-gate amendment.** A69 excluded from champion; four-catalog selection; post-gate benchmark; DuckDB mart; health report.
- **2026-06-12 — Month-0 de-risking spike.** Eight probes; record at `docs/spike-feed-status.md`, evidence SHA `20fc1ff`. Findings that still bind: revision metadata is a dead end; **no overwrite was detected for the single sampled 2026-06-13 A65/A69 delivery day — and the A69 half of that result is benchmark provenance only** (`docs/spike-feed-status.md`, v6.3 interpretation note); feature feeds are natively PT15M; A69 initial publication is post-gate; ENTSO-E↔SMARD agree 96/96 within €0.01. **A65 present at 15:35 CEST D-1 — now the corroborating evidence the KFT assumption rests on.**
- **2026-06 — launch and conversion:** G0-mid and L1–L4; the M0 spike and v6.1; PJM→DE-LU conversion; program launch 2026-06-09.

---

## Blockers / Open Questions

- **OPEN — verify `ENTSOE_API_TOKEN` live before CP-1. Blocked on ENTSO-E, not on us.** The token is restored and present; the missing piece is one successful read-only request, and the API gateway is returning 599 to *every* caller including unauthenticated and invalid-token controls (Setup State carries the four controls). **No owner action is available** — this is a wait, then a re-check. Do not issue the M1 brief until one request succeeds without exposing the value.
- **Closed 2026-09-08 — the mis-resolved merge is repaired on `origin/main` at `c8dd5cc`.** The published tree is clean. `30b1b9f` remains in the commit history carrying its markers; that was accepted rather than rewritten, because rewriting a published portfolio branch costs more than a visibly-corrected log entry. Reopen only if the visible history itself becomes a problem.
- **Convention note — the amendment sheet's location.** Every prior sheet (`capstone_V6_1-to-V6_2` … `V6_5-to-V6_6`) sits at the repository root. The owner's authorization named `docs/track-b/capstone_V6_6-to-V6_7-amendments.md`, so that is where it was written. Moving it to root would match the convention; it is a one-line `mv` plus citation repoint if wanted.
- **OPEN — the owner's income runway.** The Lab Engineer role ended July 2026. Whether income is the binding constraint decides both the Kamada question and whether this program's shape is right at all. Nothing downstream re-plans until this is answered.
- Optional/non-blocking: `.zshrc` line-137 dangling-source warning.

**Closed by the 2026-09-07 work:** the pending scope amendment (ratified and applied); the post-ratification version-marker drift (the plan is now v6.7 with a delta block and a rename); the locked anchor/map text drift (§12 rewritten, map rebuilt as v8); the public README status drift; and **`AGENTS.md`'s stale citations**, repaired 2026-09-07 under a second owner authorization — seven sites, not the three first reported. **Also closed: all seven findings of the fourth 2026-09-07 read-only consistency audit**, repaired under a third owner authorization — the capstone and role documents now treat the test/CI surface as unimplemented CP-1 acceptance scope; the residual weekly-snapshot sentence is gone; this file's archive claim is sample-bounded; addendum R-2 matches the five-hunk CP-0 diff and correct baseline count; the CP-0 archive tag is accurately described as lightweight; the stage map cites *Track C activation rules*; and the companion plan is restored at repository root from the Owner's source.

---

## Notes for Future Sessions

- **The next Track B action is exactly one M1/CP-1 brief** from `docs/track-b/gauntlet-templates.md` §1, once PRE-1 closes — the token is present, the live read-only request is the part still owed. Ten checklist items, one fresh Integration Critic, one approximate hour timebox.
- **Pre-dispatch brief validation is the operative front gate.** `BRIEF_INVALID` no longer exists; a malformed brief is corrected in conversation before repository work begins.
- **Do not re-raise:** AMD-G5, the publication-metadata substitution, the point-in-time capture ledger, the four-catalog selection system, or the forward confirmatory audit. All are closed owner decisions with their reasoning recorded.
- **CP-1's bulk pull may face a 30-day-per-request cap.** ENTSO-E's 3 September 2026 service update imposed a temporary limit: *"API requests covering periods longer than 30 days are currently not supported."* CP-1 item 1 needs **2019-01-01 → the pull date**, about 2,800 days — roughly **94 chunks per series, ~375 requests across A44, A65/A01, A69 and A75**. The migration notice says these restrictions hold *until after the migration*, so the cap may already be lifted; **it is unverifiable while the API is down and must be re-checked the moment it returns.** As of 2026-09-08 the migration is complete but the API is still off, so the cap's status remains unknown; `~/pre1_check.py` tests it with a 60-day request and reports the answer in the same run that clears PRE-1. This interacts directly with CP-1 item 2's chunk-stitching requirement — the chunk boundaries would be externally imposed and numerous. **The File Library / FMS API stayed functional throughout the Web API outage** and is a legitimate alternative route for a bulk historical pull; naming it as an option belongs in the CP-1 brief, and the choice is the Lead's.
- **Redact before logging any ENTSO-E error.** `entsoe-py` 0.8.0 puts `securityToken` in the query string, so the credential appears in request URLs and in raised exception text. The CP-1 brief must carry this as a named constraint.
- **A conflict marker in a governance file is a first-class defect.** Grep the tree for `^<<<<<<<`, `^=======$`, `^>>>>>>>`, `Updated upstream` and `Stashed changes` when opening an orchestration session — `30b1b9f` published six of them to `origin/main`.
- **Both disclosed assumptions must appear wherever the leakage audit is claimed** — A65 pre-gate availability and A75 revision — in §5.2, the README limitations, and the static page. An assumption stated plainly is defensible; an assumption presented as a measurement is not.
- **The holdout is opened once.** Its DM result is labelled *confirmatory-style, not power-qualified*. There is no re-tuning and no second run after it is read, and the model that ships is the model that was evaluated.
- At each Track B `PASS`: close only that checkpoint, run the §4 landing inspection, take one disposition, take **both** tags, regenerate this file, and ask "Authorize the next stage?"
- After CP-3, ask whether the companion or any cloud work is worth opening. Neither starts on its own.

---

*Regeneration diff, 2026-09-08. **A targeted regeneration recording the landing and the merge repair — not a rewrite.** **Rewritten:** the header (the v6.7 application is landed and published at `30b1b9f`; what is uncommitted now is the repair); Track B's next-action bullet and its Governance-Lockdown bullet (the fourth task-scoped authorization); Setup State's token item (PRE-1 restored, live verification still open, with the platform-fault evidence); and the token Blocker. **Added:** two Session Log entries for 2026-09-08 — the merge damage and its deletion-only repair, and the PRE-1 re-check; a Blocker recording that `origin/main` still carries the mis-resolved merge and that only the owner can correct published history; and two Notes — redact `entsoe-py` errors before logging, and grep for conflict markers at session start. **Unchanged:** Strategic Anchors, Standing Scope Decisions, Track C, and every Session Log entry dated 2026-09-07 or earlier. **Nothing ratified was changed by this file.** `capstone_V6_7.md` v6.7 remains the anchor. The repair this file records landed at `c8dd5cc` and is published; its author does not certify it.*
