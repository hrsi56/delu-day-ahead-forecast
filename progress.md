# Yarden's Triple-Track Progress Log

*Living document. Regenerated in full under the `orchestrator-role.md` contract. Last updated: **2026-08-04 — owner-ratified capture-schedule feasibility amendment. Flagship capstone v6.4 → v6.5 (content); the point-in-time capture leaves CP-1 and splits into new M0.5/CP-0 + owner-run B-Man-PIT + CP-1 verification. Track B's next pending checkpoint is now CP-0, not CP-1. Syllabus remains v3.2; stage map rebuilt v6 → v7. Corrective consistency pass later the same day: verdict-staleness rule aligned to the tooling across plan/templates/amendment record, reserve-vs-ceiling citations fixed, `st_nlink` log entry corrected, Month-0 pace FLAG restored. **2026-08-05: Gauntlet prune Phase 1 complete and verified — 138 rules inventoried and single-owned across capstone §12 / `engineering-role.md` / templates / `cp2-blind-protocol.md`; D-1/D-2/D-3 plus two verified near-drops (G4, I9) closed; agent-facing prose 10,808 → 8,506 words. Phase 2 Stage 1 executed (`st_nlink` and mode-enforcement gates removed, 5,649 → 5,626 lines); Phase 2 remainder ABORTED on the merits — the CP-2 commitment layer is already honestly labelled, size does not apply to executed-not-read code, and a refactor bug would first surface at CP-2 itself. Reserve raised 24 h → 48 h (envelope ≈753 h). No version bump; v6.5 remains the anchor. Next: CP-0.***

---

## Current Position

### Track A — Learning

- **Anchor:** `syllabus_v3_2.md` v3.2, semantically rebased 2026-08-02 to the flagship plan. No learning topic, depth, deliverable, or Track A checkpoint changed by the 2026-08-04 amendment.
- **Phase:** I, Mathematical Foundations. **Month:** 0, week 6 (launched 2026-06-09). No later completion evidence has been supplied; do not infer it from elapsed calendar time.
- **L1–L4 + G0-mid — CLOSED.** Four subspaces through least squares/Gram–Schmidt and eigen/diagonalization at [AUTH]. Month-0 deliverable #1 shipped. G0-mid closed 2026-07-06 with one gap—`AᵀA` geometry ↔ eigenvalues ↔ condition number—routed into L4/L5 and due for explicit closure in the G0 verdict.
- **L5 — ISSUED 2026-07-15, checkpoint block.** SVD/PCA + condition number; PCA [APPLIED-AUTH], SVD/Eckart–Young [REC], condition number [REC]. Deliverable #2 + written interview answer + NotebookLM consolidation verdict with explicit `AᵀA`-gap-closure statement.
- **Next pending checkpoint:** **G0**. Required one-liner: both Month-0 deliverables runnable + NotebookLM verdict, including the explicit gap-closure statement.
- **After G0:** Month 1 opens; C7 networking begins; the authorized C2+C8 Track C floor runs at the Month-0→1 seam unless Yarden changes that scheduling decision.
- **Pace — FLAG (restored 2026-08-04; dropped in error at the 08-04 regeneration).** Month-0's six-week envelope ended **~2026-07-21**. L5 was issued 2026-07-15 and G0 is still open, so Month 0 is running roughly **two weeks long** and no Track A block has closed since. The 07-22 → 08-04 period went to Track B governance (v6.4/v6.5 + the Gauntlet protocol) while Month 0 stayed open. Month 0 remains the binding constraint; nothing downstream re-plans on it, but the overrun is real and is not absorbed anywhere below. **Envelope:** ≈729 h (prior ≈727 h + the 2 h CP-0 reserve). Months 2–4 remain ~5–5.5 weeks at 20–22 h/week. **G5 ≈ early/mid-February 2027; G6 ≈ early/mid-April 2027**, both assuming G0 closes without further slip. Applications still open after G3 and no later than Month 5; ALG still closes at G4.

### Track B — Capstone Builds

- **Flagship anchor:** **`capstone_V6_5.md` v6.5**, owner-ratified 2026-08-04. Supersedes v6.4 while preserving the strict-gate scientific architecture and every v6.4 statistical correction. The prior full v6.4 plan remains retrievable from Git as `97627a4:capstone_V6_4.md`.
- **Position:** **M0 DONE. M0.5 NOT STARTED. CP-0 PENDING.** M1 not started; CP-1 pending behind CP-0 and B-Man-PIT. The Month-0 feed spike remains the completed engineering artifact. No active Track B brief, agents, branch, or `workbench.md` exists.
- **v6.5 amendment (2026-08-04) — capture-schedule feasibility.** Scope-preserving; no scientific criterion weakened, removed, or relabelled. The §3 point-in-time capture contract kept every requirement (three non-consecutive delivery days, two pre-gate instants, fail-closed on partial/rate-limited requests) but no longer executes inside CP-1. The capture spans ≥5 calendar days while CP-1 carries a ~6 h active-elapsed ceiling, and neither reading of the eligible-pause rule survives that: treating the wait as a pause freezes all CP-1 work for days, and not treating it as one exhausts the ceiling on day one. The work therefore splits three ways:
  - **M0.5 / CP-0** — a new bounded Gauntlet checkpoint whose only scope is authoring and independently reviewing the capture instrument. No calendar dependency.
  - **B-Man-PIT** — owner-run manual block that executes the reviewed instrument on the qualifying days. No agent, no Gauntlet clock; waiting for a publication date needs no engineering judgement.
  - **CP-1** — keeps the bar but a fresh Critic *verifies* the resulting ledger (hashes, UTC provenance, DST handling, D+1 completeness) rather than producing it. An incomplete ledger returns `BLOCKED` naming the missing qualifying days, never a weakened bar.
- **Why CP-0 exists rather than an unreviewed instrument:** the capture ledger is CP-1 checklist evidence. An instrument authored outside any review regime would put CP-1's `PASS` on an uninspected tool — the exact hole the architecture closes, displaced one step earlier. CP-0 is also the first checkpoint executed under `engineering-role.md`, so its Return Packet is that contract's operational validation on a genuinely needed artifact.
- **Scheduling consequence — act early.** B-Man-PIT is calendar-bound and long. CP-0 and B-Man-PIT should be scheduled as early as the owner's capacity allows, ahead of M1, so the ledger is complete when M1 opens. Deferring them to the M1 seam adds their full calendar span to the critical path.
- **v6.4 statistical corrections (carried forward unchanged):**
  - the five pinned folds are development/catalog-selection/stress evidence; their DM p-values are labeled descriptive/post-selection;
  - one future audit window remains sealed until the pre-registered `max(90 complete days, N80)` and ≥12-calendar-week eligibility rule is met, then runs once only;
  - the hashed A65/A01 point-in-time capture ledger is required as before, now produced under B-Man-PIT and verified at CP-1; the selection cutoff/audit quarantine still freezes at M1;
  - CP-2 requires complete hypothesis/parent/delta/budget/result/decision experiment lineage; no minimum run count;
  - M3/CP-3 independently recomputes the hand-checkable CQR order-statistic fixture and a persisted real calibration fold.
- **Execution contract:** the Orchestrator authorizes exactly one repo/checkpoint with the exact anchor, citation to the complete named CP/FCP checklist plus a supporting extract, and one numeric active-elapsed wall-clock ceiling. Engineering-Lead autonomously runs Builder → fresh Critic → repair → fresh Integration. Every Critic works from a clean detached snapshot without `workbench.md`; its verdict binds the full candidate SHA, exact plan/bar citation, decision-bearing input/data hashes, exact commands/tolerances, and unchanged before/after Git tree-integrity evidence. Component verdicts declare `reviewed_paths` and staleness is computed from them: a repair invalidates only the verdicts whose reviewed paths it touched. Integration uses a separate clean checkout. At terminal return the Lead freezes a committable copy of the evidence records out of ignored `.gauntlet/`. Yarden carries one brief in and one consolidated Return Packet out. Only the Orchestrator may extend the ceiling. Every terminal result stops before the next checkpoint, which requires Yarden's explicit go/no-go.
- **Gauntlet planning reserve — RAISED 24 h → 48 h (owner-ratified 2026-08-05)** to remove time as a constraint on the Phase 2 prune decision. Envelope **≈729 h → ≈753 h**. Per-checkpoint baseline unchanged: CP-0 **2 h**, CP-1 **6 h**, CP-2 **5 h**, CP-3 **5 h**, CP-4 **3 h**, CP-5 **3 h** = 24 h, plus **24 h uncommitted headroom**. **Consumed to date ≈4 h** (prune Phase 1 ≈3 h, Phase 2 Stage 1 ≈1 h); ≈44 h remain. **Open — see Blockers:** the increase was authorized to unblock a decision that then went the other way (Phase 2 remainder aborted), so the 24 h of headroom is currently capacity without a named consumer. This is planning-load capacity added to the milestone estimates, not additive agent-effort accounting and not a stand-alone runtime ceiling. Each issued brief states one combined active-elapsed ceiling; parallel contexts overlap on that clock and never sum. B-Man-PIT consumes no Gauntlet reserve.
- **CP-0 scope:** flagship §12 M0.5 checklist — standalone instrument requiring only `ENTSOE_API_TOKEN`; append-only immutable ledger; fail-closed `not_qualifying` on partial/rate-limited responses with a positive control; UTC `pulled_at` never rewritten as first-publication time; Berlin DST handled per §4.0 including fall-back-day length; hash-bound raw captures; mandatory independent Critic PASS with Critic-materialized fixtures (truncated response, rate-limit response, Berlin fall-back day); fresh Integration `PASS`.
- **CP-1 scope:** flagship §12 full checklist — feeds and reconciliation, UTC/DST/PT15M integrity, A75 archive/fit lineage, strict-gate runtime firewall, **verification of the B-Man-PIT capture ledger**, frozen audit boundary, DuckDB SQL-A mart, spectral EDA, invariant/property evidence, attribution — plus five mandatory independent acceptance oracles: adjacent PT15M chunk stitching; missing-quarter fail-closed behavior; Berlin fall-back-hour identity/25-row day; A75 proper-training-only fit isolation with a proper-training positive control; and champion-vs-benchmark schema/actuals firewall. CP-1 also requires fresh Integration `PASS`.
- **Pending B-Manual:** **B-Man-PIT** (run the CP-0-reviewed capture instrument on ≥3 non-consecutive delivery days at ~10:30 and ~11:45 Europe/Berlin on D-1; gates CP-1) — blocked until CP-0 closes. DagsHub account before M2. Optional 10-minute SMARD gas-page check pre-M1. B-Man3 (Kaggle account/token/rules) stays in Month 5 and opens the companion gate.
- **Companion anchor:** `Binary Classification Mini-Capstone.md` v1.0, stage-gated. Do not read, brief, or load it before B-Man3/FM0. Its Months 6–7 sequence is unchanged.
- **DEC-AWS:** `aws-extension-spec_v1_1.md` remains **PARKED/UNRATIFIED** until G5. Its baseline is now the v6.5 content; **ratification would produce capstone v6.6 + map v8** (v6.5 and map v7 were consumed by the capture-schedule amendment) and move the envelope **≈729 → ≈779–799 h**, with G6 roughly early/mid-June 2027. No AWS Track B action before G5.
- **Next pending checkpoint:** **CP-0**, but it does not activate until the Orchestrator schedules and issues a valid M0.5/CP-0 brief with a numeric ceiling.

### Track C — Marketing: FROZEN by default

- Post-M2 phase trigger remains: outreach/prep/pipeline-building begins after M2; active applications open only after SQL-B completes at G3 and no later than Month 5. The sealed forward audit may still be pending and does not delay this trigger.
- **Geography:** ≤3 office days/week TLV/Herzliya; Beer Sheva base.
- **C2 floor — authorized, pending:** LinkedIn headline/About/Featured and README polish; recruiter-only open-to-work deferred to M2 trigger; Lab-Engineer framing fix closed.
- **C8 research — pending at the G0 seam:** target-company list plus take-home/live-screen ratio (DEC-ALG) and SQL/A-B/cloud/LLM requirement ratios (DEC-AWS/positioning).
- **C7:** networking floor starts Month 1, ~1 h/week.
- **Positioning:** career changer from clinical research to industry DS; flagship demonstrates strict-gate probabilistic forecasting, honest uncertainty and scientific audit discipline; companion later adds imbalanced decisioning/calibration.
- **Artifacts/pipeline:** none published; applications not started. Three CV iterations remain after M2, M3, and M5. Companion/AWS evidence later uses manual surface updates, not a fourth slot.

---

## Setup State

- **Uncommitted v6.5 package — owner action pending:** the capture-schedule amendment, the plan rename, the map v7 rebuild, and the accompanying Gauntlet-protocol changes sit in the working tree of `main`, unstaged and unpushed, per `AGENTS.md`. The v6.4/v6 governance release itself was published earlier. Review and commit is Yarden's decision.
- **Project Knowledge swap — owner action pending:** use `capstone_V6_5.md` v6.5, `syllabus_v3_2.md`, `program-stage-sequence.md` v7, this `progress.md`, the current role/router docs, and the parked AWS spec. Retire current-facing v6.3/map-v5 copies. Keep historical amendment records; the prior full v6.3 plan remains retrievable from Git as `0b9c5cb:capstone_V6_3.md`.
- **NotebookLM swap — owner action pending:** load only `syllabus_v3_2.md` and the flagship plan file. Do not load the stage map, AWS proposal, progress, role docs, or amendment history.
- **Companion:** Project Knowledge copy may remain present but must not enter NotebookLM, an engineering repo, or a brief before FM0.
- **Role routing:** `AGENTS.md` is the canonical short router; `CLAUDE.md` points to it; `orchestrator-role.md` governs program management; repo-root `engineering-role.md` governs one authorized Track B checkpoint.
- **Canonical templates:** `docs/track-b/gauntlet-templates.md`; the CP-2-only label-blind protocol lives separately in `docs/track-b/cp2-blind-protocol.md` and is read only when CP-2 is authorized. No template is an active brief or workbench.

---

## Strategic Anchors

- **Target:** industry Data Scientist at NIS 35K. Audience: DS hiring manager, not MLE.
- **Authoritative documents:** `syllabus_v3_2.md` v3.2 + `capstone_V6_5.md` **v6.5** + stage-gated `Binary Classification Mini-Capstone.md` v1.0. `progress.md` decides which versions are ratified.
- **Planning aid:** `program-stage-sequence.md` **v7 (2026-08-04)**, static/non-anchor; rebuilt on owner authorization to add the M0.5/CP-0 and B-Man-PIT rows. Every v6 stage is preserved in the same order. FM/FCP rows remain unverified against the gated companion plan until B-Man3.
- **Parked proposal:** `aws-extension-spec_v1_1.md`, semantically rebased 2026-08-02, numbering corrected 2026-08-04; produces capstone **v6.6** and map **v8** only if ratified at G5.
- **Amendment chain:** `capstone_V6_1-to-V6_2-amendments.md`; `capstone_V6_2-to-V6_3-amendments.md`; `capstone_V6_3-to-V6_4-amendments.md`; **`capstone_V6_4-to-V6_5-amendments.md`**; `syllabus_v3_0-to-v3_1-amendments.md`; `syllabus_v3_1-to-v3_2-amendments.md`.
- **Repos:** flagship `hrsi56/delu-day-ahead-forecast`; companion fraud repo created at FM0; standalone CNN repo at DL; public ALG solutions repo at ALG-1.
- **Budget/hardware:** $0 expected run rate, $65/month policy ceiling; M3, 16 GB, CPU-only. DEC-AWS may later amend only the flagship run-rate line.
- **Program envelope:** ≈729 h; G5 early/mid-February 2027; G6 early/mid-April 2027. Dates are delivery estimates, not an offer guarantee.
- **Language:** Orchestrator/Engineering-Lead replies and briefs in English; Hebrew input is fine.

---

## Standing Scope Decisions

- **v3.1 expansion remains ratified:** L-OPT + DL riders, L-MTX, spectral EDA, CLS-1/2, ALG, UNSUP/CAUS/ERR, and the companion arc. Hard caps and the capstone-wins conflict rule remain.
- **v6.3 strict-gate architecture carries forward:** delivery-day A69 and derivatives are forbidden from champion runtime; the two gate-feasible proxy arms share one proper-training-only A75 climatology; exactly four matched catalogs decide retention at CP-2; "no proxy survives" is valid; §7.2 A69 arm is benchmark-only; three-stage coverage separates formal post-CQR from empirical final post-isotonic coverage.
- **v6.4 Gauntlet/statistical-audit amendment is ratified:** forward sealed audit, capture ledger, experiment lineage, CQR rank fixture, mandatory independent acceptance oracles, clean-detached Critic provenance and tree-integrity checks, fresh Integration verdict, one measured active-elapsed ceiling, Return Packet, and stop-before-next-stage.
- **v6.5 capture-schedule amendment is ratified (2026-08-04):** the point-in-time capture keeps every criterion but executes as CP-0 (instrument, reviewed) → B-Man-PIT (owner-run captures) → CP-1 (independent ledger verification). A checkpoint ceiling is never inflated to absorb calendar waiting; calendar-bound work with no engineering judgement is routed to a manual block instead.
- **Mandatory M1 acceptance oracles:** adjacent PT15M chunk stitching; missing-quarter fail-closed behavior; Berlin fall-back-hour identity/25-row day; A75 proper-training-only fit isolation plus proper-training positive control; champion-vs-benchmark schema and actuals firewall. Later applicable independent surfaces remain the CP-2 label-blind four-catalog metric recomputation from frozen predictions and the M3 CQR threshold recomputation.
- **DuckDB SQL mart:** SQL-A integrates/tested at M1 before CP-1; SQL-B completes through G3; one 20 h combined cap. Spectral EDA and SQL-A are the only supplementary firewall exceptions.
- **Offline health report:** M4/CP-4 generation, CP-5 static rendering; input/output health only, not performance drift; ≤~8 h; no service/dashboard/scheduled retraining/AWS.
- **Static first touch:** GitHub Pages is primary; marimo Space is labeled deep-dive; no keep-alive; Monday snapshot + static export; CP-5 <3 s cold-cache static gate.
- **Market/data:** DE-LU via ENTSO-E + SMARD, CC BY 4.0; PJM is closed. No gas layer unless a CC-BY/API daily THE price series appears. No external weather. A75 is fit-target-only. Hourly target spans the 15-minute MTU transition with full-bin integrity.
- **Model/scope:** single LightGBM quantile ensemble; CQR then isotonic; no neural challenger, trading layer, live session pulls, DVC, enterprise monitoring, EnbPI/SPCI/Giacomini-White unless the one ≥10 pp regime-coverage remediation trigger fires.
- **Validation claims:** five pinned folds are development evidence. Public forward-audit status is reported explicitly as `PENDING_UNDERPOWERED`, `CONFIRMED`, or `NOT_CONFIRMED` and is never recycled into model selection; "sealed" and "eligible" describe lifecycle, not additional verdicts.
- **Experiment evidence:** complete decision lineage replaces minimum-run counts. Thin CI runs invariant/property tests and the separate CQR fixture after M3; no regression matrix.
- **Track B authority:** Orchestrator decides what/when/repo/checkpoint/ceiling; Engineering-Lead decides how and manages subagents; the Return Packet—not the workbench or silence—is the only upward checkpoint evidence.
- **Publication authority:** all agent work stays local. No push, no PR, no commit to `main`; agents leave work in the working tree and hand over a diff. Publication is Yarden's alone.

---

## Session Log — newest first

- **2026-08-05 — GAUNTLET PRUNE PHASE 2 — STAGE 1 EXECUTED, REMAINDER ABORTED ON THE MERITS.** Owner raised the Gauntlet reserve 24 h → 48 h to remove time as a constraint, then authorized the "Core" variant. **Executed:** `scripts/gauntlet_protocol.py` 5,649 → 5,626 lines — `st_nlink` gates removed from both sites (**zero occurrences repo-wide**), `_require_mode` and **all six call sites** removed, custody snapshot stripped of its mode and hard-link gates while keeping the functional hash/size comparison that proves reveal did not mutate custody. `0o600`/`0o700` **creation** modes retained by design. Suite green at every step. **Aborted:** the commitment / preimage / receipt / seed layer. The `cp2_permutation` → `SystemRandom` change broke two callers and the fix cascades irreducibly into `prepare`, five validators, and the freeze/reveal chain; that work is atomic and was reverted rather than left half-applied. **Three reasons it was abandoned rather than rescheduled.** (1) **It is already honestly labelled** — the threat boundary mandates `COOPERATIVE_PROCEDURAL` absent a real read sandbox and states verbatim that disclosure "does not turn hashes into proof of blindness," so the system does not overstate itself; the correctness-and-honesty argument that justified the cut holds only for what Stage 1 removed (silent gates blocking nothing, since copying is permitted and yields an identical hash). (2) **What remains is a size argument, and size does not apply** — `gauntlet_protocol.py` is executed, never loaded into agent context; Phase 1 already removed the prose that was actually costing us. (3) **Risk asymmetry** — the blind protocol makes the CP-2 four-catalog selection honest, and a refactor bug would first surface at M2/CP-2 in Month 3 with no intervening run to catch it, at the exact moment the protocol is needed. **Rule ledger: no rule fully retires, count stays 138.** G4 loses its `st_nlink` clause; O4 loses mode *enforcement*; **O9 is now VESTIGIAL** — reveal still copies rather than hard-links, but the rule it enforced against is gone, so it is correct behaviour that must no longer be cited as a control. G2, O5, O12 untouched. **Re-entry trigger:** do not schedule the simplification standalone; if CP-2 code is modified before Month 3 for any other reason, do it then. Full record in `docs/track-b/rule-inventory.md`. **No version bump**; no bar, checklist item, invariant, or acceptance criterion changed. Next: **CP-0**.
- **2026-08-05 — GAUNTLET PRUNE PHASE 1 (prose de-duplication) — COMPLETE, VERIFIED.** Owner-approved ≤8 h prune charged to the 24 h Gauntlet reserve, not added to the envelope (≈729 h unchanged). Built a full **rule inventory** first (`docs/track-b/rule-inventory.md`, new): all four prose documents read end-to-end, every normative rule enumerated with its current locations and assigned post-prune owner. **Total is 138 rules, not the 148 first written — an addition error in the inventory header, corrected in the verification block; per-domain counts were right throughout.** Assigned one owner per rule: **capstone §12 = the bar (27)**, **`engineering-role.md` = the process (85)**, **`gauntlet-templates.md` = forms (16)**, **`cp2-blind-protocol.md` = CP-2 only (10)**. The inventory immediately surfaced three live defects and the verification pass surfaced two more. **D-1:** the rerun-all rule survived in three further places the 08-04 targeted grep missed (`engineering-role.md:40`, `:196`, `gauntlet-templates.md:372`) — all closed; zero matches repo-wide. **D-2:** capstone reserve table omitted CP-0, reading 22 h against progress.md's 24 h — corrected to CP-0 2 h + CP-1 6 h + CP-2 5 h + CP-3 5 h + CP-4 3 h + CP-5 3 h = 24 h. **D-3:** brief template cited §12 as CP-2 protocol authority — repointed to `cp2-blind-protocol.md`. Verification then caught two genuine near-drops: **G4** (`st_nlink`/hard-linked-inode invalidity) existed only in the deleted capstone paragraph and a form document, never in its owner — restored to `engineering-role.md`; **I9** (Lead allocates internal windows, cannot enlarge the ceiling) was only implicit across three sentences — now explicit. All 138 verified present in their assigned owner by normalized-whitespace match. **Measured: 10,808 → 8,506 words loaded per checkpoint (−21%)**, with §12's execution contract −57% and CP-2's 2,338-word protocol now off every non-CP-2 checkpoint. **The ~4,000-word target was not reachable and is recorded as such** — it predated the enumeration, and 85 rules legitimately owned by the role doc set the floor for a relocation-only pass; the deeper cut is Phase 2, which removes rules rather than moving them. **The nine Phase-2 candidates (G2, G4, O4, O5, O9, O12) were relocated fully intact, none removed.** Suite green before and after (69 passed, 18 subtests); no code, schema, or test touched. **No version bump** — relocation only, no bar, checklist item, invariant, or acceptance criterion changed; v6.5 remains the anchor and DEC-AWS still cascades to v6.6. Next: **Phase 2 (Python cuts)**, then CP-0, then Phase 3 on CP-0's measurements.
- **2026-08-04 (later) — CONSISTENCY CORRECTION: verdict staleness, ceiling citations, `st_nlink`.** Orchestrator review of the six Gauntlet commits found the ratified plan contradicting its own tooling. **(1) Verdict staleness — the blocking defect.** The 08-04 pass replaced rerun-everything-on-any-change with computed staleness from declared `reviewed_paths` in `gauntlet_protocol.py`, the verdict schema, and `engineering-role.md` — but not in `capstone_V6_5.md` §12, the two `gauntlet-templates.md` Integration passages, or the amendment record. Under version precedence the plan governed, so it forbade the reuse the tool implements: at CP-1's 16 checklist items and three mandatory M1 surfaces, every integration commit would have forced a full rerun of all of them. All four statements now match the tool: a component `PASS` binds iff its candidate is an ancestor of the final candidate, the diff restricted to its `reviewed_paths` is empty, and the declared set matches the referenced record; only stale verdicts rerun; a current component verdict binds its own earlier SHA/tree and only Integration runs at the final SHA/tree; `reviewed_paths` must be declared honestly and broadly, which is the rule's soundness condition. **(2) Reserve vs. ceiling.** `capstone_V6_5.md` §M0 and the v6.5 amendment sheet argued from "CP-1 carries a ~6 h ceiling," but §12 and this file both define that 6 h as planning reserve combined with the milestone allocation at issue time — explicitly *not* a stand-alone runtime ceiling. Reworded to the structural argument (calendar span vs. active-elapsed work), which holds at any ceiling; the M0.5/CP-0 + B-Man-PIT split is unaffected. **(3) `st_nlink`.** The 08-04 log entry claimed the `st_nlink` and two-record run-root mechanisms were removed. They were not — `gauntlet_protocol.py:411`/`:3622`, `test_cp2_blind_protocol.py:509`, §12, and the v6.4 sheet all still enforce them. What was withdrawn was the same-UID non-observation *claim*. Log corrected; addenda recorded in both amendment sheets, with the v6.3→v6.4 sheet's superseded rule preserved unedited and marked historical. **No version bump** (corrective consistency pass, per the `936aa8a`/`fef80f4` precedent); no scientific criterion, checklist item, invariant, or acceptance bar changed. Suite re-run green. **Also restored the Month-0 pace FLAG**, dropped in error at the 08-04 regeneration and not eligible for pruning while G0 is open.
- **2026-08-04 — v6.5 RATIFIED (capture-schedule feasibility) + Gauntlet protocol made executable.** Adjudicated a collision between the §3 capture contract (≥5 calendar days) and CP-1's ~6 h active-elapsed ceiling, which no reading of the eligible-pause rule survived. Split the work into M0.5/CP-0 (instrument, independently reviewed), B-Man-PIT (owner-run captures), and CP-1 (independent ledger verification); rejected inflating the ceiling because it measures work, not calendar span, and inflating it would gut `BUDGET_EXHAUSTED` and corrupt the 22 h reserve feeding the program envelope. Track B's next pending checkpoint moved CP-1 → CP-0. Renamed the plan `capstone_V6_4.md` → `capstone_V6_5.md` and updated every reference; set the CP-0 reserve at 2 h (reserve 22 → 24 h, envelope ≈727 → ≈729 h); rebuilt the stage map v6 → v7 with M0.5/CP-0 and B-Man-PIT rows on owner authorization. Corrected DEC-AWS numbering to capstone v6.6 + map v8. Separately, reviewed the 2026-08-02 protocol implementation and fixed what blocked execution: computed verdict staleness from declared `reviewed_paths` replacing rerun-everything-on-any-change; `scaffold-verdict` to end hand-transcribed hashes; `freeze-evidence` so verdict records leave git-ignored `.gauntlet/` and become committable at terminal return; the CP-2 label-blind protocol moved to its own file so it is not loaded at other checkpoints; the Orchestrator receipt gate replaced with reported `validate-verdict`/`verify-ref` exit codes it can actually check; the same-UID *non-observation claim* withdrawn — modes and hashes no longer presented as proof that another same-UID process cannot observe custody, so the honest label is `COOPERATIVE_PROCEDURAL` absent a real read sandbox (**corrected 2026-08-04:** an earlier wording here said the `st_nlink` and two-record run-root *mechanisms* were removed; they were not, and both remain enforced in tool, tests, plan, and amendment sheet); a deterministic post-review `verify` trap on ignored byproducts fixed; `pytest` declared as a dependency. **No M1 or CP-0 product code started; no active workbench; nothing committed.**
- **2026-08-02 — v6.4 / map v6 RATIFIED, AUTHORED, AND PACKAGED (documentation/governance only).** Sealed forward audit, multi-day point-in-time capture, complete experiment lineage, finite-sample CQR indexing fixture, five mandatory M1 independent acceptance oracles, clean-detached Critic provenance/tree-integrity checks, fresh Integration review, one active-elapsed wall clock, one Return Packet, bounded terminal statuses, workbench lifecycle, and hard checkpoint stops. Added 22 h of planning-load reserve and rebased dates/future AWS cascade. Replaced root routing and Engineering-Lead contract; created canonical templates, snapshot-verification helper, and Hebrew guide.
- **2026-07-22 — v6.3 strict-gate amendment:** A69 excluded from champion, proxy lifecycle/four-catalog selection, post-gate benchmark, three-stage coverage, DuckDB mart, health report; syllabus v3.2 and map v5 authored.
- **2026-07-20 — AWS/D7 + role split:** D7 static-first-touch ratified; AWS D1–D6+D8 parked; `engineering-role.md` separated.
- **2026-07-15 — L5 issued; map audit; companion stage gate reaffirmed.**
- **2026-07-12 — expansion ratified:** v3.1/v6.2/companion v1.0.
- **2026-06 — launch and conversion:** G0-mid/L1–L4; spike and v6.1; PJM→DE-LU conversion; program launch 2026-06-09.

---

## Blockers / Open Questions

- **Next user evidence:** G0 one-liner. Until supplied, Track A stays at Month 0.
- **CP-0/B-Man-PIT scheduling vs G0:** M1 remains gated behind G0, but CP-0 and B-Man-PIT are not learning-dependent and are calendar-bound. Recommend starting them before G0 closes so the ledger is ready when M1 opens. Needs Yarden's scheduling decision.
- **C2+C8:** scheduled for the post-G0 seam unless Yarden changes it.
- **Gauntlet reserve headroom — decision pending.** The reserve was raised 24 h → 48 h on 2026-08-05 specifically to unblock the Phase 2 prune decision; that decision then went the other way (remainder aborted). ≈44 h remain against ≈24 h of named per-checkpoint need. Hold the headroom for CP-0–CP-5 overruns, or revert to 24 h and return ≈24 h to the envelope (≈753 h → ≈729 h)? Not blocking CP-0 either way.
- **`O9` is vestigial** (Phase 2 record): reveal still copies rather than hard-links, but the `st_nlink` rule it enforced against is retired. Correct behaviour, no longer a control — do not cite it as one in a CP-2 packet.
- Optional/non-blocking: 10-minute SMARD gas-page check before M1; `.zshrc` line-137 dangling-source warning.

---

## Notes for Future Sessions

- At the next opening, close/remediate G0 from the NotebookLM verdict; do not infer success from silence because G0 is a predefined Track A checkpoint.
- Month-1 planning uses map v7 L6–L13 + L-MTX; C2+C8 at the seam; L-OPT precedes the standalone CNN at the Month-1→2 seam.
- **When CP-0 is scheduled, issue exactly one valid M0.5/CP-0 brief** from `docs/track-b/gauntlet-templates.md`: flagship repo, the flagship plan file with its content version stated, citation to the complete CP-0 checklist plus a task-specific supporting extract from §3 and §4.0, Orchestrator-reported expected state for the Lead to verify, one numeric active-elapsed ceiling, owner actions (`ENTSOE_API_TOKEN` already available), and the stop contract. Do not create the workbench upstream.
- **After CP-0 `PASS`, issue B-Man-PIT** as a manual block naming the milestone served, the exact commands, the qualifying-day rule (≥3 non-consecutive delivery days, ~10:30 and ~11:45 Europe/Berlin on D-1), and the rule that a failed/partial/rate-limited attempt is repeated on another day. Track it as a CP-1 prerequisite and do not open CP-1 until the ledger reports three qualifying days.
- **When M1 is scheduled, issue exactly one valid M1/CP-1 brief** on the same pattern, with the expected-state paragraph naming the completed B-Man-PIT ledger path and hash for the Lead to verify.
- CP-2 freezes champion lineage and the forward-audit contract and runs the mandatory label-blind four-catalog chain from `docs/track-b/cp2-blind-protocol.md`; CP-3 adds the CQR fixture/real-fold recomputation and honest audit state; CP-4/CP-5 preserve lineage and cross-surface claim consistency.
- At each Track B `PASS`, close only that checkpoint, update this file, summarize evidence, and ask: **"Authorize the next stage?"** Do not issue or begin it before Yarden answers. Confirm the packet names a frozen evidence root, not only live `.gauntlet/` paths.
- CP-0's Return Packet is the first operational test of the `engineering-role.md` contract. Read it for protocol defects as well as for its checkpoint verdict, and route any contract fix as its own task.
- At B-Man3, verify the gated FM/FCP map rows against the companion plan before issuing FM0.
- At G5, adjudicate DEC-AWS on C8 cloud requirement ratio, live funnel/interview evidence, and capacity. If approved, D8 itself authorizes capstone v6.6 + map v8; no second map approval.
